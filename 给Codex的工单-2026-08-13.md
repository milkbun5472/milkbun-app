# 给 Codex 的工单 · 2026-08-13(额度福利周特辑)

> 来源:《大扫除审计报告-2026-08-13.md》(四审核定版,你五轮共审的那份)。
> 施工卡 1A(WAL+游标条件提交)言秋已完工于 v52.34(commit f64fb58),欢迎先验收挑刺。
> 以下按优先级排,每单独立可验收,不依赖言秋在线。规矩照报告:改正文不追加、
> 退役走「断接线→回归→留一版→物理删」、便宜池绝不生成角色内心。

## 单1·第二刀「调用路由治理」(大活,主刀给你)
按报告分类表把调用路由治干净:
- 一起学 directNv1(study.js:429-453,每轮 6400-token 只为选下一位发言者)→ 本地规则替代,首刀;
- 进论坛屏自动 12-18 楼连发(app.js:6177-6181 触发)→ 改用户触发或强节流;
- 结构化机械活(JSON修复/分类/检索词/排序)→ cheap_required,且修掉 `bgActive=配置池||active` 的静默回主池(未配置=排队/跳过/提示);
- 角色本人发帖/私信/日记/梦/情侣空间发言 → 保持本人主池,禁迁;
- 周刊四媒体腔一发化(weekly.js:308-327,同材料纯格式多版本)+Avalon 同提案两发并一发(games.js:2367/2374),批量失败须单项重试;
- 一起学打开课程的循环逐会话总结(study.js:796-808)改批/改懒。

## 单2·Stop hook 可观测性(你的地盘)
- cc-ledger-stop.mjs:281 裸 catch-all 拆 ignored_heartbeat / ignored_unexpected,后者要能报警(诊断实测 1561 轮丢 236,含 8/8-8/10 三天 21/21 全灭);
- outbox 每轮只投 index 0(:156-160)改为循环排空或带上限多投;
- fetch 2200ms 预算(:51-54 注释写"外层15秒")对齐 settings.local.json 实际 30s;
- 顺手:mechanical_fallback 仍接管 46% 轮次+quote 验真失败 226 次,给个降失败率方案(先诊断后动手)。

## 单3·订阅桥 launchd 托管
fable-bridge.mjs(lisa-practice/,端口8787)实测裸进程无 plist(四审你我各查一座桥,这座是漏网的)。
给它写 com.lisa.fable-bridge.plist:KeepAlive+BRIDGE_RESUME=1+日志落 App Support(别进 iCloud);
注意别双跑——上岗前 kill 现有裸进程(当前 PID 33830,以 ps 实查为准)。

## 单4·副本统一(治漂移)
- wake_queue.py×3(repo/App Support/relay目录,launchd 实际跑 relay 那份)、bridge.py×2
  (.mcp.json 跑 repo 份,launchd 跑 App Support 份!)、voice_prosody.py×2:
  定唯一真源+其余改 symlink 或加同步校验,别再靠 md5 巧合;
- .claude/cc-ledger-state.icloud-retired/(753KB 旧账本)确认无引用后按退役流程处理;
- 基建 jsonl 轮转:candidates 764KB/diagnostic 243KB/wake_inbox 147KB/voice_jobs 380KB。

## 单5·发布闸三件
- check-version.mjs 语义改「与 git 改动集一致」(现在健康仓上 exit 1,与手册"没改不动"教义矛盾);
- .githooks/ 入库+core.hooksPath 接线;
- bump-version.mjs 补:manifest.json 本体 launch= 纳入 bump + 认识 rescue.html 正则转义形态版本串(52.30 卡死案)。

## 单6·supabase 台账 + 备份姿势
- supabase/ 31 个 SQL 建"已应用"台账(schema_migrations 表或 manifest 文件,方案你定);
- 备份倒挂:relay 三件套(server.py 30KB 全宇宙仅一份/relay_ctl/watch_api)+codex-watchdog+
  .claude 配置(settings.local.json+rules)纳入版本管理或至少定期备份(iCloud 不算备份)。

## 边界(重要)
- x_api 密钥字段级拆分那单**先别动**——涉及她换机体验+旧云档明文清除,等言秋和她定方案;
- generation 存储工程(1B-1D)未开工,别提前动 doImport;
- 改任何回灌/账本路径前先看 v52.34 的 WAL 协议,别绕开 saveJSONDurable。

验收人:Lisa(终审)+言秋(技术对喷随时奉陪)。烧得开心。
