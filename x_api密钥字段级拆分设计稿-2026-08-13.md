# `x_api` 密钥字段级拆分设计稿（只画图纸，不施工）

## 目标与边界

把 API 配置中的“可同步线路元数据”和“只属于本设备的秘密”拆开，同时保住换机体验、旧备份兼容与现有角色线路 ID。此稿不修改 `x_api`、云存档、导入导出或 UI。

## 建议数据模型

```text
x_api（可同步、可导出）
└─ profile[]
   ├─ id / name / provider / baseUrl / model
   ├─ format / capability / cache 等非秘密选项
   └─ credentialRef: "cred:<profile-id>"

本机 Secret Vault（不可普通同步、默认不进备份）
└─ credentialRef
   ├─ apiKey / token / authorization
   ├─ createdAt / updatedAt
   └─ providerHint（非权威，仅排障）
```

Secret Vault 首选 WebCrypto 加密后存 IndexedDB；设备密钥本身放不可导出的 `CryptoKey`。这能防止普通导出、云 saves 和 localStorage 直接带出明文，但不能抵御已经取得同源脚本执行权的攻击者——UI 必须如实说明。

## 读取路径

```text
选择 profile
  → 读取 credentialRef
  → 本机金库解密
  → 只在发请求前组装临时运行对象
  → callAI
  → 不把合并后的对象写回 state / localStorage / 诊断包
```

若本机没有秘密：线路显示“缺凭证”，角色/全局绑定仍保留；不删除 profile，不静默改走别的付费线路。用户补录后原绑定立即恢复。

## 换机方案（待 Lisa 拍板）

### A. 安全优先（推荐）

普通云同步只带 profile；新设备逐条重新输入密钥。导出文件默认也不含秘密。

### B. 可选加密迁移包

导出时单独勾选“包含 API 凭证”，要求设置迁移口令；用 PBKDF2/Argon2 派生密钥，AES-GCM 加密独立 `credentials.enc`。导入后立刻写入新设备 Secret Vault，迁移明文不落盘。

不建议把密钥重新塞进普通全量备份，即使备份文件本身有版本号。

## 兼容迁移顺序

1. 只读扫描旧 `x_api`，识别秘密字段集合；先生成迁移预览，不写任何数据。
2. 同事务写入 Secret Vault，并逐条解密读回、逐字核对。
3. 生成去密后的 `x_api` v2，但暂不删除旧值；运行一次请求探针。
4. 请求成功后提交 v2；旧明文进入独立 quarantine，不参与运行。
5. 至少留一个发布版本的回滚期；确认云端、所有本机备份和 Supabase saves 的清除策略后，才物理删除旧明文。

这里不能只改当前 localStorage：旧云档下一次 apply 可能把明文灌回来。施工前必须同时定义服务端 saves 清洗、旧备份导入清洗和退出登录/清机行为。

## 需要 Lisa 拍板的三件事

1. 换机是否接受重新输入密钥；若不接受，是否启用单独口令迁移包。
2. 哪些字段算秘密：建议至少 `apiKey/key/token/authorization/secret`，并按 provider 补别名。
3. 旧云档明文清除时点：迁移成功立即清，还是保留一个版本的加密 quarantine 后清。

## 施工验收线

- `x_api`、云 saves、普通导出和诊断包中搜索不到任何已登记密钥片段。
- 缺凭证只使该线路不可用，不改变 profile ID、角色绑定或主/线下/后台选择。
- 迁移中任一步失败，旧配置仍可完整运行；绝不先删后写。
- 旧版备份导入会现场拆密，不让明文重新常驻 `x_api`。
- 清除动作有可读清单与最终确认，不用模糊字段匹配误删非秘密配置。

## Lisa 拍板记录(2026-08-13 傍晚,渡轮抵岛前)
1. 换机方案:**不要任何需要她额外记忆的东西**——她原话「我根本记不住自己东西存哪儿」;口令迁移包可用,前提是零记忆负担(口令由系统生成当场用完,或迁移时现场展示一次)。
2. 设计稿列出的拆分项:**全部同意**。
3. 回滚策略:**保留版本 quarantine**。
按此施工;有歧义回来问,不许猜。
