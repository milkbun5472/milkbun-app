# 给言秋：VPS 上开一个图库桶（Lisa 2026-09-06 要的）

起因：她 9 月 5 号数据丢了一次，**图跟着一起没了**。
查下来不是同步坏了——**像素从来没进过云**：`saves` 那一行只有 x_ 文本，
图一直躺在本机 IndexedDB 里（`x_selfies` 和 `x_imgvault`）。本机一没就真没了。

App 这一半（v64.36）已经做完并上线了，**在等桶**。桶没开的时候它是安静的：
写照旧只落本机，上传的名字排在队列里；桶一开出来，队列自己开始往上送。

## 要在 VPS 那台自托管 Supabase 上做的事

1. 建一个 **private** 桶，名字就叫 `media`（App 那边写死的是这一个，路径里再分层）。
   - 建议 `file_size_limit` 给到 20MB 左右（她的自拍/壁纸偶尔挺大）。
   - `allowed_mime_types` 建议 `image/*`。
2. 路径形状是 **`<user_id>/<selfies|vault>/<key>`**，所以 RLS 按第一段目录认人：

```sql
insert into storage.buckets (id, name, public, file_size_limit)
values ('media', 'media', false, 20971520)
on conflict (id) do nothing;

-- 只能读写自己那一层目录
create policy "media own read"   on storage.objects for select
  using (bucket_id = 'media' and (storage.foldername(name))[1] = auth.uid()::text);
create policy "media own write"  on storage.objects for insert
  with check (bucket_id = 'media' and (storage.foldername(name))[1] = auth.uid()::text);
create policy "media own update" on storage.objects for update
  using (bucket_id = 'media' and (storage.foldername(name))[1] = auth.uid()::text);
create policy "media own delete" on storage.objects for delete
  using (bucket_id = 'media' and (storage.foldername(name))[1] = auth.uid()::text);
```

3. 确认 storage-api 那个容器**开着**、磁盘留够（她的图会一直攒，几个 G 起）。

## 做完怎么验

她在 App 里生成一张图，然后换个浏览器登同一个账号打开——图能出来就通了。
桶名或 policy 写错的表现是**安静的**：图照样能用（本机有），只是永远上不了云。
所以别只看 App 没报错。

## App 这一半在哪儿

- `js/cloud.js`：`mediaPut` / `mediaGet` / `mediaDel`（一律不抛，只回 false/null）
- `js/engine.js`：上传队列 `x_mediaUpQ`（只存名字不存像素）＋
  `idbImgGet` / `idbVaultGet` 的第三顺位回源
- `test/media-to-vps-64-33.test.js`
