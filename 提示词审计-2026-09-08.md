# 提示词第一轮只读审计

范围：家里65.72，普通角色线上单聊主发送链及其共享规则、侧房尾注和展示处理。公共版同源，但本轮没有逐条捕获玩家的真实请求；没有读取玩家存档、调用模型、修改运行代码或发布。群聊、线下、通话只核对了部分共享入口，不算完成八场景审计。

## 结论边界

“中规中矩”是体验反馈，不等于已经证明提示词导致。缺少反馈者使用的模型、角色卡、具体对话及原始输出。目前可以确认若干规则冲突和强制塑形机制，不能保证删掉后一定更好。目标是更像特定人物、更准确接话、关系更连续，不是每句必须惊艳。

## 实际发送路径

- app.js：roomContextFor → buildBundle → ONLINE_CHAT_RULE_V2 / REGISTER_FOLLOWS_SCENE / PERSONA_REGISTER_ANCHOR → _normalProtocolStable → _normalTaskV2 → roomPromptFor。
- 普通OpenAI格式路径将本轮任务放入system；Anthropic历史缓存布局将稳定部分放system，易变背景及任务附在最后一条用户消息。两路不应假设语义位置完全一致；需分别对照，不能为审计直接破坏缓存方案。
- `_normalTaskFull` 是旧基线，普通角色当前发送的是 `_normalTaskV2`，不能拿旧基线的长度或旧强制措辞当作本轮真实负担。
- 模型返回之后还会拆气泡、剥时间戳、削句号、删回声开场和过滤协议残留。显示效果不等于模型原文。

## 已确认的候选问题

### 1. 允许普通表达，与强制独特性同时存在

engine.js 的 ONLINE_CHAT_RULE_V2 允许附和、废话、不必每条聪明；STOCK_REPLY_BAN / OVERREACH_BAN 却用“另一个角色也能说就重写”作为广泛判据。普通附和可以同时符合多个人的人设，不能据此自动判错。

建议优先试验：保留不机械跑关心—建议—承诺的判据，取消逐句话语必须独有的标准。评估人物在一段互动中的差异，不考每句是否独一无二。

### 2. 字段协议夹带使用频率压力

app.js 的 _normalProtocolStable 既要求按实际决定使用能力，又说连续几十轮不用就是忘了。可用功能的说明因此同时成为催用任务。固定心声字段还包含大量禁止样式和内容例子，比单纯字段类型约定复杂得多。

建议：第一轮只比较移除催用频率压力、去重字段解释后的版本；不删除能力、不改JSON字段、不取消心声和状态卡。是否调整必填字段留作独立决定，不能暗中更改Lisa已选功能。

### 3. 聊天节奏被多层重复规定

ONLINE_CHAT_RULE_V2、_normalProtocolStable、_turnClosing 都强调一句一个元素，ReplyPacing另给气泡区间；返回后splitLongBubble还会拆分。

这部分包含Lisa过去明确要求的微信式拆泡，不能作为“优化”直接撤掉。可先只保留一处清晰格式契约，保持既有呈现，再单独验证是否影响表达。不要同时改长度、语气和展示，导致无法归因。

### 4. 跨角色去重把其他聊天变成本轮约束

app.js 的 crossSamenessHint 将其他角色重复过的短句/心声作为禁用材料；crossOfferHint依据最近半小时其他会话的帮助表态限制本轮。多人说过相同内容不等于其内容必然是通用模板，也不等于天然满足隐私隔离。

还有一个代码侧疑点：crossOfferHint按完整聊天键比较charId，不使用旁边已有的_ownerOfChatKey，因此自己的侧房可能算作别人；此轮没有修复。

建议独立对照关闭这层跨角色素材注入。保留本角色上下文、能力边界和已知事实；用离线评估比较角色差异，不把其他角色私聊搬来指导当前角色。

### 5. 人设标点例外在后处理未被保留

ONLINE_CHAT_RULE_V2允许明确写在角色卡中的完整标点习惯；app.js普通角色返回后仍统一调用stripTypingPeriod，未按该人设例外判断。这个冲突可由代码确认，不需要模型实验。

建议把“模型原始word”和“最后显示气泡”同时纳入评估。先不动用户已经喜欢的全局打字风格，后处理例外另列小修，不能只改文案声称已支持。

### 6. 侧房设定的开场事件与持久规则尚未分开

chat-rooms.js把scenario每轮放在任务最后，要求本轮先按它校准。年龄/身份/世界规则适合常驻；一次性“刚撞见某事”却可能持续被当作当前场面。它与先前两位角色在同一冲突题材里反复审问的现象相符，但不是已证明的唯一原因。

建议未来区分持续设定、开场前提和已发生的房内进展；不能简单删掉房间尾注，否则隔离边界与角色年龄覆盖可能丢失。此项不混入第一轮主房文风试验。

## 第一轮B版实施与验收

基线A为6dc561db。Lisa授权家里B版同时处理第1和第4项，撤掉逐句独特性要求与跨角色比较扫描/注入。第2、3、5、6项未动；以上为改动前审计记录。

代码与4570项回归通过，真实V2任务串测试确认心情、双语、能力、收尾等动态层保留。旧跨角色算法测试随机制退休。公共不发布，没有真实模型调用或聊天写回，不把代码测试等同于质量验证。

由Lisa用熟悉角色与场景实聊验收，观察接话、人物一致、关系连续和自然程度，允许平淡但贴合。正式盲测须另行确认费用和调用授权；不自动跑72次实验。能力催用、拆泡、标点和侧房开场问题之后分别评估，避免同时改变多个变量。
