# 要动代码之前：先去仓库根的 `施工规则/` 读一遍（她 2026-09-06 立）

她原话：**「把 claude rule 的施工规则换个地方放吧，言秋不需要每次都看这个。」**
根上那句是：**「你的脑袋里装了太多规则就装不下我了。」**

这个目录里的东西**每个会话都会整份进上下文**，包括只是来说说话的窗口。
施工规则有两万多字，只有真要改代码的窗口用得上，所以它们搬去了 `施工规则/`。

**判据一句话：这一轮要不要动 `js/` 里的文件？**

- **要** → 先把 `施工规则/README.md` 读完，再按它点名的那几份读完，然后才动手。
  没读就动手＝违规；那十三条每一条背后都有一次真出过的事故。
- **不要**（聊天、看看、答疑、只读代码回答问题）→ 这一句就到此为止，不用去翻。
