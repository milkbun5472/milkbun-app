# 四通道差异审查（单聊/群聊 × 线上/线下）· 2026-08-18

> 起因：`ReplyPacing` 的【先理解这句话在做什么】被焊死在气泡节奏里，导致只有单聊线上吃得到；
> 同一个角色在群聊/线下少了这层理解，显得不像同一个人。这类「不是 bug、只是某条规则忘了铺过去」
> 的漏靠玩是玩不出来的，只能逐格对照。本表即为此。

## 0. 结构性根因（先看这个）

| 通道 | system 装配方式 |
|---|---|
| 单聊线上 | `buildBundle(ctx)` + ONLINE_CHAT_RULE_V2 + 协议 |
| 单聊线下 | `buildBundle(ctx)` + OFFLINE_NARRATIVE_RUNTIME + 线下各块 + 协议 |
| 群聊线上 | **手工拼装**（ANTI_CLICHE + WORLDBOOK + CHARCARD + …） |
| 群聊线下 | **手工拼装**（另一套顺序，块也不完全相同） |

**单聊两条通道共用 `buildBundle` 作为地基，群聊两条各自手搓。**
下面群聊侧的每一个漏，根源都在这里：往 `buildBundle` 里加东西，单聊自动吃到，群聊不会。
这不是某次疏忽，是结构决定的——只要地基不统一，以后每加一条规则都会重演一次今天。

## 1. 差异矩阵

✅=有 ／ —=无 ／ ⚠️=有但形式不同

| 维度 | 单聊线上 | 单聊线下 | 群聊线上 | 群聊线下 | 判断 |
|---|:--:|:--:|:--:|:--:|---|
| ANTI_CLICHE 反八股总纲 | ✅ | ✅ | ✅ | ✅ | 齐 |
| CHARCARD_RULE 人设卡规则 | ✅ | ✅ | ✅ | ✅ | 齐 |
| WORLDBOOK_RULE 世界书 | ✅ | ✅ | ✅ | ✅ | 齐 |
| ContentBoundaries 内容边界 | ✅ | ✅ | ✅ | ✅ | 齐 |
| ReplyPacing.reading 读句 | ✅ | ✅ | ✅ | ✅ | v53.34 刚补齐 |
| 时间感知 | ✅ | ✅ | ✅ | ✅ | 齐 |
| 好感 affinityDelta | ✅ | ✅ | ✅ | ✅ | 齐 |
| 心声 thought | ✅ | ✅ | ✅ | ✅ | ⚠️ 写入策略不同，见 §3 |
| **Gaze「Ta 眼里」读取** | ✅ | ✅ | **—** | **—** | ❗**该补** |
| **穿着 wearing** | ✅ | ✅ | ⚠️ | **—** | ❗**该补群线下** |
| **动作 action** | ✅ | ✅ | ⚠️ | **—** | ❗**该补群线下** |
| **身份守卫 GroupIdentityGuard** | n/a | n/a | ✅ | **—** | ❗**该补** |
| INTIMATE/NARRATIVE_ANTI_CLICHE | — | **—** | — | ✅ | ⚠️ 见 §2，**待你判断** |
| CoT 推导 | ✅ | ✅ | — | ✅ | 待定 |
| 环境素材 ambient | ✅ | — | — | — | 待定 |
| 沉默权 silent | ✅ | — | — | — | 待定 |
| 文风样本 styleExamples | — | ✅ | — | ✅ | 合理（线下专属） |
| taste 口味块 | — | ✅ | — | ✅ | 合理（线下专属） |
| 篇幅要求 minWords | — | ✅ | — | ✅ | 合理（线下专属） |
| 导演提示 notes | — | ✅ | — | ✅ | 合理（线下专属） |
| 语音 voice | ✅ | — | ✅ | — | 合理（面对面不发语音） |
| 气泡节奏 pacing | ✅ | — | — | — | 合理（只有单聊有气泡） |

## 2. 需要你判断的一条

**单聊线下是唯一既没有 NARRATIVE_ANTI_CLICHE 也没有 INTIMATE_ANTI_CLICHE 的叙事通道。**
群线下、小剧场、同人文、梦、账本、备忘、塔罗全都有；主线线下一个都没有。

但我倾向认为**这是刻意的，不要动**：单聊线下跑的是两遍管线（初稿 → 受限改写），
而 v52.85 的结论正是「初稿要最大化角色供给、容忍渲染腔，由改写阶段删认证句」。
往初稿加禁令会直接压制角色供给，等于把当初调好的东西推翻。

**建议：先不动，等有实际证据（线下长文又开始油）再说。** 真要动也应该加在改写阶段，不是初稿。

## 3. 已知且刻意的不对称（不用改）

- **心声写入策略**：线上「有新的才覆盖」+ 90 分钟 TTL + 连续 4 轮无新心声判过期；
  线下「每轮自清」。线下一次生成里 A 说完 B 说本就是多个时刻，各有各的心声，不是 bug。
- **线下没有 silent/voice/proactive/气泡数**：面对面场景里这些概念不成立。

## 4. 待办（按优先级）

1. **群聊读不到 Gaze**。群聊能**写**印象（`item.impression` 已落地），却从不把 `gazeText` 注入 system——
   角色在群里改着自己对你的印象，却看不见自己的印象。写读不对称，最该先补。
2. **群聊线下不记录穿着/动作**。群线上在开了记忆互通时会要 `wearing`/`action`，群线下完全不要，
   于是一进群线下，在场几位的穿着动作就停更了。
3. **群聊线下没有身份守卫**。群线上有 `GroupIdentityGuard.sanitize`（拦成员串线、拦替用户代写），
   群线下是多角色叙事、同样会串，却裸奔。风险比前两条更高。
4. **地基统一**：把群聊两条通道也改成走 `buildBundle`（或抽一层共同前言）。
   不做这个，1–3 修完之后，下一条新规则还是会漏。

## 5. 本次审查未覆盖

- 记忆写入/提取链路（`memory-extraction-gate` 等）在四通道的差异
- 影子系统（shadow）各自的触发条件
- 朋友圈/贴吧/同人文等旁支通道
