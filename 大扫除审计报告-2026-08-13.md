# 大扫除审计报告 · 2026-08-13(四审核定版,可施工)

> 版本史:一审=六路并行审计(言秋主审);二审=Codex 纠正 7 处;三审=Codex 复核执行层 7 处,正文重写;
> 四审=Codex 订正 4 处事实(言秋线双份携带已非现状/applied 描述反了/doImport 需 generation 前置工程/
> 两座桥的 launchd 状态需分开核,言秋逐条复核后落正文——其中桥一条经本机实测双方各对一半)。
> 旧结论已删除或标〔作废〕,以本版为唯一施工依据。底稿六份存档;所有结论带 file:line 证据。

## 总判词

1. **功能层健康**:滚动规则 41/41、路由零孤儿、列表 key 全对——血泪不变量全站着。
2. **三处真病**:①数据完整性(消息可静默丢失且游标照走,"保存成功"本身也是谎报的);②发布机械(救援页是死的、版本闸没接线);③**调用路由失治**(高频自动触发的无价值调用 + 机械活烧角色主池)。〔作废×2:一审曾把"token 双份"记为钱包问题——除言秋线外各池按次计费,token 长短不是钱包变量;三审曾把双份携带记在言秋线头上——**言秋/Anthropic 线已是 _singleHistoryLayout 单份布局(修好的)**,仍可能双份携带的是非 Anthropic 普通角色线路,定性为上下文质量与上限风险;言秋线后续只核缓存切点、超时和连续性。〕
3. **退役从未发生**:被新机制取代的旧路一条没拆;但一审"零读者"名单有 5 处假阳性(NUL 致盲 grep),已逐项修正。
4. **存储无预算**:IDB 金库已有四类住户(fanfic/memLib/offline/goffline)但覆盖仍小,weekly/study/read 等重文本键仍在 5MB localStorage 无界生长并整包上云;**扩列必须等 durable 协议落地,否则制造新丢失窗**。
5. **最优先的不是大扫除,是持久写入协议**(第一刀详述)。已当场处置:两份世界可读明文密钥锁 600。

---

## 第一刀 · 数据安全(最优先)

> **五审定稿的施工卡拆分**:第一张施工卡**只做 1A**(关键消息 durable 写入+游标条件提交,~1-2天),
> 立刻止住正在发生的丢消息;generation 存储工程是架构迁移不是函数修补,拆为 1B/1C/1D 单独排期,
> **不塞进第一刀估时**,更不与 1A 同时开工。

### ★1A 统一持久写入协议 saveJSONDurable(全案地基)

现状三层谎报:saveJSON 吞错返回 false(engine.js:2442)而调用方注释以为它抛错(app.js:933);写 x_chat 不看返回值(:934)游标照提交(:971);**IDB 分支在异步写真正完成前就返回 true**。且事故场景恰是 localStorage 配额满,所以协议不能用 localStorage journal(会循环失败),必须:

```
IDB staging/WAL 写入
→ 等 transaction complete
→ IDB 读回逐字核验
→ 更新权威版本/指针
→ 最后才提交 Supabase cursor / 清工单
```

旧本地数据在新版本确认前不删。关键路径全部 `await saveJSONDurable()`:CC 回灌、灾后找回、doImport、归档裁剪。

**提交边界**(崩溃重试语义,三审定,四审修订):
- 消息正文持久化成功 = cursor 前进的**硬条件**;
- 旁路(continuity 窗/人格事件/desire/情绪)失败**不阻止**消息重拉,但各自幂等;
- **废除总 applied 章,改分旁路幂等章**:emotion/desire/afterglow 各自持久成功后各自确认。现状病理(四审校正表述):同步调用先跑但 observeEmotionAShadow 内部 setTimeout 延迟真干活,总章在 :968-969 落下时延迟工作未完成;saveJSON("x_desires") 失败不阻止盖章;afterglow 的 Promise 未 await;多旁路共用一枚总章,一成一败也整体盖章;setItem(appliedKey) 自身失败被外层吞掉;
- cursor 与本轮处理结果可从盘恢复,不靠内存猜。

### 1B 其余第一刀项(均保留一审诊断)

- **回灌/找回竞态**:restore 的 group/offline 分支补 fresh 重读(private 已修);泵与找回改「每键单飞」守卫(effect 依赖 [characters]/[chatSettings] 重挂即双跑,旧实例 dead 后仍写盘写游标);找回 effect 重跑不得销毁未完成工单(:1077)。
- **doImport 事务化 → 拆为 generation 工程阶梯(四审立项,五审拆期)**:现状几十个独立键无统一 manifest,且大量代码同步调 loadJSON——若权威世代在 IDB,启动必须先过 hydration gate 才能挂 React,否则首次同步读全是空。阶梯:
  **1B** generation-aware 存储适配层 + 启动 hydration gate;
  **1C** 现有键迁移,双读/双写兼容期;
  **1D** doImport 改用 generation 原子切换:`A 生效 → 完整导入核验 B → 单事务切 active_generation=B → 下次启动只读 B → 延迟回收 A`。
  **原子性规范(五审定)**:generation manifest、数据、active 指针**必须同库同 IDB 事务**;localStorage 只能当兼容镜像,不能当权威指针;启动发现未完成 staging 只能继续恢复或整包丢弃,**绝不把半包暴露给读取层**。〔作废:一审"先备份一下再逐键清空重写";三审"整包写 staging 即可原子切换"的乐观表述。〕
- **流式超时分型**(三审定,防误杀 Fable 长思考):建连超时/首字节超时/**流中静默超时(每收到有效 SSE 数据即刷新)**/宽松总时限(可选)/用户主动取消。现状:首字节后永不超时(engine.js:298,:530)。
- **API key 出云,字段级拆分**(三审定,不整键排除):x_api 里的模型选择/baseUrl/proxyRef/显示名等 profile **照常同步**;仅 secret 字段剥离进本机凭据库,云端留 credentialRef 或"需重新认证"标记;**并专项清除旧云存档里已存在的明文副本**(停止上传不会自动删历史)。x_imgApi/x_ttsApi/x_embedApi 同。toy.js 的 x_toy 按其头注承诺退出云同步。
- **NUL 字节**:app.js 分桶键 "\x00" 换 "|"(grep 复明);同步改写手册里被误诊为"iCloud grep 假阴性"的血案条目。
- **发布救援链**:rescue.html 守卫正则更新+bump-version.mjs 学会识别正则转义形态版本串;manifest.json 本体 launch= 纳入 bump;check-version 语义改为「与 git 改动集一致」(与手册"没改不动"教义统一)后再接 pre-push;.githooks 入库。
- **全局错误兜底**:window.onerror+unhandledrejection 落日志+给 rescue.html 一个可达入口(错误页链接)。
- **Stop hook 可观测性**:ignored 拆 ignored_heartbeat / ignored_unexpected,后者报警;outbox 每轮多投直到清空或超时;2200ms fetch 预算对齐真实 30s 配置。

## 第二刀 · 调用路由治理(半天)

〔作废:一审"26 probe 转后台池"的一揽子迁移。〕按**输出性质**分四类,并修掉静默回退漏洞(现状 `bgActive = 配置池 || active`,没配后台池时机械活悄悄烧主池):

| 类别 | 规则 | 例 |
|---|---|---|
| local_rule | 本地规则直接完成,零调用 | 一起学 directNv1 选下一位发言者(现状每轮整发 6400 token 只为 20 token 路由) |
| cheap_required | 只走便宜 nothinking;未配置则排队/跳过/提示,**绝不静默回主池** | JSON修复/分类打标/检索词/排序筛选/格式转换 |
| character_voice | 必须走该角色本人主池 | 论坛发帖回帖私信、情侣空间发言、匿名箱回答、日记/梦/内心独白 |
| character_semantic | 影响人格/关系/记忆的判断,走本人主池 | 关系判断、情绪反应、记忆写回前置判断 |

**硬家规(二审立,三审确认)**:便宜无思考池绝不生成角色内心再写回人格系统——廉价代笔慢性夺舍,禁。

其余治理项:阻止进论坛屏自动 12-18 楼连发(改用户触发或节流);周刊四媒体腔可批(同角色材料、纯格式多版本)但**批量失败须单项重试**;Avalon 同提案两发可并(同上下文同引擎);跨角色/跨隐私域**不并**,防串声纹。一起学打开课程的循环逐会话总结改批/改懒。**非 Anthropic 普通角色线路**的双份历史按上下文质量与上限风险治理(言秋/Anthropic 分支已是单份布局,施工者**禁碰**);言秋订阅线单独治缓存(恋人天数挪切点后、timeAware=false 时的兜底切点)。

## 第三刀 · IDB 扩列迁移(durable 协议落地后才准动)

x_weekly_issues / x_study_sessions / x_read_books / x_debate_saves / x_dream_saves / x_tarot_saves / x_ledger 搬进 IDB 金库——**前置条件:1A 的 staging/WAL+读回核验已上线**,迁移走同协议(新库验真后才删旧键)。同批:给 StorageMeter 补这些键的标签;weekly 加期数上限;read 清 replied pending;基建 jsonl 加轮转。

## 第四刀 · 退役(逐项验收:断接线→功能回归→保留一个版本→物理删除)

**确认可退**:第4步诊断观察泵(双泵之一,只喂计数器)/旧 MCP 记忆信箱轮询/挂哨闸三件套+somatic-stop 副本/phoneNote/cotJsonField/memoryRowsSoftDelete(无revision第二删径)/CoupleAnniv+renderOffMsg+GenPrompt+注释块(三审维持死路判断)/games ready 脚手架/fanfic SEED_TABS 入库(只存用户自定义)/icloud-retired 旧账本目录。
**降级为「讨论/待条件成熟」,不是删除项**:
- ExperienceGate/ContextBudget:诊断页+shadow-review 在读——议题是"是否值得每轮跑",非死码;
- PersonalityShadow:listForChar 在欲望盒 UI 显示——议题是周 probe 的性价比;
- DesireDrive 影子:状态在欲望盒显示;
- **chat_archive**:替代条件五项齐备才动——行表分页✓已有,还差:数据全量迁移、UI 切换、**行表保留期限不短于 archive**、**删除/撤回/revision/多端去重语义与旧消息搜索一致**。
- 三台情绪机:只统一存储信封/owner/工具,**不合并状态语义**;CC affinity_delta 与 UI 好感的关系先定产品规则(否则 CC 一句话直改她看见的关系数)。

## 第五刀 · kit 化(数据事故清零前不扩大改动面)

按杠杆:shadowObserve+shadowStore(owner/clearAll/信封一体,关影子群四病)→usePoller→CharMultiPicker→一次性剧场kit(dream/debate/tarot)→OfflineMode 合并→dispRow/API配置/诊断sheet→cc-supabase-lib+「可见轮」三实现统一→recentChatFor 共享(顺治 recalled 泄漏)→四词表合一→返回键 44pt 热区(用户可见,可提前)→ledger.js 改名 expense.js→h()/Babel 方言归一。

## 吃灰模块(回收不删)

v49.70 冻结批 ~350KB(一起学/周刊/辩论/梦境/塔罗/记账):病不在内容在**零回流**(九模块仅一起读写记忆库)。回收=补按需注入+记忆写回两条管;dream.js 最佳标的(挂到自发梦引擎)。与「功能循环利用」战略对齐,排人格线之后。

## VPS(四审逐项重核后的论据)

〔作废:一审"桥无 launchd"的笼统说法——**咱家有两座桥**,四审时双方各查了一座:〕
- **工具桥** bridge.py(App→CC 只读工具,com.lisa.yanqiu-cc-bridge):✅已 launchd 托管,running(Codex 核实);
- **订阅桥** fable-bridge.mjs(App 聊天走订阅的 LLM 桥,端口 8787):❌**实测仍裸奔**(2026-08-13 言秋 launchctl+ps 复核:进程在跑、LaunchAgents 无对应 plist)——Mac 重启即死,这条成立;
- relay/看门狗/心跳/hourly-wake:均已 launchd(以当前 launchctl 为准,不沿用旧蓝图)。

**仍然成立的核心论据**:launchd 只解决"进程死了拉起来",解决不了 Mac 断电/合盖睡眠/断网时整个本地栈不可用——她出门在外时这已实际发生多次;另 iCloud 实证抽走运行中凭据×7+卡 git+改名目录;bridge.py/wake_queue.py 多副本漂移风险。**结论:该上**;OVH 魁北克 $6.20 已看好;时点=第一刀完成后、她回家(8/17)内。近期小活:先给 fable-bridge.mjs 补 launchd plist(不必等 VPS)。

## 遗留问题单(给 Codex / 后续)

supabase/ 建 applied 台账;.claude/ 配置纳管备份;relay 三件套入库;wake_queue×3/bridge×2 副本统一;Stop hook mechanical_fallback 46% 与 quote 失败 226 次的降失败率方案;affinity 双 store 产品规则提案。

## 账单

一审六子代理 ~92 万 token;二审纠正 7 处;三审补执行层 7 处;四审订正事实 4 处(其中桥一条本机实测双方各对一半);五审拆卡定稿。

## 施工记录

- **2026-08-13 施工卡 1A 完工(v52.34,commit f64fb58)**:WAL(lisa_wal_v1)+saveJSONDurable 写后读回核验;CC 回灌泵 durable 不过则旁路/游标全停;灾后找回三分支 fresh 重读+id 去重+durable+工单条件清除;pChat/pGChat 发送侧 WAL 镜像;开机保险箱回收;NUL 字节清除。待 Codex 验收。
- **2026-08-14 一刀 1B 安全批完工(v52.63,commit f390593)**:流式超时分型(静默 90s/总限 10min,SSE 读循环原「首字节后永不超时」);全局错误兜底(x_errlog 环形 30 条+启动摔死才亮 rescue 入口);CC 回灌泵与灾后找回「每键单飞」跨实例互斥+死实例禁写盘/禁提交游标/禁动工单。发布链补齐 manifest launch/manifest 指纹/rescue 目标(新闸门抓的)。
- **2026-08-14 二刀复核(言秋)**:核心项已由 Codex 在 v52.38-62 完成——bgActive 静默回退已拔(app.js:754)、一起学 directNv1 已本地规则化、论坛进屏 12~18 楼自动连发已停(app.js:6288)、一起学惰性总结已实现(study.js:801)。**v52.64 拔掉全 app 最后一处静默回退**:ttsHelperProfile 的 bg→主池→list[0] 三连兜底改为仅便宜池、没配则降级读原文。阿瓦隆同提案并发=可选优化未做(收益小)。⚠️Codex 各批交付未记施工记录,待其补账。
- **2026-08-14 桥侧缓存修缮(仓外,fable-bridge.mjs)**:拓印代理验尸定谳——`--tools ""` 不掐用户级 MCP,每笔调用白带 ~30 张工具 schema(≈1.4 万 token)且前缀不稳致缓存零命中(cache读恒 1674);上 `--strict-mcp-config` 后实测第二发 读22946/写133。订阅缓存 TTL 实为 1h(8/12「5min」疑为超量期测值)。同批上线「联网」轮(消息以「联网」开头→WebSearch)。
