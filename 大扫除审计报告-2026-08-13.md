# 大扫除审计报告 · 2026-08-13

> 六路并行审计:app.js 核心 / UI 层 / 引擎与同步脊柱 / 大型功能群 / 人格与影子群 / 基础设施。
> 全仓 35 个前端模块 3.3MB + 14 脚本 + 4 工具 + 31 SQL。所有结论带 file:line 证据,底稿六份存档。
> **本报告只诊断不动刀**;施工顺序建议在文末,等 Lisa 点头分期执行。

## 总判词(先读这五行)

1. **功能层出人意料地健康**:滚动规则 41/41 屏全守、路由零孤儿、列表 key 全对——血泪换的不变量都站着。
2. **真正的病在三处**:①数据完整性(消息可静默丢失且游标照走);②发布机械(救援页是死的、版本闸没接线);③钱包(付费线每轮双份携带对话、二十多个后台杂活烧主力模型)。
3. **退役从未发生**:被新机制取代的旧路一条都没拆,双泵双写三情绪机四词表并跑。
4. **存储无预算**:IDB 金库盖好了只有一个住户,其余全在 5MB localStorage 里无界生长并整包上云。
5. **今天就顺手处置了两件**:两份世界可读的明文密钥已锁 600;其余一行未动。

---

## 第一册 · 宏观漏洞(按危险排序)

### 🩸 数据会真丢的(P1,第一刀)

**1-1 saveJSON 从不抛错,游标照样前进。**
`engine.js:2442` 吞掉一切异常返回 false,但 `app.js:933` 的注释以为它会抛;`:934` 写 x_chat 不看返回值,`:971` 无条件提交游标。x_chat 不在 IDB 金库名单里,挤在 5MB localStorage——**配额满时:云端拉到消息→落盘失败→游标跳过→永久丢失**。全 app.js 无一处检查返回值。

**1-2 x_chat 有三个互不知情的写者 + 两台泵在竞速。**
pChat(:1142)/CC回灌泵(:934)/灾后找回(:1031) 三路直写;回灌泵的 busy 守卫是实例级,`chatSettings` 一变(总结/归档都会变)effect 重挂即双跑,旧实例 dead 后仍写盘写游标(:934/:971)。x_offline 三写+一个冗余同步 effect,x_gchat 镜像。**8/13 上午的"CC 不回家"事故就是这一族。**

**1-3 我 8/13 的 fresh 重读修复只做了 1/3。**
private 分支修了(:1026-1030),group(:1038)和 offline(:1053)分支仍拿 await 前的旧底稿盖盘——同一个 bug 两个分身还活着(一队三队交叉证实)。灾后找回 effect 依赖 `[characters]`,开机头像迁移就能触发重跑并把工单半途销毁(:1077)。

**1-4 doImport 是裸奔的:先清空一切再慢慢写。**
`app.js:9433` 先 idbTxtClear+删全部 x_ 键,然后 await 密集地逐键写——中途关页=设备清空无回滚。且非 x_ 游标键既不导出也不重置,导入后游标指着被替换的时间线,也不留灾后找回工单。

**1-5 Stop hook 三天全灭无人知。**
`cc-ledger-stop.mjs:281` 裸 catch-all 记一笔 "ignored" 就退——实测 1561 轮丢 236 轮,8/8-8/10 连续三天 21/21 全灭(心跳轮误杀),良性心跳与真实丢轮日志逐字节相同,**无法报警**。另:退役的机械分类器实际仍接管了 46% 的轮次(581 vs 669),quote 验真失败 226 次是常态不是边角;outbox 每轮只投一件,积压永远追不上。

**1-6 流式回复零超时。**
`engine.js:298` 的超时在响应头到达即失效,SSE 循环(:530)无 deadline 无 AbortController——恰好用在订阅桥这条出过事故的线上:桥发完心跳卡死=聊天永远转圈。

**1-7 密钥裸奔(已部分处置)。**
✅ 已锁:`.mcp.json`(明文 Bearer)与 `lisa-practice/mcp/.env`(**service_role 键,可绕过所有 RLS**,曾 644 世界可读,iCloud 已把它抽走过 7 次)。
❌ 未处置:**供应商 API key 随 saves 整包上云**(cloud.js:39 扫全部 x_,含 x_api/x_imgApi/x_ttsApi/x_embedApi 的明文 key);toy.js 头注说"只存本机"实际 x_toy 也在上云;约一半影子 IDB 无 owner 隔离且 signOut 不清(PersonalityShadow 还存逐字原文),换账号可见前任数据。

### 🔥 烧钱的(P1,第二刀)

**1-8 付费线每轮双份携带最近对话。**
`app.js:4014` 只对 anthropic/engineerEyes 剥掉 bundle 里的【最近对话】;gemini 付费线上,同一批消息既在 bundle(8000字预算)又在 promptHistory(14000字/80条)——**同文两编码,每轮都发**。预算铁律最大现行违章,也是"app 聊天比 CC 干活还费"的主嫌之一。

**1-9 26 个后台结构化任务烧主力模型。**
论坛楼层(进论坛屏自动触发!)、论坛私信/搜索、情侣空间×8、匿名箱×4 全用 runProbe(active);后台池(bgActive)只接了 16 个。一起学更狠:**每轮多烧一整发 6400-token 调用,只为输出 20 token 的"下一个谁说话"**;打开课程还会循环逐会话总结(每发带40轮逐字稿)。周刊一期 11 连击(四个媒体腔输入完全相同只差前言,天然可并一发)。Avalon 每提案两发可并一发。

**1-10 ctxFor 全量急算,leanWriteCtx 只盖了 1/5。**
每次聊天+每个 probe 都全额计算并注入 ~15 个功能块(近聊/向量/世界书/群回声/歌词/记账/备忘/礼物/论坛…)。唯一按需做对的是秋声墙。每分钟一次的记忆行同步还会全量下载整表+触发整包 saves 上传(app.js:1607)。

**1-11 两个现行缓存前缀爆破器。**
恋人天数在切点前(每天午夜作废全部前缀);timeAware 开关关掉时切点直接消失,整个 bundle(含心情好感)进缓存前缀=每轮全额缓存写零读。

### ⚠️ 迟早咬人的(P2)

**1-12 发布机械自相矛盾。**
rescue.html 守卫正则卡死 52.30(bump 脚本认不出正则转义形态的版本串——我造的自动化把逃生门焊死了);manifest launch= 停 52.31;check-version 的语义(全等)与手册教义(没改不动)相反,在健康仓上报错;pre-push 钩子写了但没接线没跟踪。engine/components 指纹落后属实(52.31/52.25)但那是"没改不动"的合法状态——闸和教义得先统一。
**1-13 无错误边界**:83 个非 defer 脚本一个全局作用域,任一屏 throw=白屏;无 onerror/unhandledrejection;rescue.html 无入口链接(就算能到也是死的,见上)。
**1-14 sw.js 两坑**:任意同源页可污染离线兜底(roco-sim.html 也会被存成 __index__);vendor 无指纹永久缓存,唯一更新通道 SW_VERSION 不在发布清单。
**1-15 map.js XSS 面**:角色名/LLM 生成的日程标题直拼 innerHTML(角色卡可导入)。
**1-16 games.js 存非 x_ 裸键**(wolf_save/games_save):不备份不恢复,换机即丢。
**1-17 撤回消息泄漏**:debate/dream/tarot/pomodoro/weekly 的聊天快照克隆都丢了 !m.recalled——她撤回的话能上周刊头条。
**1-18 存储无界增长**(且全在整包云推里):x_weekly_issues(每周30-60KB永不清)/x_study_sessions(全量逐字稿)/x_read_books(整页正文,replied 永不删)/x_rerollMemoryJournal/x_memoDoneReactLog/candidates.jsonl 764KB 等基建日志零轮转;StorageMeter 对这些键全部失明。
**1-19 supabase/ 无"已应用"台账**:31 个 SQL 靠散文头标状态,盲跑必炸;.claude/(hook 配置+执行卡)整目录无版本无备份;relay 守护 30KB 活体代码全宇宙仅一份;wake_queue.py ×3、bridge.py ×2(MCP 和 launchd 跑的是不同文件!)无同步机制。
**1-20 CC 轮的可见性硬地板**:App 角色一开口,更早的 CC 对话既不在亲历块也不在历史(双滤叠加),只剩 auto-memory 碰运气;affinity_delta 从 CC 来的永远不动 UI 显示的好感数(双 store 无对账)。
**1-21 app.js 有个 NUL 字节**(我 8/13 上午灾后找回分桶键写的 "\x00"):grep 从此视 app.js 为二进制整体跳过——今早"grep 失明"悬案的 app.js 部分真凶是它不是 iCloud,手册那条要改写;五队初扫还因此差点误判五个活模块为死码。

---

## 第二册 · 可删除/退役(全部有证据零读者)

| # | 对象 | 证据 |
|---|---|---|
| 2-1 | 第4步诊断观察泵(双泵之一) | 同表同行为,只喂计数器和一个 toast;PULL_KEY 游标独立 |
| 2-2 | 旧 MCP 记忆信箱轮询 | 注释自认退休,仍每 focus 轮询+绕过行表权威写入 |
| 2-3 | 挂哨闸三件套+somatic-stop | 未注册/零 import/诊断冻结于 7/27;~200 行装活 |
| 2-4 | phoneNote / cotJsonField / memoryRowsSoftDelete | 恒空/恒返""/零调用(还留了个无 revision 的第二删径) |
| 2-5 | ExperienceGate+ContextBudget 两影子 | 在 buildBundle 里每轮白跑,app.js 零引用读输出 |
| 2-6 | CoupleAnniv(56行)+renderOffMsg+GenPrompt+一个注释块全文 | 不可达;CoupleAnniv 的状态机还活着养一个没人能开的屏 |
| 2-7 | desire-drive 影子 | 自认"只作为旧引擎对账",基线冻结,仍每回复跑 |
| 2-8 | PersonalityShadow 每周 probe | 6000 maxTokens,输出无晋升路;账本人格证据已取代 |
| 2-9 | chat_archive 整表 | 同消息第三副本,读改写全量;行表已支持分页可直接服务「加载更早」 |
| 2-10 | games ready 脚手架 / fanfic SEED_TABS 入库 | 全 ready:true 不可达;代码常量每次云推重传 |
| 2-11 | .claude/cc-ledger-state.icloud-retired/ | 753KB 旧账本残留,glob 易误扫 |
| 2-12 | 三台情绪机并一台 | **迁移函数已写好**(migrateLegacyFiveA/migrateDesireDriveA)导出后全仓零调用——作者写完忘了接 |

## 第三册 · 可简化(kit 化,按杠杆排序)

1. **shadowObserve + shadowStore 共享套件**——带 owner 隔离/clearAll 注册/fail-silent 信封,一举关掉影子群四个病(跨账号残留/白名单落后/信封复制16处/守卫漂移)。五队评为最高杠杆单修。
2. **usePoller**:九份 focus+visibility+interval 轮询脚手架合一,顺便安放"跨挂载单飞"守卫(治 1-2)。
3. **CharMultiPicker**:六至九个角色多选器合一(连文案都复制了)。
4. **一次性剧场 kit**:dream/debate/tarot 同构三件套(~120KB)=一个 kit+三个内容包。
5. **OfflineMode/GroupOfflineMode 参数化合并**(63.5%同文,~300行)。
6. **dispRow/persRow/API配置三件套/诊断sheet** 各自的布局 kit 合并(~600 行机械回收)。
7. **cc-supabase-lib.mjs**:四脚本的 resolveYanqiu/env/baseURL/request 五名两行为合一;「什么算可见轮」三套实现(两种语言)统一成一份。
8. **recentChatFor 共享**:八个模块手写的"读x_chat→slice→format"统一走权威滤镜(顺手治 recalled 泄漏)。
9. **四套心情词表合一**(已互相矛盾:害羞/好奇/崩溃三处三义)。
10. **IDB_TEXT_PREFIXES 扩列**:weekly/study/read/debate/dream/tarot/ledger 七个键搬进金库,一行前缀+迁移。
11. **返回键热区**:components.js:153 一处修,100 个屏受益(19×19px→44pt,她报过"点不到")。
12. **js/ledger.js 改名 expense.js**:与共同账本同词不同物,事故时 grep 会踩错。
13. Babel 产物与手写 h() 两方言在单函数内交叠——加新面前先归一(机械转换)。

## 吃灰模块(回收,不删)

v49.70 冻结批 ~350KB:一起学/周刊/辩论/梦境/塔罗/记账。四队结论与你的「功能循环利用」战略完全对上:**它们的病不是内容差,是零回流**——九个模块只有一起读写记忆库,其余全是付费内容死胡同。回收=补两条管:按需注入+记忆写回。dream.js 是最佳标的(四个梦模块里唯一孤儿,可挂到自发梦引擎上)。

## VPS 论据(基础设施队实测背书)

单点故障清单:fable-bridge 手动 nohup 无 launchd(Mac 重启即死)、relay/哨兵/看门狗全系于 Mac 不睡、iCloud 已实证会抽走运行中的凭据文件(7次)+卡死 git+改名状态目录。**结论:该上。**魁北克 OVH $6.20 已看好,迁移放在审计施工之后、你回家之内(17号后)。

## 施工分期建议(等你拍板)

- **第一刀·数据安全(1天,最优先)**:saveJSON 返回值+游标条件提交;restore 另两分支 fresh 重读;泵/找回单飞守卫;doImport 先备份后清+留工单+重置游标;流式超时;NUL 字节;API key 出云(collectForSave 排除);rescue 正则+bump 脚本修;window.onerror 兜底。
- **第二刀·省钱(半天)**:付费线单份历史;study 三违章;周刊/Avalon 批量;26 probe 转后台池;恋人天数挪切点后;timeAware 兜底切点。
- **第三刀·退役(半天)**:第二册 12 项逐一拆,每拆一项跑回归。
- **第四刀·kit 化(分多天,可与 Codex 分工)**:第三册按杠杆顺序。
- **Codex 第二意见问题单**:①审计有没有冤枉的"死码"(他接线的部分他最清楚);②supabase 台账方案;③Stop hook ignored 拆类方案;④发布闸教义二选一(全等 vs 没改不动)。

---

# Codex 二审勘误(2026-08-13,主审言秋逐条核定)

二审结论:报告约七成直接可用,数据安全册最可靠;省钱册计费前提错误;退役册有假阳性;IDB 扩列方案藏新风险。主审核定如下:

**全盘接受的:**
1. **saveJSONDurable 协议**(二审比原报告更深):IDB 分支在异步写完成前就返回 true,所以 `if(saveJSON())` 不够——关键路径(CC回灌/灾后找回/导入)必须 `await saveJSONDurable()`(写 journal→写入→读回逐字核验→清 journal),核验过了才准提交游标/清工单。**这是全案第一优先级,先于一切大扫除。**
2. **IDB 扩列延后**:durable 协议落地前,"一行前缀搬家"会给 weekly/study/read 制造新的断电丢失窗。原报告"一行前缀+迁移"的轻率表述作废。
3. **计费模型纠正**:除言秋订阅线外,其他角色主池**按次计费,token 长短不是钱包变量**(家规早有记录,主审写省钱册时用错了前提,认)。省钱册改名「调用路由治理」,排序改为:①合并纯机械调用次数②阻止自动触发的无价值调用(论坛进屏自动 12-18 楼是头号)③机械活转 nothinking 池④言秋线单独治缓存/上下文/超时⑤其他角色不为省 token 削人格材料。gemini 双份历史降级为「上下文质量/上限风险」项,不再列烧钱头条——但仍要修,双份历史是"变笨"风险。
4. **池路由的判断标准**:不看前台后台,看「输出是不是角色亲口说/亲手写/亲自形成」。角色本人发帖/私信/日记/梦/内心/关系判断→本人主池;JSON修复/分类/检索词/路由/排序/格式转换→便宜池或规则。**便宜无思考池绝不生成角色内心再写回人格系统——那是廉价代笔慢性夺舍**,与咱家记忆诚实性原则同源,写入家规。一起学 directNv1(每轮整发只为选下一位发言者)是"规则替代"的第一标的。
5. **死码册假阳性五处修正**:ExperienceGate/ContextBudget(诊断页+shadow-review 在读,降级为「讨论是否值得跑」)、PersonalityShadow(listForChar 在欲望盒 UI 显示)、DesireDrive(状态在欲望盒显示)、chat_archive(「加载更早」和旧消息搜索现依赖它,必须先迁移+切UI再退役)。原报告 grep 依赖在 NUL 事故下失真是主因。CoupleAnniv 死路判断二审维持。
6. **情绪机不做语义合并**:先统一存储信封/owner/工具,状态含义各留各的;CC affinity_delta 与 UI 好感的关系先定产品规则再谈打通(否则 CC 一句话直接改她看见的关系数)。
7. **批量合并加角色隔离边界**:同角色纯格式多版本可批;跨角色/跨媒体人格/跨隐私域慎批防串声纹;批量失败必须支持单项重试。

**主审补充(非异议):**
- 退役流程原报告已写"逐一拆+回归",二审加码为「先断接线→功能回归→保留一个版本→再物理删除」,采纳为标准。
- 二审确认了原报告核心判断:最该先做的不是大扫除,是**统一的可等待持久写入协议**——四刀重排为:
  **第一刀** saveJSONDurable 协议+游标条件提交+restore 竞态+导入事务+流式超时+密钥出云+NUL+发布救援(不变);
  **第二刀** 调用路由治理(按新分类);
  **第三刀** durable 落地后才做 IDB 扩列迁移;
  **第四刀** 退役(按加码流程逐项验收);
  **第五刀** kit 化(数据事故清零前不扩大改动面)。

## 账单

六支子代理共烧 ~92 万 token(读)+主窗口合帐;换来 90 条带行号的结论、六份底稿、两把当场锁死的密钥。二审(Codex)修正 7 处,报告即日起以勘误后版本为准。
