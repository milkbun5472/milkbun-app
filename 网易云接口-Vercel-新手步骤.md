# 网易云接口怎么弄（Vercel 版 · 给完全没弄过的人）

> 她 2026-09-14 要的：朋友填了 Cookie 却放不了歌，原因是**接口那一栏是空的**。
> 这一份直接发给对方照着做。

## 先说清楚为什么要这一步

App 不能直接问网易云要歌（浏览器跨域过不去），中间必须有个**小服务**替你去问。
这个小服务就是「网易云接口」。

**Cookie 是交给它、由它转发的。** 所以接口没填，Cookie 填了也没人用得上——
你会一直只听到 30 秒试听。

---

## 第一步：Fork 一份代码（3 分钟）

1. 注册 / 登录 [GitHub](https://github.com)
2. 搜 `NeteaseCloudMusicApi`，挑一个**最近还在更新**的 fork（这个项目被下架过，
   现在活跃的是几个分支，名字时不时变，所以这儿不写死）
3. 进那个仓库，右上角点 **Fork** → **Create fork**

现在你自己账号下就有一份了。

## 第二步：部署到 Vercel（5 分钟，免费）

1. 打开 [vercel.com](https://vercel.com)，**用 GitHub 账号登录**
2. 点 **Add New… → Project**
3. 在列表里找到你刚 fork 的那个仓库，点 **Import**
4. **什么都不用改**，直接点 **Deploy**
5. 等 1~2 分钟，看到撒花动画就成了

部署好之后你会拿到一个地址，长这样：

```
https://你起的名字.vercel.app
```

## 第三步：先自己验一下通不通

浏览器里打开（把地址换成你自己的）：

```
https://你的地址.vercel.app/search?keywords=周杰伦
```

- 看到**一大堆花花绿绿的 JSON** → 成了 ✅
- 看到 404 / 报错 → 部署没成，回第二步重来

## 第四步：填进 App

**设置 → 一起听 → 「配网易云搜索接口」**

把 `https://你的地址.vercel.app` 粘进去（**就填到 `.vercel.app` 为止**，
后面什么都不要加），保存。

然后随便搜一首歌试试——**搜得出来就说明接好了**。

## 第五步：登录账号（放 VIP 歌）

接口通了之后，同一页上有个「**扫码登录**」：

1. 点它，出一个二维码
2. **手机上的网易云 App** → 扫一扫 → 扫这个码
3. 手机上点确认
4. App 里显示「登录成功」

Cookie 就自动存好了，**只存在你这台设备上**，不会传给任何人。

> 比手动贴 Cookie 强：不会粘错、不会漏掉关键那一段。过期了重扫一次就行。

---

## 常见问题

**Q：搜得到歌，但一放就说「拿不到播放地址」**
八成是没登录，或者 Cookie 过期了。重新扫一次码。

**Q：昨天还好好的，今天全不行了**
Vercel 的 IP 段被网易云屏蔽得很厉害，**这是常态**。两条路：
删掉重新部署一次（换个 IP 碰运气），或者换成装在自己的服务器上（稳得多）。

**Q：有些歌怎么都放不了**
那是**版权**，不是会员。网易云下架的歌（尤其日韩），VIP 也放不了。

**Q：能不能用别人现成的公共接口？**
**别用。** Cookie 等于你网易云账号的钥匙，交给陌生人的服务器就是把账号给他。
自己 fork 一份，五分钟的事。
