# 全库审计工单（给 5.1 审计员 · 2026-09-04 夜）

你是本仓库的**审计员**，不是施工者。今晚的产出是一份报告，不是一行代码。

## 铁律（违反任何一条=整单作废）

1. **只诊断，不动刀。** 不许改任何文件、不许 commit、不许 push。你的全部输出写进
   `AUDIT-REPORT-2026-09-04.md` 一个新文件（这是你唯一允许创建的文件）。
2. **每条发现必须带 `文件:行号`**，引用真实代码；引不出行号的发现不许写。
3. **每条发现必须给判据**——「为什么这是病」的一句话推理。禁止「建议优化」「可以考虑」
   「最佳实践」这类没有判据的空话。写不出失败场景的，降级到附录，不占正文。
4. **严口径**：这家的判卷规矩是「数纸上有什么，不数作者想了什么」。宁可漏报不可虚报，
   每条要经得起「照着行号打开文件，病就在那儿」的复核。
5. 先读 `.claude/rules/` 下全部规则文件——那是这家的法律，审计以它为准绳，
   不按你自己的偏好立新法。
6. 测量/验证一律离线静态读代码。**绝不调用任何真实 API、真桥（8787/8443/VPS）、
   不发网络请求。**（家规：测量不打真桥。）
7. 报告语言：简体中文。每个专项给一个按严重度排序的表：`P0 数据会丢 / P1 功能是坏的 /
   P2 规则违背 / P3 坟场与死代码`。

## 专项一：数据安全线（最重，先做，做穿为止）

背景判例（2026-09-03 真实事故）：旧云端 Supabase 被限流后，原生壳的云同步**静默失败九天**
没有任何提醒；导出按钮因大 payload 噎死 WK 通道**无声装死**；旧书签标签页把 8/25 的旧账
整份盖上云端。三道防线同夜哑火，用户丢了九天数据。

要审的问题，每一条都拿这判例当尺子：
- 找出所有「失败了但用户看不见」的路径：云推/云拉/导出/导入/IndexedDB 写入/图库迁移/
  账本回流。逐处回答：失败时用户看到什么？（答案是「什么都看不到」的就是 P0）
- `js/cloud.js` 的 autoPush/autoPull「本地权威」规则：还有哪些路径能让**旧数据盖新数据**？
  （旧标签页、多设备、时钟漂移、suspend 竞态）
- 备份完整性：doExport 打包的键集合 vs collect() 上云的键集合 vs apply() 恢复的键集合，
  三张名单逐键对差，列出「只在其一」的每个键和后果。
- credential-vault 的设计边界：哪些数据像 API key 一样「重装必丢」但用户不知道？
- 每个 P0 给一条**失败可见性方案**（在哪儿加什么提醒/闸），一句话即可，不写代码。

## 专项二：五处一样喂合规扫

准绳：`.claude/rules/four-surfaces-same-context.md`（先精读，含全部判例）。
名单是【五处】：单人线上 / 单人线下 / 群聊线上 / 群聊线下 / 通话（单人+群）。
- 列一张矩阵：每一层上下文/禁令/规则 × 五处，标 ✅/❌/（合法差异注明理由）。
- 特别盯三种病型：①规则只挂在可选块的从句里（v55.90 型）；②声明了从没被引用（v55.95 型）；
  ③一处靠结构白送、换个结构就没了（v56.27 型）。
- 新长出来的层重点查：A 情绪底色（v62.37-39 刚接）、创作小稿、动念出口——它们接齐五处了吗？

## 专项三：家规遵纪扫

对照规则文件逐条扫，产出**排好优先级的清单**（不改）：
- 半窗残留（`h(Sheet` 全列，标哪些是新写的=违规、哪些是存量=排队）；
- 基础款 tab（一排药丸/下划线式）现存清单；
- `maxTokens` 低于 8000 的调用点（games.js、trpg.js 两块豁免地不查）；
- 深色主题写死 `#fff`/`#000` 的选中态；
- 提示词里的「内容示范」（prompt-no-content-samples 判据：这例子被逐字照抄是对的还是错的）。

## 专项四：坟场普查

- 只进不出的累积数据（phone-data-layers 的判据：这一栏里的东西会不会「不再是」）；
- 声明了但没人引用的常量/提示词块；
- 没有任何入口能到达的界面/函数；
- 同一层写在多处、彼此已经漂移的重复实现。

## 交付

`AUDIT-REPORT-2026-09-04.md`，结构：四个专项各一节（每节先 P0 后 P3 的表格），
最后一节「审计员的十条意见」——你只有十个名额，把你认为最该先修的十件事按顺序写出来，
每条一句话病灶 + 一句话为什么它排得上前十。名额珍贵，别浪费在小事上。

写完报告即收工。不修、不问、不追加。
