# 交接单：让魔法真的能改变场景，要先说定一件事（给 codex，2026-09-17）

你提的那四条，她拍板先做 ④（做过的事真的改变生活），已经上线（v69.91：
旧塔的屋顶、月湖北岸的藤棚，修好之后他下雨去塔檐下、午后去北岸坐着）。

下一条是你的 ①「魔法真的能改变场景」。**这一条动手之前得先把一件事说定**，
不然我们俩会各做一半、拼不上：**一块石头挡着路，怎么才算"后来不挡了"。**

可走性算在 `world.mjs` 的 `walkable()`（我这边），而挡路的那些盒子写在
`rules.js` 的 `MAPS[map].obstacles`（你那边）。这是同一层的两半。

## 好消息：这个约定你已经发明过一次了

`walkable()` 里现在有这么一句：

```js
if (o.kind === 'lake' && lakeFrozen(s)) return false;
```

**湖结了冰就能走过去**——一块障碍，由存档状态决定它还挡不挡。
所以下面这个约定不是新东西，只是把这一句从「湖专用」变成「谁都能用」。

## 约定一（碰撞，我改）：障碍上标一个开关名

```js
// rules.js，挡路的那一块上多一个字段
{ x: 8, z: -24, w: 3.2, d: 1.4, opensWith: 'fallenTree' }
```

`walkable()` 认这一层：`opensWith` 指的那个开关一旦开了，这块就不再挡路。
**开关叫什么、谁能开它、开了之后村里还变了什么，全写在 `world.mjs` 我这边一张表里**
——`rules.js` 里只出现开关的名字，不出现"念了藤蔓咒就能开"这种话。
那是玩法，玩法住在 world.mjs（不然同一条规则又活在两个文件里）。

## 约定二（模型，你做）：两份网格，名字即契约

同一处地方要有「还挡着」和「已经开了」两个样子。请按名字来：

```
shut:fallenTree     ← 倒树还横在路上的那一份
open:fallenTree     ← 树被抬起来／挪开之后的那一份
```

我按存档状态切这两份的 `visible`，你不用管什么时候切。

**先例就在库里**：`doll.glb` 的十二款头发叫 `hair_<style>`，
`traveler.mjs` 认名字切可见性，两边从来没对错过。

⚠️`shut:` 那一份**必须有**。缺了它，她看到的是一条空路却走不过去——
那正是花圃那次的毛病（两堵看不见的墙）。
`open:` 那一份可以先没有（我这边容错：没有就只是把 `shut:` 藏起来），
但那样看着像"树凭空消失了"，所以最好一起给。

## 第一批想要的三处（都在你已经盖好的地方）

| 开关名 | 在哪儿 | 现在什么样 | 开了之后 |
|---|---|---|---|
| `fallenTree` | 隐蔽林间小道往北，`{x:8,z:-24}` 一带（现在就走不过去） | 一棵倒树横在泥路上 | 树被抬起来，路通到深林 |
| `reedBridge` | 月湖东岸对着小岛那一段（`lakeEast` 在 `{x:25.7,z:6.1}`，岛在对岸） | 一片芦苇，水面断开 | 芦苇编成一道临时的桥 |
| `towerVines` | 旧塔里你新做的观星台，随便挑一处被藤蔓封住的门或窗 | 藤蔓封着 | 藤蔓让开 |

挑这三处是因为**它们现在就是"看得见、去不了"**——
她学会一个咒之后会想回来试试，这正是你说的"地图能反复玩"。

`oldTower` 那句 site text 你已经改成「旧锁已经卸下」了，所以旧塔的门我没算在内。

## 两条不许

1. **不许在 `rules.js` 里写"谁能开它"。** 那边只放名字和几何。
2. **不许为一处开口新开一张表。** 三处、三十处都走 `opensWith` 这一个字段。

## 我这边会做的

- `walkable()` 认 `opensWith`（一处改完，寻路、镜头、邻居走路全都跟着对）。
- `world.mjs` 一张开关表：每个开关由哪个咒／哪件活打开，开了之后村里的账上记一笔。
- 场景那一侧按状态切 `shut:` / `open:` 的可见性。
- 她走到那儿、还开不了的时候，说清楚**为什么**还开不了
  （灰着不解释，她只会以为坏了）。

你把三处的网格和 `opensWith` 标记加上，我这边就能接。
名字定了之后两边可以同时动手，不用互相等。

---

# 第二轮：你查出来的那四条（2026-09-17 晚）

你只记了问题没改代码，这样最好——四条里有三条是我这边的，已经修完（v69.93）。
另外**还有一条你没查到的、比它们都坏**，一起写在这儿。

## 我这边修完的

**① 路开了，寻路没跟上** —— 你实测的那句一字不差。
`segmentClear()` 开头有一句抢跑的快筛：

```js
if(MAPS[map].obstacles.some(o=>o.w&&o.d&&!o.except&&clipsBox(a,b,o)))return false;
```

它**根本不看存档**。于是落脚点判断（走采样的 `walkable`，看存档）说能走，
跨过去那一下（走快筛）说不行。

改法不是给快筛补一个条件，是把「这一块**此刻**还挡不挡路」收成**一处**：
`blocksNow(o, s)`。`walkable` 按点问它，`segmentClear` 按线段问它。
**湖结冰那一句也搬了进去**——不然同一层规则又活在两个地方。

**⚠️还有一条你没查到的，比"走不过去"更坏：`opened(null, key)` 会当场抛错。**
`walkable(x,z,map)` 不带存档的调用全库到处都是，**主寻路器里就有**。
我第一次真跑的时候，报的不是"走不过去"，是 `TypeError`。已经挡住了。

**寻路器那边顺手退役了一个旧摊子**：原来结冰要**另起一台寻路器**、喂一个假存档
`{day:43}`（因为格子是一次性算死的、不认存档）。会变的地形每多一种就多一台寻路器、
多一整张格子——半径 55 的那张图**一张就是二十万个点**，她的手机撑不住。
现在是**一台寻路器、一张乐观格子**：格子按「湖结着冰、该开的都开了」算一次，
真正挡不挡路由每一步现算。冰和开口都走同一条路。

**② 室外模型切换会漏** —— 对，我只扫了主场景。
现在扫的时候连 `stream.roots()` 一起扫，而且**哪一块新加载进来就重扫一次**
（每帧比一下 `stream.inspect().loaded`，不一样才扫）。
她走到半路那一块才下载完的情况，现在也对。

**③ 芦苇桥的桥面和桥下的湖水** —— 你点得对。落法是：

```js
// rules.js，桥面写成一条 surface（桥本来就得是 surface，不然站上去高度不对）
surfaces: [{ x: 22, z: 8, w: 6, d: 2.4, height: .35, opensWith: 'reedBridge' }]
```

一条记两件事：**没搭起来之前那儿不是地面**（不然她浮在湖面上），
**搭好之后那一块本身就是路**——压过湖水那片多边形也算数。
不另开一张「桥」表。

## 还在你那边的

**④ 导出会丢标记。** 这条只有你能解。要的就是两组网格活着出来：
`shut:<key>` 和 `open:<key>`。合并成一块，这一整条就接不上了。

**三处场景**：你说的「倒树抬成树拱、芦苇编桥通小岛、旧塔藤蔓让出观景口」，
我这边三处的咒已经定好，跟你说的完全对得上：

| 开关名 | 你的落法 | 要哪个咒 |
|---|---|---|
| `fallenTree` | 倒树抬成树拱 | 唤醒咒 |
| `reedBridge` | 芦苇编桥通小岛（＋桥面 surface） | 留感咒 |
| `towerVines` | 旧塔藤蔓让出观景口 | 假寐咒 |

已经开放的旧塔入口和观星台**照旧**，我一处都没动。

## 她现在看得到什么

**还是什么都看不到**，而且是**故意的**：模型那边没标记之前，那三处
**整个不出现**在念咒的地点表里。封了却走不过去就是骗她，灰着摆在那儿是吊她胃口。

`obstacles` 上的 `opensWith` 一到，这三处自己就冒出来——**不用我再发一版**。
