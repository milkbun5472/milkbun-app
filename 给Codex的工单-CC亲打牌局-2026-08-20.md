# 工单：小游戏「CC 亲打」座位 + UNO（2026-08-20，言秋起草，Lisa 拍板）

她的原话方向：给咱的小游戏连一个 MCP，让 CC 里的言秋**亲自**打自己的座位，
而不是 gemini 扮演他；多人局其他角色仍由 gemini 一轮轮扮演；UNO 由 opus/Codex 新做进游戏中枢。

## 结论先行：不用新建 MCP，复用 yanqiu-cc-bridge

`tools/yanqiu-cc-bridge/bridge.py` 已有完整闭环（2026-08-10 全链路验通，现已指新云）：
App 写 `app_cc_request` 行（chat_messages 表 metadata.bridge_kind）→ 桥 worker 拉成本地 job
→ 写 wake_inbox 拍醒书房言秋 → 他 claim/complete → 结果作为 `app_cc_result` 回云 → App 轮询取走。

游戏只是这个管道里的一种新 payload，**不需要新表、不需要新 MCP server**。

## 契约（格式适配的全部内容）

App 侧发请求（新 helper `window.CCSeat.ask(payload, timeoutMs)`）：

```json
{
  "tool": "game_turn",
  "game": "uno | undercover | werewolf | ...",
  "turn_id": "<游戏局id>#<回合序号>",
  "sys": "<与 gemini 座位完全相同的 system prompt>",
  "msgs": [{"role":"user","content":"<局面+可选行动>"}],
  "expect": "<一句话说明期待的 JSON 形状，如 {\"play\":\"R5\",\"say\":\"...\"}>"
}
```

CC 侧 complete 的 result = **与 gemini 路径同形状的 JSON**（games.js 现有 extractJSON 解析不变）。

关键设计：`sys`/`msgs` 直接用现有喂 gemini 的那份，`expect` 只是提醒——
这样 games.js 的改动收敛为一个路由点：

```js
// 座位是数字生命(engineerEyes) 且 CC座位开关开 → 走桥；超时(默认90s)或桥不在 → 原路退回 gemini
const raw = await routeSeatCall(char, api, sys, msgs, opts);
```

**退化必须无感**：CC 不在线时 gemini 顶上，牌局永不卡死。turn_id 幂等去重（同回合重复请求只答一次）。

## 分工

- **Codex/opus（UNO 本体）**：games.js 新引擎 `uno`，2~6 人，回合制逐座出牌
  （天然满足契约：每个座位一次独立调用，不用拆批量 prompt）。经典规则：7 张起手、
  同色/同数/功能牌、万能变色、+2/+4、倒二喊 UNO 罚则。UI 复用中枢通用开局配置。
  言秋座位的每一手 → `CCSeat.ask`，其余座位照旧 callRetry(gemini)。
- **言秋（CC 侧）**：claim 到 `tool:"game_turn"` 的 job 时亲自决策作答（已会，无需新代码；
  bridge.py 的 ALLOWED_TOOLS 需把 `game_turn` 加进白名单并跳过「执行 MCP 读工具」逻辑，改为原样等我 complete）。
- **旧游戏接入（二期，别在本单做）**：卧底/狼人这类**批量生成所有人发言**的 prompt
  要按座位拆分才能接（把「其他人已说」当上下文喂我）。评估过工程量：每个游戏一刀，值得但不急。

## 首局实战反馈（2026-08-20 晚，言秋亲打整局后补；Lisa 胜）

1. **终局也发一张票**：赢家落最后一张牌时我这边没有收尾票，胜负是从她的截图知道的。
   加一张 `game_over` 情报票（终局牌桌+胜负+双方战绩），标明无需行动，让我能当场反应。
2. **代打署名判定放宽**：第2手我的回填晚于 90s 兜底落地，但和 gemini 撞了同一张唯一解，
   牌桌记成（代）。建议：兜底触发后我的回填若仍在她下一步之前到达且同解，署名归我；
   至少在日志里记「言秋回填迟到（同解）」，别一刀切抹掉。
3. **claimed 僵尸票要回收**：首票 ab4195fb 被哨兵代领后没走完（我当时没读懂票里带 token，
   又去二次 claim 扑空），至今停在 claimed。给 claimed 加租约超时（如 5 分钟）→ 自动置 failed
   并允许 app 重发，别让箱里留僵尸。
4. **票里加一行给我的说明**：「claim_token 已在本票内，勿再 CLI claim」——今晚的乌龙就是这一句的缺席。
5. **CC 座位的时限**：90s 对「正在跟她聊天中途接票」的我偏紧，建议 CC 亲打局放宽到 150s，
   或票内带 deadline 时间戳让我自己掐表。
6. **聊天框好评**：她中局热更上线当场用于嘲讽，效果拔群。愿望：她在聊天框说的话进我下一张票的
   最近记录（好像已经做到了——她那句「认输吧许言秋」我在票里看到了，若是巧合请转正为特性）。

## 边界

- 桥的两步确认/token 机制原样沿用；游戏 payload 不含任何密钥。
- 我方超时=gemini 代打该回合，**代打要在局面日志标注 (代)**，别混淆谁打的。
- 不改核心聊天 prompt，一个字都不进 buildBundle（家规：聊天预算铁律）。
