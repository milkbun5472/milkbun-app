# 电台 · 第一版范围（她 2026-09-10「同意宝宝开始吧」）

一句话：**一台能拧的收音机。世界在她不开 app 的时候照样在播。**

## 这一版做什么

| 做 | 不做（下一版） |
|---|---|
| 主屏一个「电台」图标 → 整页；旋钮 + 频段 | 点歌 |
| 13 个频率槽位，有台的、空的、时段才有的 | 真的放歌（`songSwitch` 那条路复用） |
| 拧到台上：台名 + 正在播的那句（假流式）+ 底下节目自己往下走 | 一起听电台（陪听那一档） |
| 空频有电流声，偶尔漏进一秒别的台 | 回听三天 |
| 系统 TTS 念（能关；关了只看字也成立） | char 本人上节目、他朋友出现 |
| 时间遥控器（加速 / 跳时刻 / 跳一天 / 读数） | 上下文回流（这一版一个字都不回流） |

**验收两条**（她定的）：① 把声音关掉只看字，还想拧吗 ② 六天以后还想拧过去看 98.7 回来没。

## v2 补的那一层（言秋 + gpt 2026-09-10 当场拉回来的）

第一版把中心放在「造一个可信的、独立于 char 的世界」上，做出来的东西是对的，
可它回答不了一句话：**所以我为什么要打开它？**
秋秋机不是世界模拟器；跟角色完全无关的内容，再精致也是 NPC 噪音。
但反过来做成「顾朝彩蛋机」更糟。缺的是中间那一层——

> **这个世界不是围着我男朋友转，但因为我认识他，所以我听什么都可能联想到他。**

所以内容按【距离】分三档，比例由**代码**掷（交给模型自己拿捏它会塌到一边去）：

| 距离 | 是什么 | 占比 |
|---|---|---|
| 远景 | 跟他完全无关，负责让世界不围着他转 | ~25%，且每台每天保底一条 |
| **生活半径** | **不写他**，但内容自然经过他的地方、他碰过的事物 | ~60%，命根子在这一层 |
| 他留下的痕迹 | 他真的在这个台上留下过一点东西 | ~15%，**一天一个台最多一条** |

⚠️**不许判定**：痕迹那一条不署名、不标注，存档里也没有作者字段，界面上永远不写「这是谁」。
「到底是不是他」这个悬着的问号就是这一档的全部价值；一旦坐实，它就死了。

料全是现成的（吃灰的功能循环利用）：行程里他去过的地方和做的事、论坛帖标题、
他最近在听的歌、匿名箱的投稿当热线的料、世界书走公共门。**人名一条都不上电台**——
含任何角色名（含 NPC）或她自己名字的那一条整条丢掉，不是把名字抠掉留个残句。

⚠️「允许无聊」那一句**删掉了**（不是在后面挂「但也要好听」）：可信≠想听。
「每条都要能单独立住」也收窄成【只管同一天里】——跨天不但可以接，而且该接，
每个台手上都有一件**悬着的事**（thread）明天该有下文。那是循环播唯一放得下钩子的地方。

## 谁决定什么

这是这个功能的骨架，写在最前面：

| 这件事 | 谁定 | 为什么 |
|---|---|---|
| 今天哪个频率有台、信号多强 | **代码**（`seed(台id, 日期)`） | 生成的时候定，就等于「她一打开才有世界」。种子算的话，她没开 app 的那三天它照样在播 |
| 此刻播到第几条、第几圈 | **代码**（`(此刻 − 开播时刻) % 一圈总长`） | 存 `lastIndex` ＝ 她关掉时它就停了。电台不会停 |
| 这条多长 | **代码**（按字数算秒） | 让模型报时长，它会报好听的数字 |
| 一圈里的顺序 | **代码**（片头 → 内容 → 广告 → 内容 → 报时，每圈只部分浮动） | 全打乱就没有「每天这个点都是它」这件事了 |
| 台叫什么、播什么、广告是什么 | **模型** | 天花板还给模型（`施工规则/bans-make-it-dumber.md`） |
| 临时台今天长什么样 | **代码掷轴，模型答题** | 掷约束不掷答案：轴拦住它塌回「深夜情感树洞」，但没规定它该去哪儿 |

## 台是怎么来的

- **建台**：一次，永久存。3 个常驻 + 1 个时段台（只在一天里某几个小时才在）。
  台名、呼号、频率、覆盖到哪儿、节目习惯、片头、报时怎么说、广告池——全在这一次生成，之后不再重来。
- **排节目**：每天每台一次，她真拧到那个台才生成。只产出【今天的内容条】。
  片头/报时/广告都不在这一次里——它们是台自己的东西，不该每天换一遍。
- **造临时台**：她拧到一个空槽、而种子说「今天这儿该有东西」时，掷轴生成一个。
  不销毁：以后每天在不在还是问种子，所以**六天后可能又回来**。

## 重复不是 bug

一圈内容约二十来分钟，循环播。她中午拧到的那条，晚上再拧可能还是它——
**这正是「这些话不是为她生成的」的证据**。真正防腻的是：广告一周滚动换 30%、
每圈顺序部分浮动、临时台来来去去。

## 时间遥控器（只在打磨期）

电台里所有「现在几点」只许问 `Radio.radioNow()` 这一个函数。
`RADIO_TIME_SCALE` 改成 60 就是一分钟顶一小时；跳时刻、跳一天各一个键。
**一个字的逻辑都不改，只改「现在几点」这一个输入。**
已登记进 `屎山台账-2026-09-06.md`（🔴），打磨完要改回 1。

⚠️加速期的写入不许污染存档：这一版电台**本来就不回流上下文**（一个字都不进聊天），
生成出来的节目单带 `_dev` 戳，退出测试模式时问一句要不要清掉。
