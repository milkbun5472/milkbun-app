// 内在生活系统 B · 关系轴检测接线（v49.45 shadow step 2）
// 只对批准试点在回复落地后调用 bg；只写 A 独立 IDB 的 relationAxes，不诊断、不注入、不喂 A 情绪。
(function(root,factory){"use strict";const api=factory(root);if(typeof module==="object"&&module.exports)module.exports=api;if(root)root.InnerLifeBShadow=api;})(typeof globalThis!=="undefined"?globalThis:this,function(root){
  "use strict";
  const PILOTS=Object.freeze({"阿屿":["continuity","neglect","boundary","seriousness"],"顾暮":["identity","continuity","boundary","neglect"]});
  const DENY_NAMES=Object.freeze(["小克"]),AXES=["identity","continuity","seriousness","boundary","neglect","repairFailure"],KINDS=["harm","repair_progress","neutral"],REPAIRS=["behavior_changed","apology_only","silence","elapsed","mood_softened",null];
  const queues=new Map(),clean=(v,n)=>String(v==null?"":v).trim().replace(/\s+/g," ").slice(0,n),nameOf=char=>clean(char&&char.name,40);
  function pilotFor(char){const name=nameOf(char);if(!name||DENY_NAMES.some(x=>name===x||name.includes(x)))return null;const enabled=PILOTS[name];return enabled?{charId:String(char.id||""),name,enabledAxes:enabled.slice(),phase:"shadow",approvedBy:"lisa"}:null;}
  function messageRows(messages){return (Array.isArray(messages)?messages:[]).filter(m=>m&&["user","assistant"].includes(m.role)&&clean(m.content,1)&&!m.recalled&&!(["system","ooc","thought","thinking","offlinelog"].includes(m.kind))).slice(-10).map((m,i)=>({id:clean(m.id||m.mid||(m.ts?"ts_"+m.ts:"b_idx_"+i),160),role:m.role,text:clean(m.content,500)}));}
  function detectorSpec(char,pilot,state,messages){
    const rows=messageRows(messages),active=state&&state.relationAxes&&state.relationAxes.axes?Object.entries(state.relationAxes.axes).filter(([,v])=>v&&v.active).map(([axis,v])=>({axis,enteredAt:v.enteredAt,lastHarmAt:v.lastHarmAt,repairLocked:v.repairLocked})):[],anchors=state&&state.emotion&&state.emotion.temperament&&state.emotion.temperament.anchors||[];
    const system="你是关系含义检测器，不扮演角色、不续写对话。判断这一轮是否明确伤到指定关系轴，或是否出现同轴真实行为改变。亲昵脏话、双方玩笑、惯常互损、撒娇绝不能判伤；只道歉、暂时安静、时间过去、情绪缓和绝不能判修复。六轴：identity身份被混同或可替换；continuity亲近后被突然切断；seriousness真诚被当玩笑取消；boundary边界表达后仍被推或否认；neglect重要信息被绕开或长期悬置；repairFailure同一处不适再次发生且未真正处理。只输出 JSON，不要解释。";
    const payload={role:{name:pilot.name,temperament:anchors.slice(0,6)},enabledAxes:pilot.enabledAxes,activeEpisodes:active,messages:rows,output:{events:[{axis:"enabled axis",kind:"harm|repair_progress|neutral",confidence:0.0,explicitRelationMeaning:false,playfulContext:false,repairKind:"behavior_changed|apology_only|silence|elapsed|mood_softened|null",evidenceMessageIds:["message id"],evidenceQuotes:["verbatim quote"]}]}};
    return {system,messages:[{role:"user",content:JSON.stringify(payload)}],maxTokens:6000,rows};
  }
  function validateEvents(raw,enabledAxes,messages){
    const rows=messageRows(messages),byId=new Map(rows.map(x=>[x.id,x])),list=raw&&Array.isArray(raw.events)?raw.events:[];
    return list.slice(0,8).map(item=>{const axis=clean(item&&item.axis,30),kind=clean(item&&item.kind,30),confidence=Number(item&&item.confidence),repairKind=item&&item.repairKind==null?null:clean(item.repairKind,30),ids=Array.isArray(item&&item.evidenceMessageIds)?item.evidenceMessageIds:[],quotes=Array.isArray(item&&item.evidenceQuotes)?item.evidenceQuotes:[];if(!enabledAxes.includes(axis)||!AXES.includes(axis)||!KINDS.includes(kind)||!Number.isFinite(confidence)||confidence<0||confidence>1||!REPAIRS.includes(repairKind)||!ids.length||ids.length!==quotes.length)return null;const evidence=ids.slice(0,3).map((id,i)=>{const mid=clean(id,160),quote=clean(quotes[i],180),row=byId.get(mid);return row&&quote&&row.text.includes(quote)?{id:mid,quote}:null;}).filter(Boolean);if(!evidence.length||evidence.length!==Math.min(3,ids.length))return null;return {axis,kind,confidence,explicitRelationMeaning:item.explicitRelationMeaning===true,playfulContext:item.playfulContext===true,repairKind,evidenceMessageIds:evidence.map(x=>x.id)};}).filter(Boolean);
  }
  function observe(input){
    const char=input&&input.char,pilot=pilotFor(char);if(!pilot||!pilot.charId||!input.ownerId||typeof input.runDetector!=="function")return Promise.resolve({skipped:true});
    const prev=queues.get(pilot.charId)||Promise.resolve();const job=prev.then(async()=>{const Store=root&&root.InnerLifeAShadow,Core=root&&root.JiwenEmotionA,start=Date.now();if(!Store||!Core)return {skipped:true};try{let state=await Store.get(input.ownerId,pilot.charId),now=Date.now();if(!state)state=Core.createState(Store.hash(pilot.charId),now);if(!state.relationAxes||state.relationAxes.schemaVersion!==1){state.relationAxes=Core.createRelationAxes(pilot.enabledAxes,now);state.relationAxes.pilotConfig=pilot;}const spec=detectorSpec(char,pilot,state,input.messages),raw=await input.runDetector(spec),events=validateEvents(raw,pilot.enabledAxes,input.messages);const elapsed=Math.max(0,Math.min(7*24*60,(now-Number(state.relationAxes.updatedTs||now))/60000));if(elapsed>0)state.relationAxes=Core.regressRelationAxes(state.relationAxes,elapsed,now).state;const audits=[];events.forEach(event=>{const result=Core.applyRelationEvent(state.relationAxes,{...event,at:now},now);state.relationAxes=result.state;audits.push(result.audit);});state.updatedTs=now;state.revision=Number(state.revision||0)+1;const saved=await Store.put(input.ownerId,pilot.charId,state);await Store.addRelationDiagnostic(input.ownerId,pilot.charId,{t:now,latencyMs:Date.now()-start,rawCount:raw&&Array.isArray(raw.events)?raw.events.length:0,validCount:events.length,audits});return {saved:!!saved,eventCount:events.length,audits};}catch(_){try{await Store.addRelationDiagnostic(input.ownerId,pilot.charId,{t:Date.now(),latencyMs:Date.now()-start,detectorFailed:true,rawCount:0,validCount:0,audits:[]});}catch(e){}return {saved:false,error:"B shadow detector failed"};}});queues.set(pilot.charId,job.catch(()=>null));return job;
  }
  async function report(ownerId,char){try{const pilot=pilotFor(char),Store=root&&root.InnerLifeAShadow;if(!pilot||!Store)return {pilot:null,calls:0};const state=await Store.get(ownerId,pilot.charId),stats=await Store.relationReport(ownerId,pilot.charId);return {pilot,state:state&&state.relationAxes||null,...stats};}catch(_){return {pilot:null,calls:0,error:"B 影子诊断读取失败"};}}
  return Object.freeze({PILOTS,DENY_NAMES,pilotFor,messageRows,detectorSpec,validateEvents,observe,report});
});
