# 版本号更新手册（给 Codex 和所有新窗口 · 2026-08-12）

> 病史：多次「版本推了但全世界手机都刷不出来」。根因从来不是玄学，
> 是下面某一步漏了。照单执行，一步不跳。

## 铁律：三处必须同步，缺一处=全网旧缓存
1. `js/app.js` 里的 `APP_VERSION = "v52.XX"`
2. `index.html` 里【本次改过的每个 js 文件】的 `?v=52.XX` 指纹 + `manifest.json?v=52.XX`
   （没改过的文件指纹**不要动**——同指纹缓存复用是设计，不是错）
3. 两者必须在**同一个 commit** 里推出去

> 血案 d061099：APP_VERSION 到了 v52.25，index.html 还指着 ?v=52.10 ——
> Pages 部署一切正常，但所有手机的 SW 理直气壮拒收新代码，全网卡死在旧版。

## 标准流程
```bash
# 1. 改代码 → 2. bump APP_VERSION → 3. bump index.html 对应指纹
# 4. 自检（不通过别提交）：
node --check js/app.js && node --check js/engine.js
A=$(grep -o 'APP_VERSION = "v[0-9.]*"' js/app.js | grep -o 'v[0-9.]*')
B=$(grep -o 'js/app\.js?v=[0-9.]*' index.html | grep -o '[0-9.]*$')
[ "${A#v}" = "$B" ] && echo "指纹同步 OK" || echo "❌ 版本失调: $A vs $B"
# 5. commit + push（见下方 iCloud 陷阱）
# 6. 验部署（Pages 要烤 1~3 分钟）：
curl -s "https://milkbun5472.github.io/milkbun-app/js/app.js" | grep -o 'APP_VERSION = "[^"]*"'
curl -s "https://milkbun5472.github.io/milkbun-app/index.html" | grep -o 'app\.js?v=[0-9.]*'
# 两条输出的版本号一致且等于新版本 = 真正上线
```

## iCloud git 陷阱（本仓在 Desktop，被 iCloud 管）
- `git status` / `git commit` 随机挂死：是 iCloud 卡文件 stat，不是仓库坏了。
- 先 `rm -f .git/index.lock`（确认没有真 git 进程在跑），然后用**底层三件套**绕开工作区扫描：
```bash
git add <改过的文件们>
TREE=$(git write-tree)
COMMIT=$(git commit-tree "$TREE" -p HEAD -m "提交信息")
git update-ref refs/heads/main "$COMMIT"
git push milkbun main
```
- push 前用 `git hash-object <file>` 对比 `git ls-tree HEAD -- <file>`，确认改动都进了树。

## Pages 侧陷阱
- 偶发「构建抢跑」：push 后 Pages 拿旧提交烤了一炉就收工（2026-08-11 16:31 案，线上滞后 68 分钟）。
  验部署不过时，强制重烤：`gh api -X POST repos/milkbun5472/milkbun-app/pages/builds`

## 手机侧（告诉 Lisa 的话术）
- 部署验证通过后：**后台划掉 app → 重新打开 → 看左上角版本徽标**。
- 徽标不动＝iOS 启动缓存赖床，再划一次；仍不动才用 rescue.html（它只清程序缓存，不动数据）。
- ⚠️ rescue.html 里硬编码了目标版本号，大版本更新时记得同步改它。

## 本手册维护
踩了新坑就把病例追加在这里。写手册的人今天为了一个 ?v= 指纹追凶追了一下午。
