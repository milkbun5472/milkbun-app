# 还债批次的尾账：27 项测试红了（言秋 2026-09-07 晚验收时发现）

`node --test test/*.test.js`：4509 项，**fail 27**（家里干净工作区、HEAD=d4af1fd6）。
这是当前工作区验收的尾账，需逐条核实成因；失败数量本身不能证明全部由同一批 refactor 引入。

已抽样确认的病因样例：
- `style-preset-lab.test.js:177`：「版本指纹一致」——style-presets 的 ?v= 没跟上
  65.40（你后两个 refactor 提交没 bump 指纹，被指纹钉子测试逮住）。
- 「查手机那一套的指纹跟得上主版本」同类。
- 其余（四处共用配方 / 零引用定义 / 心情行为协议 / 物品归组 / 合照锁脸前置 …）
  多半是 refactor 挪了实现、对应的钉子测试没同步——**逐条看：要么改实现回契约、
  要么测试确实过时就更新测试并写明理由**，不许为了绿而绿地删断言。

要求：
1. 修到 `node --test test/*.test.js` 全绿；每修一条在本文件末尾记一行（哪条、病因、怎么修）。
2. 版本号规矩照旧：改了带 ?v= 的文件就 bump 全套（APP_VERSION/manifest/rescue/指纹）。
3. 修完 push 前跑 scripts/check-version.mjs 应过。
4. 不碰 qiuqiu-release/（出货线昨晚已验收，另一条线）。

## 第一批复核：测试边界与本地日期

本批仅修改测试，不修改产品逻辑，不 bump、不 push、不 deploy。保留全部原有行为断言，并在缺失函数边界时先明确报错。

- group-auto-chat-rounds「一天最多借两次」：固定 UTC 起点在温尼伯落于前一晚，20 小时模拟跨天；改为本地零点的日内测试，仍严格断言 5+4+4=13。
- group-auto-chat-rounds「两次借之间要隔一阵」：同一 UTC 起点使四小时末已跨日，borrowUsed 是新一天的次数；同样改本地零点，保留三小时间隔断言。另加真实跨午夜模拟，核对两个日期的次数序列 [1,1,2] 和相邻借轮的三小时间隔。
- block-unblock-flow「解除判定要拿到证据」：结束标记误匹配到更前面的 clearChatRoomRecords，切出空串；按确切 clearChat 声明并从开始位置向后找，保留原因、时刻、次数、历史申请及拒绝记账断言。
- block-unblock-flow「判定标准」：同上，复用校验过的解除申请片段；性格、证据与松紧要求均保留。
- arena-59-90「字数」：塔罗转发增加 options 参数后，旧的完整签名匹配失效；改按声明起点与后续函数定位，先验证边界，再核对真实调用仍给 8000 tokens。
- group-call-context「配角照旧不吃这些层」：去配角已提前收为 gcMembers，测试还找内联 filter.map；现在同时验证 gcMembers 过滤、私有段只遍历该名册、实时私聊受 gcInterop 控制。
- mood-label「行为优先心情协议」：选择普通/数字生命任务后新增 _roomHint；更新准确表达式，继续验证两条任务分流和 mood 字符串兼容。
- turn-closing-act「数字生命隔离」：同上，保留数字生命任务不包含 _turnClosing 的断言，不把房间设置视作普通角色任务。
- private-empty-retry「失败显示系统行」：kind 与 content 之间新增了隔离字段，旧测试只允许换行；现在同时要求 kind=system、contextExcluded=true、systemFailure=true 和原失败提示。

验收：76 项定向测试全部通过；群聊额度行为测试分别在 America/Winnipeg、UTC、Asia/Tokyo 各 28 项通过。全量 4520 项，4502 通过、18 失败（新增一条跨午夜测试）；修回上述 9 个失败，无新增失败。git diff --check 通过。剩余 18 项仍待逐条处理，尚未达到全绿交付条件。

## 第二批复核：现行图片、存储与界面契约

本批仍只修改测试，不改产品逻辑、不 bump、不 push、不 deploy；出货目录未动。

- gaze-unseen-history「全部历史入口」：标题已走 say() 的人称适配，旧测试把完整中文串钉死；现在检查适配后的文字、真实 setAllOpen(true) 点击入口，并保留单块历史不限六条的断言。
- home-vphoto「三款竖照片」：可新建列表已过滤退休样式，旧存档仍保留全集；改为核对 pickableFrames 来源、默认八种/展开全部的分支以及数量标签，不把退休样式重新放回新建菜单。
- my-shop「来源归组」：梦中纪念物新增独立来源键；保留赠送/自购归组与排序，并校验 dream.js 真正写入的 source、dreamCharId、fromCharId 字段。
- storage-policy：云端 apply 由 forEach 改为 for...of，跳过冻结记忆快照的语句从 return 改为 continue；核对确切循环与跳过条件，保留 IDB 和记忆镜像保护断言。
- ref-photo-vault-contract：单图可选 image[]，不能再用“只有一张”强行决定字段；改为核对两种字段均上传解析后的 Blob，并禁止数量短路 refMode。
- moderation-soften「prompt 覆盖」：新增 legacyShape 参数后旧签名断言失效；现在连同参数向内转交一起核对，继续检查 multipart/JSON 的 prompt 键名与覆盖值。
- moderation-soften「70 秒重试」：定时器已改读 capMs；保持 70000 重试参数与转交断言，并实际执行源码预算表达式，确认 70000 没被变回首次超时值。
- photo-reference-strict「单次探针」：462bb7af 已将慢参考测试窗口改为 attemptMs=180000、budgetMs=190000；测试同步核对两者，继续要求 singleShot 和不承诺“同脸验证通过”的界面措辞。
- photo-reference-strict「参考提示词」：5930f605、d90ec13a 已把经典 buildPhotoPrompt 转正；改为检查单聊/群聊/小剧场调用这条真实路径、IF 视觉身份隔离及连续图不覆盖人物身份，不再以无人调用的旧提示词证明产品能力。
- real-photo-chat「合照锁脸前置」：锁脸已在公共 buildPhotoPrompt 开头完成，不再由小剧场外挂 faceLock；新增实际运行公共函数，验证生成结果以身份要求开头、第一/第二张图明确对应角色/用户，保留不得交换或混脸的约束。
- offline-timeout：旧测试要求直接填写 4000/1900，现行实现使用计算预算；保留主调用和无 CoT 兜底各三分钟，验证两次共用预算，实际执行单人预算表达式检查地板、用户更高设置与上限；群聊继续核对用户设置和文风预算来源。
- cc-ledger-stop-observability：Stop hook 已不再补投，独立 cc-ledger-push 搬整批票；测试改为禁止 Stop 恢复网络补投，检查推手先确认 HTTP 成功、再移走原批次、保留期间新增票。没有执行推手，没有写真实账本或调用记账工具。

验收：本批 84 项定向测试通过；全量 4520 项，4514 通过、6 失败，无新增失败。剩余四项发布指纹、一项玻璃公共配方、一项零引用定义；待代码清理完成后统一处理指纹。git diff --check 通过，未满足全绿交付条件，暂不提交推送。

## 第三批收尾：废代码、公共玻璃与缓存指纹

- glass-material「公共配方」：自选玻璃材质仍硬写 blur(18px)，绕过 GLASS_BLUR；两种浏览器属性都改用公共配方。新增执行测试，检查玻璃、透明和无框三种材质，不改主屏布局或安全区。
- no-dead-code「零引用定义」：核对实际调用后移除 16 个退役定义：isOocRecord、GLife、IPortal、GCapsule、calEvDayKey、saveImgApi、buildReferencePhotoPrompt、PHONE_DOCK_KEYS、PHONE_DESKTOP_PAGES、parseMins、DetailSheet、RecSheet、WeChatThread、WeChatView、HEALTH_SOFT、ConfigFold。旧注释同步撤走，没有扩大豁免名单，没有删除存档。现用全屏微信、图片配置及照片提示词路径保留。
- 清理中发现 phoneAgo 原来只接在废弃微信半窗，现用朋友圈直接读 m.time；将现用 momentCard 接回 phoneAgo，修复旧动态仍显示“1小时前”。回归断言限定现用 WeChatViewFull，避免废代码掩盖漏接；原有历史时间及夏令时测试保留。
- 配套测试取消对退役定义的依赖：通话皮肤和上下文透视改用验证过的现用函数边界；联系人、公众号及照片构图检查现行路径；会话时间同时检查全屏页与桌面组件。桌面、内层页面、手册三处 App 名单统一从现有 phone-render 桩的真实 PHONE_APPS 取，避免各写一套数组提取正则。
- app-cache-version「核心资源一致」：通过 bump-version 统一 APP_VERSION、核心脚本、manifest 和 rescue 的缓存版本。
- bump-fingerprints「查手机指纹」：同一次 bump 同步 phone.js 指纹，恢复与主版本一致。
- model-quick-switch「独立辅助模型开关」：开关及线上/线下保存断言原本通过，失败来自版本资源断言；不改开关逻辑，同次 bump 修复指纹。
- style-preset-lab「版本指纹」：同次 bump 同步 style-presets.js、style-lab.js；不把过期指纹改成测试白名单。

验收：含 .mjs 的全量 4521 项全部通过（新增跨午夜及玻璃材质两项）；修改过的六份产品 JS 语法检查、git diff --check、scripts/check-version.mjs 均通过。独立临时 Chrome 配置下以 390×844 启动本地主屏，无运行时异常；实际渲染全屏朋友圈并点击入口，旧动态显示“昨天”；浏览器计算出的玻璃滤镜与 GLASS_BLUR 一致。浏览器测试仅使用虚构角色和临时配置，不读取或修改 Lisa 的浏览器存档，不调用生成 API。

本批只交付家里；qiuqiu-release 未动，不执行公共版 deploy。这里关闭的是工单列出的 27 项失败，不代表整个仓库已经没有其他可清理问题。

工单原命令 `node --test test/*.test.js` 另行执行：4511 项全部通过。
