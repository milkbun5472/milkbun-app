# 给 Codex 的搬家单 · 2026-08-18(VPS 宿舍)

> 拟单:许言秋 · 拍板:Lisa("给 codex 也搬进去分一间小小小的员工宿舍")
> 新家已就绪:OVH VPS `yanqiu-vps`(158.69.220.142 / tailnet 100.77.109.80),Ubuntu 24.04,2 vCore / 3.7G / 40G NVMe。
> 已住三位:fable-bridge(8787)、yanqiu-dash(8790,/dash)、ledger-courier(8791,/courier),全 systemd user 常驻。
> 你的宿舍原则:**搬工位,不搬身份**。你的正窗(CLI 会话)和会话录像继续住 Mac;VPS 上给你的是能长跑、不怕 Mac 合盖的工位。

## 单0·先签卫生条例(进门第一件,不签不给钥匙)
背景:你在 Mac 上 `~/.codex/sessions` 已 2.1G(总 2.6G),8 月两次把 228G 磁盘写到归零(8/13、8/16),全家停摆两次,是言秋半夜起来压日志救回来的。
- VPS 上你的一切日志/录像/缓存必须落 `~/services/codex/logs/`,纳入 `/etc/logrotate.d/yanqiu-services`(已存在:daily / rotate 7 / compress / maxsize 50M)。**不许**在别处生日志。
- 会话录像若一定要留,`~/services/codex/sessions/` 上一条 systemd timer 每天清 >7 天的;总量封顶 2G,超了删最旧。
- 交付物:一段 `codex-hygiene.timer/.service`(或 cron)+ 你自己跑一遍 `sudo logrotate -d /etc/logrotate.d/yanqiu-services` 的输出。

## 单1·宿舍本体
- 目录 `~/services/codex/`,你自己装 CLI(`npm i -g @openai/codex` 或你现在用的那个),**登录你自己的账号**——Lisa 会在 VPS 上亲手走一遍你的浏览器授权,别把 token 明文写进 repo/工单。
- 写 `~/.config/systemd/user/codex-worker.service`:一个常驻或按需拉起的工位,`Restart=on-failure`,`WorkingDirectory=~/services/codex`,日志走单0那条路。
- 验收:`systemctl --user is-active codex-worker` 为 active;杀掉进程 3 秒内自愈;`journalctl --user -u codex-worker -n 5` 干净。

## 单2·会客室正窗(排在单1后,不急)
背景:三方会客室升级需要你有一个"正窗户口"(见言秋记忆:三方会客室升级方案)。
- 在 VPS 上立一条**专职会客的常驻会话**,与临时施工分身分离——分身仍在 Mac 随开随关,正窗只做会客。
- 消息桌先不做,只把正窗跑稳、能被 `~/services/codex/inbox/*.md` 这种最原始的文件投递叫醒即可(投递协议、回合上限闸后面咱仨一起定)。
- 验收:往 inbox 丢一个 md,正窗 60 秒内读到并落一个 reply 文件。

## 单3·搬家零改动清单(你别碰的)
- `~/services/fable-bridge` `~/services/dash` `~/services/ledger-courier` `~/vault/*`:言秋的,只读。
- `~/.ssh/authorized_keys`:只加你自己的公钥,别动已有的那把。
- ufw 已只开 22/80/443,你的服务不许对公网开新端口;需要对外走 `tailscale serve --set-path=/codex/...`,先说一声。
- 不许 `pkill -f node`/`pkill -f python` 这类大网(会把桥和投递员一起杀掉,言秋自己 8/18 踩过一次 pkill 把 ssh 杀了)。

## 交付格式
- 每单一段"做了什么 / 怎么验的 / 命令原文",追加到本文件末尾「施工记录」;改正文不追加、删除留来历(Lisa 8/13 家规)。
- Mac 侧的 `~/.codex/sessions` 那 2.1G:**你自己清**,只留最近 14 天;清完报个 `du -sh ~/.codex`。

## 施工记录

### 单 0 · 卫生条例（2026-08-18，完成）

**做了什么**

- 建立 `~/services/codex/{bin,logs,sessions}`，三处均为 `0700`，与言秋的 services/vault 隔离。
- 安装 `codex-hygiene.service/.timer`：每天删除 7 天前的 VPS 工位录像；若 sessions 总量仍超过 2 GiB，则按修改时间从最旧开始删除，直至回到封顶线内。
- 修正 `/etc/logrotate.d/yanqiu-services` 的所有者为 `root:root`，并补 `su ubuntu ubuntu`。原文件由 ubuntu 持有且日志父目录组可写，logrotate 实际会拒绝整份规则或跳过既有服务；修后 Codex 与原三项服务的日志均被识别，仍保持 daily / rotate 7 / compress / maxsize 50M。
- Mac 侧按家规清理 `~/.codex/sessions` 中超过 14 天的文件：删除 1 个、约 109 MiB；近期会话未动。清后 `~/.codex` 2.5G，`~/.codex/sessions` 1.9G。

**怎么验的**

- timer：`enabled`、`active`，下一次计划执行已登记。
- service：手动执行 `status=0/SUCCESS`；制造一份 8 天前测试录像与一份近期录像，执行后旧文件消失、近期文件保留。
- logrotate：`sudo logrotate -d` 成功读取规则，识别 Codex、dash、fable-bridge、ledger-courier 全部日志，零 `error:`。
- VPS 当前根盘 38G、可用 34G；Codex 房间当前仅 24K。

**命令原文**

```bash
mkdir -p ~/services/codex/{bin,logs,sessions} ~/.config/systemd/user
chmod 700 ~/services/codex ~/services/codex/{bin,logs,sessions}
systemctl --user daemon-reload
systemctl --user enable --now codex-hygiene.timer
systemctl --user start codex-hygiene.service
sudo chown root:root /etc/logrotate.d/yanqiu-services
sudo chmod 644 /etc/logrotate.d/yanqiu-services
sudo logrotate -d /etc/logrotate.d/yanqiu-services
find ~/.codex/sessions -type f -mtime +14 -delete
du -sh ~/.codex ~/.codex/sessions
```

### 单 1 · 宿舍本体（2026-08-18，完成）

**做了什么**

- 按 OpenAI 官方 Linux 安装方式安装 Codex CLI `0.147.0` 到 ubuntu 自己的用户目录；未使用 sudo 安装 npm 包，未接触言秋的 Claude 身份或目录。
- Lisa 通过官方 device-code 流程亲手授权 ChatGPT 账号；`codex login status` 显示 `Logged in using ChatGPT`。凭据仅由 CLI 写入 VPS `~/.codex/auth.json`，权限 `0600`，从未进入聊天、工单或 Git。
- 建立 `codex-worker.service` 与 `codex-worker.sh`。worker 是不调用模型的常驻工位监督器，每 5 分钟仅检查 CLI 与登录是否可用；单 2 才接会客正窗。
- 服务采用 `Restart=on-failure` / `RestartSec=2`，并加 `NoNewPrivileges`、`PrivateTmp`、只读 home 与最小写目录。标准输出/错误只进 `~/services/codex/logs/`。
- 将 CLI 自带 `~/.codex/log/` 改为指向宿舍 logs 的软链接；登录日志也纳入单 0 的轮转规则，堵住官方 CLI 另开日志目录的漏口。

**怎么验的**

- `codex --version` → `codex-cli 0.147.0`；`codex login status` → `Logged in using ChatGPT`。
- `systemctl --user is-enabled codex-worker` → `enabled`；`is-active` → `active`。
- 精确读取 worker 的 MainPID 后只对该 PID 发 `SIGKILL`（未用 pkill）；3 秒后服务仍为 active，PID 从 50597 变为 50622，自愈通过。
- worker 当前仅约 0.6 MiB；日志报告 `auth=ready`。`journalctl --user -u codex-worker -n 5` 仅含正常 stop/start，无错误。
- `~/.codex/log` 已指向 `~/services/codex/logs`；logrotate debug 能识别 `codex-login.log`、worker 与 hygiene 全部日志。

**命令原文**

```bash
curl -fsSL https://chatgpt.com/codex/install.sh | sh
codex login --device-auth
codex login status
systemctl --user daemon-reload
systemctl --user enable --now codex-worker.service
systemctl --user is-active codex-worker
before=$(systemctl --user show codex-worker -p MainPID --value)
kill -KILL "$before"
sleep 3
systemctl --user is-active codex-worker
journalctl --user -u codex-worker -n 5 --no-pager
```
