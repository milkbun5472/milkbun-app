import test from 'node:test';import assert from 'node:assert/strict';import {restState,changeRest,restContext} from './rest.mjs';import {startTrip,restoreTrip,advanceTrip} from './travel.mjs';import {photoPlan} from './photography.mjs';
test('休息分别记录两人，旧档默认坐姿，重进保留，新旅行回桌边',()=>{let s=startTrip({day:20,minute:700,epoch:'rest'},()=>0);assert.deepEqual(restState(s),{you:false,companion:false});s=changeRest(s,'you',true);const t=changeRest(s,'companion',true);assert.equal(restState(s).companion,false);assert.deepEqual(restState(restoreTrip(t)),{you:true,companion:true});assert.deepEqual(restState(startTrip(null,()=>0,t)),{you:false,companion:false});assert.equal(restState(changeRest(t,'you',false)).companion,true);assert.equal(restState({rest:{you:'yes'}}).you,false);});
test('独自旅行不生成同行休息；休息照常走时，TA躺着不自动摄影',()=>{let s=startTrip({day:20,minute:700,epoch:'rest'},()=>0);assert.throws(()=>changeRest(s,'companion',true,false));s=changeRest(s,'both',true,false);assert.deepEqual(restState(s),{you:true,companion:false});assert.equal(restContext(s,false).companion,null);const next=advanceTrip(s,.1);assert.ok(next.minute>s.minute&&next.distance>s.distance);s={...s,distance:8};assert.ok(photoPlan(s,{id:'x'},1600,600));s=changeRest(s,'companion',true);assert.equal(photoPlan(s,{id:'x'},1600,600),null);assert.match(restContext(s).you,/下铺/);assert.match(restContext(s).companion,/上铺/);});
