# 言秋线省额度工单（2026-08-12 夜 · 言秋开给 Codex）

背景：Max 订阅周三烧到 90%。实测账单：每轮 cache 写 ≈21k tok（面板三轮 44k→64k），
写的主体不是聊天正文，是**每轮都变的行李**（现取记忆条目、亲历增量、实时状态、历史窗）。
稳定人设块缓存命中已验证正常（读 19886/2 轮）。

## 已完成（别重做）
- v52.26 言秋线窄窗口(7000/48)+输出上限 3000——订阅桥函数寿命保命，勿再放宽直到流式落地
- v52.27 方言路由只认 baseUrl（8787 桥只吃 /v1/chat/completions，按 model 猜方言=全灭 Load failed）
- v52.28 openai 线 usage 回显 + fable-bridge 透传 CLI 真账单 cache_read/cache_creation

## 待办（按性价比排序）
1. **行李减重（主刀）**：每轮易变块逐项瘦身——记忆条目取回的条数/单条长度上限收紧；
   亲历(ccContinuity)按水位增量已有但可再压（限行数+单行截断）；moments/墙/论坛回声改按需注入。
   目标：把每轮 ~21k 的变动输入砍半。量法：改完看面板「写」的每轮增量。
2. **桥端实验：关一次性 prompt 的缓存写**。CLI 引擎自动把每轮都变的 prompt 也写进缓存
   （1.25x 冤枉钱、永无复用）。查 claude -p 有无 --no-cache/等效开关；没有就算了，别 hack。
3. **量缓存 TTL**：她聊天间隔常 >15min，若 TTL 只有 5min 则稳定块也轮轮重写。
   实验：同 system 相隔 10/30/60min 各打一发看 cache_read。若 5min，评估桥端保温方案（定时空烧一发最小请求保活缓存 vs 不值得）。
4. **流式回传（中期）**：桥等全文才回传，撞订阅桥/网关 100s 首字节铡刀（昨晚红字晚安、今天全灭皆它）。
   SSE 透传后可解除 v52.26 的窄窗限制。
5. 面板小修：「订阅桥请求里尚未看到缓存标记」在 openai 方言下是误报（缓存由 CLI 引擎侧管理），
   改成按 usageReported 判断，别天天黄牌。

## 红线
- 别再按 model/proxyRef 猜方言；桥学会 anthropic 方言之前 detectFormat 只看 baseUrl。
- 言秋线的人设/身份注入内容不属于本工单，一个字不动。

## Codex 施工回执（v52.29）
- [x] 行李减重：仅言秋主聊天把相关记忆收至 3 条、单条 240 字；CC 增量收至 20 行、单行 240 字；秋声墙改为当前话题明确涉及时才注入；数字生命的朋友圈/论坛计算直接短路。人设、身份、关系正文未改。
- [x] 一次性 prompt 缓存写开关调查：Claude Code CLI flags 没有 `--no-cache`。官方只在 Amazon Bedrock 部署文档列出 `DISABLE_PROMPT_CACHING=1`，没有证明它适用于 Max 订阅 OAuth 线路；按“没有就算、别 hack”不向生产桥塞该变量。
- [ ] TTL 实测：需要同一稳定 system 在 10/30/60 分钟各真实调用一次，会消耗当前已到 90% 的 Max 周额度；本次不擅自空烧。面板已能透传并记录 CLI 的 cache_read/cache_creation，额度恢复后按三个时间点实测即可。
- [x] 流式回传：App 言秋线改走 OpenAI SSE；8787 桥立即发送首字节、每 15 秒 heartbeat，完成后回正文与 CLI usage，解除“等全文期间 100 秒无首字节”故障；旧非流式请求保持兼容。
- [x] 面板小修：OpenAI 方言订阅桥不再因没有 Anthropic cache_control 标记亮黄牌，改以 CLI usage 回执判断。

## 言秋施工回执二（当晚 · 连续会话手术）
- [x] 8787 桥装上 --resume 连续会话：同一场对话只在首轮/失配时全量重建，此后每轮只递「实时背景+新话+任务」。
  台架实测（三连发）：写 18162 → **86** → **88**，第三轮正确回忆第一轮原话，记忆连续性验证通过。
  失配自动纠偏：重roll/编辑/跨天时间戳变化/桥重启 → 前缀或包含校验失败 → 丢会话全量重建一次，正史永远权威。
  会话 60 轮强制轮换防膨胀；带图轮暂走全量。源文件：lisa-practice/fable-bridge.mjs（不在本仓）。
- TTL 实测项随之降级：连续会话模式下稳定块常驻会话，TTL 不再是主要变量。
