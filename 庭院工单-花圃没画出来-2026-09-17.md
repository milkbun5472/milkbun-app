# 交接单：屋边那块花圃只剩两堵看不见的墙（给 codex，2026-09-17）

她 2026-09-17 报：**「家里的花园也没了」**。查下来是真的，而且比「少了个装饰」更糟。

## 现象

站在花圃正上方放大看：**那儿什么都没有，就是一片草地**。
（复现：把存档的 `position` 设成 `{x:-15.6,z:7.2}`、`map:'garden'`，放大到 200%。）

## 数据其实还在

`apps/fairy-garden/rules.js` 的 `MAPS.garden` 里，那块地一样不少：

| 东西 | 值（新的 layout:2 坐标系） |
|---|---|
| 站位 `stations.garden` / `stations.note` / `stations.sow` | `{x:-15.6, z:6.55}` |
| 互动区 `interactions` 里的 `{kind:'garden'}` | `x:-15.6, z:5.2, w:1.9, d:2.1` |
| 装饰锚点 `decor.blooms` | `{x:-16.1, z:4.6, y:.32}` |
| **两个障碍盒** | `{x:-15.6, z:5.8, w:1.85, d:1}` 和 `{x:-15.6, z:4.6, w:1.85, d:1}` |

所以功能全是好的：走过去照样能种、能浇、能收花笺。

## 真正的问题

**那两个障碍盒现在是两堵看不见的墙。**
屋子旁边有两块 1.85×1 的地方走不过去，而那儿什么也没有——
这比「花圃不好看」严重：她会以为是寻路坏了。

`village-ground.glb` 重建（v69.48 村落拉开间距那一版）之后，那两畦地的模型没跟着出来。
`grow()` 只按 `data.blooms` 画最多三朵月光花，畦本身从来不是它画的——
所以 `blooms:0` 的新存档那儿就是全空。

## 要你做的

把那两畦地补回 `village-ground.glb`，对齐上面那两个障碍盒的位置和尺寸
（它们是【碰撞的权威】，模型照它们长，别反过来改那两个数——
`interactions` 那个 1.9×2.1 的互动区也是照这两畦定的）。

⚠️空着的时候也要看得见：新存档 `blooms:0`，那时候畦上一朵花都没有，
但那块地本身得在那儿——她种下去之前就该看见自己种在哪儿。

## 我这边一起改了的

v69.58 起「种下一句」从手机册子搬到了**花圃那儿**（`stations.sow`，跟收花笺同一个点）。
所以这块地现在是三件事的现场：种、浇、收。它比以前更该看得见。

## 已处理

- 原两块障碍盒尺寸/位置保留，补稳定 ID；MAPS.garden.flowerbeds 直接引用它们。
- 常驻 village-ground.glb 补土面/木框，清掉畦内固定假花和灌木；零朵、浇水、开花、采收后仍有明确的畦。
- build_flowerbeds.py 同时供局部补建与 build_lake.py 完整重建，避免下次丢失。
- 实机发现现有小屋屋檐会挡住畦边，照料/查看花圃时淡出住宅，离开恢复；不改变碰撞或房屋位置。
- GLB 世界坐标顶点边界与碰撞匹配回归，三次浇水→采收→重开通过；浏览器脚本 scripts/checks/fairy-flowerbeds-browser.cjs。图和证据在 fairy-cottage/flowerbeds/。
