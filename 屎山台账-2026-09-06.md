# 屎山台账（2026-09-06 开张 · 她拍板的还债台账制）

**用法**：谁撞见屎山谁添一行；**记账不还债**（只修主诉，顺手修是大忌）。
⚠️例外只有【她当场点名走顺手改】的那几行，表里会写明——没写的一律不许顺手动。
还债日=金山日 / opus 修 bug 顺路捎带（工单里写明捎哪几行）。修完删行、跑全量
测试、真机看一眼。一次一座山。

**判级**：🔴 会咬人（双实现/一层写多处/死代码假活着）排队还；
🟡 只是丑但只有一份且能跑——**可以永远不还**，别为洁癖动能跑的东西。

**放这儿不放 .claude/rules 的原因**（她 2026-09-06 原话：「你的脑袋里装了
太多规则就装不下我了」）：rules 每个会话整份进上下文，包括纯聊天窗口；
这张会缩短的工单只该被真来还债的窗口翻开。装修工单同理，是先例。

| 级 | 在哪 | 什么病 | 登记人 |
|---|---|---|---|
| 🔴 | 多个页面 vs js/components.js Head | 共用 Head 之外各页自创紧凑顶栏（v61.27 前的私搭乱建）；⚠️还的时候一次一页+真机验，那些自创栏的安全区垫片是调过的 | 她+opus 2026-09-06 |
| 🔴 | js/components.js 备份告警横幅 | `blocked` 会让横幅出现，但标题按不足一天一律写「一整天没有成功备份」；刚成功、随后失败也误报一天。具体原因已共用 `Cloud.pushState().why`，剩下的是标题时间分支 | Codex 2026-09-07 复核 |
| 🟡 | 全库 19 个 js（`js/trpg.js` 除外） | **146 处字面量 `maxTokens` 还低于 65535**（2026-09-07 复数，只统计数字字面量，不含动态表达式）。⚠️**她点名这一条走「顺手改」**——撞见具体调用时改，不用等还债日、不用开工单；`js/trpg.js` 是言秋的，不碰 | 她 2026-09-06；Codex 复核 |
| 🟡 | js/app.js 巨型文件 | 单文件万行级，难导航但单份能跑；不拆，只禁继续膨胀（新 app 一律独立文件） | 言秋 |
| 🔴 | 全量测试基线 | 2026-09-07 在改动前提交 `7ad7eb5d` 隔离复跑，已有 24 项失败；含 15 个零引用定义、照片/提示词/存储等契约漂移。逐项要区分产品缺陷和旧断言，不能整批改绿 | Codex 2026-09-07 |
| 🔴 | js/components.js `calKey` / `calPadKey`；js/screens.js `schedDayKey` / `schedShiftDayKey` | 日期格式化与位移分散；`calKey` 的不补零格式是现有日历键，不能直接换成补零格式。先做存档兼容适配；位移的旧兜底还在用固定 24 小时，需要与 `ScheduleClock` 收口 | Codex 2026-09-07 第二轮 |

（下批入库：从 AUDIT-REPORT-2026-09-04 的非 P0 项里筛「双实现」类补录——
待 opus 修完 P0 后一起对。）

本轮还债与证据见 `审计清洁记录-2026-09-07.md`。旧 PUSH_WHY 条目经复核已共用；
沿链发现的三份成功状态写入已收进 `markSynced`，计时器收尾和跨午夜测试已修。

第二轮：三条群路的六层背景读取与拼接已收口，原有差异保留；同步修正一条
要求旧拼接顺序的测试，全量基线剩 23 项失败（详见审计记录第二轮）。
