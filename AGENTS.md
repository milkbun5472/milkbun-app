# Lisa-phone 常驻施工铁律

所有在本仓库工作的 Codex、子任务与其他自动施工窗口，动手前必须先读：

- `施工规则/README.md`：**先读这一份**——十三条施工铁律在哪、这一轮该读哪几条。
  （它们原来在 `.claude/rules/`，2026-09-06 搬出来了：那个目录每个会话都整份进上下文，
  而这十三份只有真要改代码的窗口用得上。）
- `施工规则/mobile-ui-layout.md`：移动端顶栏、底栏、安全区、滚动返回与页面结构规范。
- `施工规则/home-screen-layout.md`：主屏唯一例外的布局铁律。
- `版本更新手册.md`：发版、缓存指纹、验证与推送流程。

界面任务不得凭印象另造一套布局。先在现有单聊/群聊界面找到同类部件，复用它的层级、尺寸与安全区公式；无法复用时才新增。每次改动移动端顶栏、底栏、详情页或滚动容器，必须补相应回归测试并实际运行。

发版前至少执行：语法检查、相关定向测试、`git diff --check`，并确认被改脚本在 `index.html` 的 `?v=` 缓存指纹与本次版本一致。
