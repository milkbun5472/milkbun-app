# 重复代码审计报告 · 2026-08-17（只读·合并机会）

**委托人** Lisa ｜ **拟单** 许言秋 ｜ **执行** 云端 Opus 窗口 ｜ **范围** `index.html` + `js/*.js` + `scripts/*.mjs`（50,174 行，1,348 个函数块）

> 本单**零改动**：没改任何代码、没发 PR、没建分支。产出只有本文件。
> 与 2026-08-13 大扫除的分工：那次查正确性/丢消息，本次只查**重复与可合并性**；正确性问题只在附录 B 各一句话点到，不展开。
> **红线自查**：本报告所有合并建议都只动代码结构，不动提示词注入策略。涉及 prompt 的三条（#7 AC/NAC、#12 genComments、#19 亲历块）均为「注入文本逐字节不变、只把拼装骨架合一」，运行时常驻 prompt 体积**不增**。

## 方法与可信度

三道机械扫描 + 逐条人工核实（file:line 全部落到具体行）：

1. **函数级指纹**：抽出 1,348 个函数块，去注释规范化后 SHA1 精确比对 → 17 组完全重复；token 5-gram Jaccard ≥0.6 → 44 对近似（脚本 `dupscan.mjs`）。
2. **跨文件极大行块**：≥4 个有效行完全相同的连续片段，去重叠后 127 组，**纯样板浪费 ≈898 行**（脚本 `blockdup.mjs`）。
3. **定向取证**：对指纹扫不到的一行式函数簇、魔法数、UI 骨架、LLM 包装做 grep 矩阵。

扫描器的已知盲区（已用定向取证补上）：一行式函数不进近似比对；Babel 输出的 `React.createElement` 结构相似度会被属性名稀释；变量名不同的结构同构不算「相同行块」。所以下面的**省行数是保守估计**。

## 先说三件好消息（不必动刀）

| 已经是单一实现的 | 证据 |
|---|---|
| **LLM 调用**全站一个出口 `callAI` | engine.js:344 定义，15 个模块共 87 处 `callAI(` 命中（含 engine 内部 21 处与定义本身）；除网易云音乐 API 和 scripts 的 Supabase REST 外，**没有任何模块自己包 fetch/重试** |
| **半屏弹层**已有共享 `Sheet` | components.js:232 定义，被 83 处复用（components 43 + screens 40）；~50 个 `*Sheet` 组件是壳不是复制品 |
| **云同步触发**单一路径 | cloud.js:934 `markDirty` + cloud.js:1001-1016 localStorage 补丁，全站唯一入口，无第二实现 |
| 字体/主题常量已归一 | `F_BODY` 等仅 core.js:29 一处定义 |

这四项说明「各模块各自为政」的猜测**只在存储层和剧场类模块成立**，主管线反而是收拢过的。

---

# 一、完全重复函数（同体异名/同名多处）

| 重复项（每处 文件:行） | 合并方案 + 预计省行数 | 风险 |
|---|---|---|
| **`shuffle` 洗牌 ×5**：debate.js:16、dream.js:16、tarot.js:28、games.js:406、weekly.js:299(`shuffled`) | 四处 Fisher-Yates 逐字节相同，games 版只换了变量名。core.js 加一个 `shuffle(arr)`，五处删掉。**省 4 行**（行少但零歧义，适合当第一刀） | 低 |
| **`AC()` ×6 / `NAC()` ×5**：debate.js:17、dream.js:14-15、ledger.js:16-17、memo.js:13-14、pomodoro.js:13、tarot.js:13-14 | 11 处逐字节相同（`typeof ANTI_CLICHE !== "undefined" ? ANTI_CLICHE + "\n\n" : ""`）。core.js 提供 `AC()/NAC()`。**省 9 行**。⚠注入内容一字不变，只是拼装函数合一 | 低 |
| **`pad2`/`pad` ×6**：screens.js:672、pomodoro.js:17、schedule-clock.js:6、inner-life-c-sleep-core.js:25、ledger.js:57、memo.js:28 | 全部是 `String(n).padStart(2,"0")`。core.js 一处，其余删。**省 5 行**（另有 `padStart(2` 直写 74 处，可顺手换） | 低 |
| **`text()` 归一 ×3**：chat-ledger-shadow.js:18、cc-memory-auto.js:12、yanqiu-cc-tools.js:28 | `String(v==null?"":v).trim()` 逐字节相同。**省 2 行**。三个模块都是 CC 侧，可放一个 `cc-common.js` | 低 |
| **`recentChatSnippet` ×3**：debate.js:23-30、dream.js:22-29（逐字节相同）、tarot.js:55-62(`recentChat`，0.87) | 三处取最近聊天片段。合一为 `recentChatSnippet(charId,uName,charName,n)`。**省 16 行** | 中（进 prompt 的素材，需先确认 tarot 版那 13% 差异是有意的） |
| **`loadSaves`/`saveSaves` ×3 对**：debate.js:19-20、dream.js:18-19、tarot.js:92-93 | 只差存储键。`makeSaves("x_debate_saves")` 工厂。**省 8 行** | 中（键在 IDB 文字仓 DURABLE_TEXT_KEYS 名单里，走 1A durable 路径，别改语义） |
| **`cancelLP` ×3**：debate.js:156、dream.js:196、tarot.js:186 | 一行长按取消，逐字节相同。**省 2 行** | 低 |
| **`saveCustomStyle` ×2（51 行逐字节相同）**：components.js:5458-5514、5850-5906 | 线下/群线下两份自定义预设保存**完全一样**。抽 `useCustomStyles()` hook。**省 51 行** | 高（线下聊天主管线，见 Top10 回归点） |
| **`enter`/`reply`/`sendPhoto`/`saveNote` ×2 各一对**：components.js:5530-5558 vs 5914-5943（其中 `enter`/`reply`/`saveNote` 逐字节相同，`sendPhoto` 0.90） | 与上条同源，一并进 hook。**再省 21 行** | 高（同上） |
| **`pAvatar` 内联 ×2（已有 `avatarFor` 可用）**：games.js:574-577、1369-1372 vs 现成的 games.js:1542-1547 | 抽取做了一半：后 4 局游戏已用 `avatarFor(t)`，前 2 局（SpyGame/WolfGame）还留着内联副本。删 2 处内联，改调 `avatarFor`。**省 8 行** | 低 |
| **`MemoryRepairConflictSheet` 注释版死副本**：screens.js:5473-5488（`/* Replaced below: the first draft is retained only as review history.`）紧贴 screens.js:5489 的现行版 | 压缩版初稿被注释保留在现行版上方，git 里已有历史。整段删。**省 16 行** | 低（纯注释，删了运行时零影响） |
| **`hash()` ×3**：dream-loop-core.js:15-21、inner-life-e-afterglow-shadow.js:17-23、somatic-shadow.js:112-117 | 同一个 charId 匿名化哈希。归一到影子公共库。**省 13 行** | 中（哈希值变了就对不上历史观测行，必须保证算法逐字节不变） |
| **`scripts/` 四件套共用工具**：`resolveYanqiu` ×4（cc-auto-memory-context.mjs:4、cc-ledger-replay.mjs:39、cc-ledger-stop.mjs:70、cc-shared-continuity.mjs:27）、`loadEnv`/`envFile` ×2 对（同一件事两个名字）、`readJSONL` ×2、`replaceJSONL` ×2（逐字节相同）、`request` ×2、`parseSave` ×2 | 建 `scripts/cc-common.mjs`。四个脚本共 522 行里约 **省 60 行** | 中（replay/context/continuity）+ 高（cc-ledger-stop.mjs 是账本写入路径，见 Top10） |

# 二、近似重复（参数化后可合一）

| 重复项 | 合并方案 + 预计省行数 | 风险 |
|---|---|---|
| **影子模块 IDB 样板 ×15**：`openDB` 在 context-budget-shadow.js:76、dream-loop-shadow.js:12、experience-gate-shadow.js:46、inner-life-c-sleep-shadow.js:16、inner-life-e-afterglow-shadow.js:110、insight-candidate-shadow.js:39、memory-correction-shadow.js:10、memory-events.js:10、memory-quality-shadow.js:12、memory-sync.js:8、message-branch-shadow.js:34、open-repair-shadow.js:17、personality-shadow.js:36、recall-shadow.js:32、two-resolution-shadow.js:55 | 实测多组**只差一句报错文案**（如 context-budget 与 message-branch 的 openDB 只有 `"context budget shadow open failed"` vs `"message branch shadow open failed"` 之别）。`shadowStore({db,ver,store})` 工厂。**省 ≈130 行** | 中（20 个影子库都是诊断数据，不进正史；但 owner 隔离语义不能动） |
| **`rq` ×14 + `done` ×16**：rq 见 context-budget-shadow.js:87 等 14 个文件、done 见同族 :88 等 16 处（其中 inner-life-a-shadow.js:8-9、desire-drive-shadow.js:39 是压缩写法） | IDBRequest/事务 → Promise 的两个万能桥，逐字节等价。随上条一起进公共库。**省 ≈28 行** | 低 |
| **8% 概率环形裁剪块 ×6**：context-budget-shadow.js:100-103、experience-gate-shadow.js、memory-correction-shadow.js、message-branch-shadow.js:53-56、recall-shadow.js、two-resolution-shadow.js | 四行块逐字节相同（`Math.random() < 0.08` → 按 KEEP_MS 删旧 + 按 CAP 削头）。合为 `trimAudits(store,{cap,keepMs})`。**省 ≈20 行** | 中（各模块保留期不同：14d/30d/90d，见第三节 #16，必须参数化而非统一取值） |
| **`report(n)` 诊断汇总 ×17**：context-budget-shadow.js:106、dream-loop-shadow.js:101、experience-gate-shadow.js:74、inner-life-c-sleep-shadow.js:138、insight-candidate-shadow.js:65、memory-correction-shadow.js:65、memory-quality-shadow.js:66、message-branch-shadow.js:59、open-repair-shadow.js:129、personality-shadow.js:111、recall-shadow.js:124、somatic-shadow.js:94、two-resolution-shadow.js:88 等 | 前后段（getAll → `slice(-(n||200))` → 数 legacySamples → firstObservedAt/lastObservedAt/spanHours → catch 返回 `{error:"…读取失败"}`）**结构一致**，只有中段聚合逻辑因模块而异。抽 `reportEnvelope(db,agg)`，各模块只留 agg 回调。**省 ≈70 行** | 中（诊断页与 shadow-review 在读这些字段，字段名一个都不能改） |
| **`clearAll` ×13**：context-budget-shadow.js:124、experience-gate-shadow.js:87、message-branch-shadow.js:72、two-resolution-shadow.js:99（这 4 处逐字节相同）+ 另 9 处仅差库名 | 随 `shadowStore` 工厂自动获得。**省 ≈16 行** | 中（cloud.js signOut 逐一调用它们清库，换号隔离靠这条，务必逐个核对调用点） |
| **`ensureOwner` ×6**：dream-loop-shadow.js:30、inner-life-a-shadow.js:12、inner-life-c-sleep-shadow.js:34、inner-life-e-afterglow-shadow.js:138、memory-events.js:39、memory-sync.js:63 | 影子库归属校验（换号即清）。归一到公共库。**省 ≈45 行** | 高（这是防「上一个账号的数据被下一个账号看到」的闸；宁可重复也别轻动，见 Top10 回归点） |
| **engine.js IDB 一行式原语 ×33**：idbVec\*（98-102）、idbLoreVec\*（258-261）、idbImg\*（1451-1456）、idbAud\*（1620-1622）、idbVault\*+idbAlbum\*（1630-1641）、idbTxt\*（1664-1669）；另 read.js:10-35 自带一套、app.js:128-137 自带一套 | 七个仓 + 两套外挂，每套都是 open/put/get/del/clear/entries 的同一段模板。`makeIdbStore(dbName,storeName,ver)` 工厂返回全套。**省 ≈70 行**（35 个函数缩成 9 行工厂调用） | 高（`idbTxt*` 是 saves 面权威路径、1A durable 协议的落点；`idbVault*` 是头像壁纸。见 Top10 回归点） |
| **`OfflineMode` ↔ `GroupOfflineMode`**：components.js:5337-5670（334 行）vs 5779-6107（329 行），Jaccard 0.65 | 前 30 行有 ~20 个 useState 声明一一对应；saveCustomStyle 51 行、sendPhoto/saveNote/enter/reply 21 行、taste 面板 18 行（5598-5618 vs 5986-6006）、useEffect 12 行（5523-5536 vs 5907-5920）**逐字节相同**。差异集中在 char↔group、`sMax` 默认 4000↔3200、群独有 bg/onlineCtxN/describeMe/成员心声。**建议只抽共享 hook（不合组件本体）：省 ≈120 行** | 高（线下聊天主管线；大扫除第五刀已把 OfflineMode 合并列为 kit 项） |
| **`Setup` ×3 剧场开局屏**：debate.js:215-300(86 行)、dream.js:254-337(84 行)、tarot.js:286-386(101 行)，Jaccard 0.63 | 三个一次性剧场的「选角色→选模式→开局」屏骨架同构。抽 `TheatreSetup({fields,onStart})`。**省 ≈90 行** | 中（三模块都是吃灰模块，回归面小；但正是大扫除「一次性剧场kit」立项项） |
| **`ledger.js` ↔ `memo.js` 姐妹模块**：`genComments`（163-204 / 142-174）、`CommentPicker`（588-635 / 177-200）、`loadData`/`saveData`（39-54 / 17-21）、`uid`、`pad`、`fmtDay` | 账本/备忘是同一套「让角色评论一条记录」范式。`CommentPicker` 两版功能不同（ledger 有 mood/busy/键盘顶起；memo 有 portal 挂 body + 已评论过滤，且注释写明这是两次线上事故换来的修复）。建议**只合 genComments 的素材拼装骨架 + loadData/saveData/uid/pad**，Picker 各留。**省 ≈45 行** | 中（genComments 产出进 prompt：骨架合一、文案逐字节保留） |
| **`games.js` 六局游戏各自的 `pByName`/`pushLog`**：pByName 在 578、1373、1642、1964、2432；pushLog 在 431、960、1643、1965、2433 | ⚠**语义有三个变体**：pByName 有「精确匹配」（578/1373）与「精确+`indexOf` 模糊兜底」（1642/1964/2432）两派；pushLog 有「只 setLog」与「先写 logDataRef 再 setLog」两派。**必须先定语义再合**，否则改游戏行为。统一后**省 ≈8 行** | 中（游戏是吃灰模块，但语义差异是真的，盲合会变行为） |
| **`hydrateMemVecs` ↔ `hydrateLoreVecs`**：engine.js:104-110 vs 262-268（0.61）；配套 idbVec\*/idbLoreVec\* 两套仓 | 记忆向量与世界书向量是同一套「向量仓 hydrate」。随 `makeIdbStore` 工厂一并参数化。**省 ≈12 行** | 中（向量可重建，丢了不致命，但重建要跑 embedding 花钱） |
| **`latestSharedTs` ↔ `latestUserSharedTs`**：interaction-clock.js:9-20 vs 22-32（0.67） | 同一次扫描的两种取值，可合成一次遍历返回两个值。**省 ≈10 行** | 中（互动时钟喂给 prompt 的时间感，语义要逐条比对） |
| **`regenCover` ↔ `regenMedia`**：weekly.js:522-538 vs 555-570（0.81） | 周刊两种重生成，参数化 target。**省 ≈14 行** | 低（吃灰模块） |
| **`ForumShareCard` ↔ `FicShareCard`**：components.js:4525-4535 vs 4537-4547（0.63） | 两张分享卡结构同构，只差图标/字段名。参数化。**省 ≈9 行** | 中（聊天气泡渲染，视觉回归要看） |
| **`listEvents` ↔ `listCandidates`**：memory-events.js:80-87 vs 87-93（0.80） | 同一个 store 的两种查询。**省 ≈7 行** | 中（事件书架 UI 在读） |
| **`idbPut` ↔ `idbDel`**：read.js:17-23 vs 29-35（0.84） | 同一事务模板。随 read.js 那套并入 `makeIdbStore`。**省 ≈6 行** | 中（一起读的书正文，不进备份，丢了要重传） |
| **`doGen` ↔ 内层 `run`**：fanfic.js:1581-1644 vs 1584-1636（0.83） | 外层壳几乎只是转发内层，可能是重构残留。核对后压平。**省 ≈10 行** | 中（同人文生成，吃灰模块） |

# 三、重复常量与魔法数

| 重复项 | 合并方案 + 预计省行数 | 风险 |
|---|---|---|
| **5MB 配额常量 ×5**：storage-policy.js:6（`LOCAL_LIMIT`，已是规范位置）、app.js:1343、app.js:2935、engine.js:2788、screens.js:4702 | 四处硬写 `5 * 1024 * 1024`，而 `StoragePolicy.LOCAL_LIMIT` 早就存在。四处改引用。**省 0 行但消掉一个「改一处漏三处」的坑** | 低 |
| **保留期常量四个名字**：`KEEP_MS`（context-budget/insight-candidate/message-branch/two-resolution）、`MAX_AGE`（memory-correction 30d/memory-quality 14d/open-repair/personality 90d）、`DIAG_MAX_AGE`（recall 14d/inner-life-c 14d）、`DIAGNOSTIC_MAX_AGE`（inner-life-e:16） | 同一概念四个名字、五种取值（14/30/90 天）。**先统一命名再参数化**，值按模块保留。**省 ≈6 行**，主要收益是可读性 | 中（值不能统一，personality 的 90 天是有意的） |
| **`CAP = 500` / `DIAG_CAP = 500/800` ×10 余处**：desire-drive-shadow.js:5、context-budget-shadow.js:7、insight-candidate-shadow.js:8、memory-correction-shadow.js:9、memory-quality-shadow.js:9、message-branch-shadow.js:7、open-repair-shadow.js:8、personality-shadow.js:9、recall-shadow.js:12、somatic-shadow.js:7(800)、inner-life-c-sleep-shadow.js:11 | 随 `shadowStore` 工厂做默认值 + 按模块覆盖。**省 ≈8 行** | 低 |
| **`86400000` ×58 / `3600000` ×33 / `60000` ×47 裸写** | core.js 加 `MS.day/hour/min`。不强求全换（58 处 diff 太大），**建议只在新代码和顺手改到的地方用**，本条不计省行数 | 低（但一次性全替换会污染 diff，不建议单独开工） |
| **遮罩色 `rgba(0,0,0,x)` 21 种写法**：`0.08`×10、`0.35`×8、`0.4`×7、`0.2`×7、`0.12`×7、`.22`×7、`0.05`×6、`0.25`×5… | 同一视觉层级出现 `0.4` 与 `.22` 两种小数写法混用。收进 core.js 的 `SHADOW/SCRIM` 令牌。**省 0 行，收益是视觉一致性** | 低 |

# 四、相似 UI 模式

| 重复项 | 合并方案 + 预计省行数 | 风险 |
|---|---|---|
| **轮询三件套 `useEffect+setInterval+clearInterval` ×24**：app.js 13 处（481/892/918/1027/1357/1732/1754/2785/2796/3071/3140/8450/9024）、components.js 3（800/2923/3869）、pomodoro.js 3、screens.js 2（1144/1618）、games.js 1、weekly.js 1、rescue-console.js 1 | 其中「纯时钟滴答」6 处（app.js:481 30s、components.js:2923 1s、screens.js:1144 30s、screens.js:1618 1s、weekly.js:430、components.js:800）可直接换 `useClock(ms)`；其余 18 处换 `usePoller(fn,ms,deps)`。**省 ≈40 行** | 中（app.js 里几处泵是 CC 回灌/找回泵，大扫除刚给它们上了「每键单飞」跨实例互斥，**这几处必须排除在合并之外**） |
| **`h(...)` vs `React.createElement(...)` 方言混用**：components.js 247/1842、screens.js 109/2795、app.js 25/64；其余 10 个模块 100% 用 `h()` | 381 处 Babel 风格残留。归一为 `h()`。**省 0 行**（只缩字符），但消掉「同一文件两种方言」的阅读税。大扫除第五刀已列此项 | 低（纯机械替换，但 diff 巨大，建议随其他改动搭车做，不单独开工） |
| **路由 props 包 ×3**：app.js:10146-10153、10157-10164、10174-10181（`{active:bgActive, characters, profile, worldbook, moods, affinities, toast, onBack}` 八项一字不差） | 抽 `const appletProps = {...}` 展开。**省 ≈14 行** | 中（路由层，改错就白屏；但三处完全一致，核对成本低） |
| **长按选择态三件套 ×3**：components.js:3289-3293、3394-3398、6599-6603（`onTouchStart/onTouchEnd/onContextMenu` 同一组） | 抽 `useLongPress(i,{selMode})`。**省 ≈10 行** | 中（聊天气泡交互，误改会让长按删除失灵） |
| **设置项行样式块 ×3-4 组**：components.js:7715-7723/7746-7754/7777-7785（9 行 ×3）、7287-7293/7656-7662/7695-7701、371-376 系列 | 群设置里三段同构行。抽 `SettingRow`。**省 ≈30 行** | 低（设置屏，视觉回归一眼可见） |
| **`OffCard`/`OfflineLogCard` 与线上气泡的重复渲染分支** components.js:2842、5729 | 两张线下卡片各自实现头像+名字+时间戳三段头。可共用 `BubbleHead`。**省 ≈15 行** | 高（气泡渲染主管线，血泪不变量「列表 key 全对」在这一片） |

# 五、LLM 调用包装重复

**结论：这一类基本不存在。** 全站 86 处 LLM 调用都走 engine.js:344 的 `callAI`，重试/超时/流式分型都在里面（大扫除 1B 安全批刚统一过超时分型）。剩下三处是真重复：

| 重复项 | 合并方案 + 预计省行数 | 风险 |
|---|---|---|
| **网易云 API fetch ×7**：app.js:4641、8568、8577、8804、8848、8906、screens.js:2920（其中 `/search?keywords=` 拼串 4 次） | 抽 `neteaseGet(path,params)`。**省 ≈20 行** | 低（听歌功能，与聊天/存档无关） |
| **Supabase REST 包装 ×2**：scripts/cc-ledger-replay.mjs:32-39、cc-ledger-stop.mjs:47-70 | 进 `scripts/cc-common.mjs`（与第一节末条同批）。**省 ≈15 行** | 高（账本写入，见 Top10） |
| **代码块围栏剥离 ×9**：engine.js:647（`extractJSON` 内，规范实现）、engine.js:2121、2208、app.js:6735、study.js:295、dreamjournal.js:88、read.js:87/149/175 | engine.js:645 的 `extractJSON` 已经是全站规范（含 `repairJSON` 截断修复）；另 8 处在自己手搓 `replace(/```/g,"")`，其中 dreamjournal.js:87-91 完整重造了一个「找 `{`…`}` 下标再 parse」的迷你版。改调 `extractJSON`。**省 ≈18 行**，并让这 8 处白捡截断修复能力 | 中（解析行为会变**更强**：原本 parse 失败返回 null 的场景可能变成成功返回对象，需逐处确认调用方能接住） |

# 六、同逻辑多实现

| 重复项 | 合并方案 + 预计省行数 | 风险 |
|---|---|---|
| **「M月D日」时间格式化 ×5 派系共 ~25 处定义**：`fmtDay`(desire.js:359) 与 `fmtD`(screens.js:2032) **函数体逐字节相同、只是一个写成 function 一个写成箭头**；`fmtDate`(tarot.js:34) 与 `fmtFull`(components.js:4378) **函数体逐字节相同**；另有 fmtHM(components.js:4379)、fmtT(components.js:2899/6157)、fmtClock(engine.js:2861)、fmtClockShort(screens.js:6176)、fmtStamp/fmtStampAI(engine.js:2864/2873)、ymd(weekly.js:93)、ymdStr(memo.js:30)、dayKey(dreamjournal.js:19)、fmtD(screens.js:5099)、fmtD(screens.js:2032)、fmtDate(pomodoro.js:19)、fmtD(capsule.js:14)、fmtMD(phone.js:57) | 归一为 core.js 一族 `fmtMD/fmtHM/fmtMDHM/fmtYMD/fmtDayKey`。**省 ≈30 行** | 中（`fmtStampAI` 进 prompt、`dayKey` 进存储键——这两个**不能动**，只合纯展示用的） |
| **数字缩写 `fmtNum` ×4 语义全不同**：fanfic.js:646（万→`w`/千→`k`）、screens.js:1001（万→`万`）、ledger.js:68（两位小数金额）、components.js:612（金额+`w`） | ⚠**看着同名其实四个函数**。建议**不合并**，改名区分（`fmtCount`/`fmtCountZh`/`fmtMoney`），避免下一个人误用。**省 0 行，防一次事故** | 中 |
| **`clamp` ×2 值域不同**：desire-drive-shadow.js:14（0-100）、somatic-core.js:41（0-1） | 同名不同域，盲合会把体征数据放大 100 倍。合成 `clamp(v,lo,hi)` 显式传域。**省 1 行，防一次事故** | 中 |
| **`dayKey`/日切算法 ×4**：schedule-clock.js:16（带设备时区偏移）、inner-life-c-sleep-core.js:30(`shiftDayKey`)、dreamjournal.js:19、weekly.js:93 | schedule-clock 版考虑了角色时区，其余没有。**先定语义**再合。**省 ≈10 行** | 中（日切错一天，日记/周刊/睡眠都会串期） |
| **生成器 try/catch/finally 样板 ×15**：app.js 内 `… return true } catch(e){ toast("失败："+e.message); return false } finally { setGen(g=>({...g,X:false})) }`，实测 8 处连续 5 行逐字节相同（7034/8043/8076/8105/8353/8409/8472/8957），`setGen` 全文 68 处 | 抽 `withGen(flag, fn)` 高阶包装。**省 ≈45 行** | 高（app.js 主管线，每处的 finally 关的是不同 flag，错一个就永久转圈） |
| **存储读写旁路 21 个键**：绕过 `loadJSON/saveJSON` 直连 localStorage（toy.js:8/12、components.js:24/753/862/1110/1355、screens.js:1030-1394 论坛游标群、app.js:1345/1409/530、cloud.js:90/92/112/114 等） | 已在《1B世代工程施工图纸-2026-08-14》§1.2-B 立为 1C 收编名单并给了逐条清单。**本单不重复出方案**，只标注：这是「同逻辑多实现」里体量最大的一项（**≈40 处调用点**） | 高（存档路径，按 1B/1C 阶梯走） |

---

# Top 10 优先榜（按 省行数 ÷ 风险 排序）

风险权重取 低=1／中=2／高=4。

| # | 项目 | 省行 | 风险 | 分值 | 位置 |
|---|---|---|---|---|---|
| 1 | 影子模块 IDB 样板工厂（openDB×15 + rq×14 + done×16 + clearAll×13 + 裁剪块×6） | ≈194 | 中 | **97** | 20 个 `js/*shadow*.js` + memory-sync/events |
| 2 | 设置行 `SettingRow` + 网易云 `neteaseGet` + 周刊 regen 参数化 | ≈64 | 低 | **64** | components/app/screens/weekly |
| 3 | 剧场 `TheatreSetup` kit（Setup×3 + shuffle + AC/NAC + loadSaves + cancelLP） | ≈115 | 中 | **58** | debate/dream/tarot |
| 4 | `report(n)` 诊断信封抽取（×17） | ≈70 | 中 | **35** | 同 #1 那批文件 |
| 5 | 线下双胞胎共享 hook（saveCustomStyle 51 + 发图/记事 21 + taste 18 + effect 12） | ≈120 | 高 | **30** | components.js:5337-6107 |
| 6 | 死副本与半途抽取清理（注释版 Sheet 16 行 + pAvatar 内联 2 处 8 行） | ≈24 | 低 | **24** | screens.js:5473、games.js:574/1369 |
| 7 | `scripts/cc-common.mjs`（resolveYanqiu×4 等，**排除 cc-ledger-stop.mjs 那半**） | ≈45 | 中 | **23** | scripts/cc-*.mjs |
| 8 | 微工具入 core.js（shuffle×5、AC/NAC×11、pad2×6、text×3、cancelLP×3） | ≈22 | 低 | **22** | 9 个模块 |
| 9 | 时间格式化归一（只合纯展示用，排除 fmtStampAI/dayKey） | ≈30 | 中 | **15** | core.js + 12 个模块 |
| 10 | 轮询 `usePoller`/`useClock`（**排除 app.js 的 CC 泵与找回泵**） | ≈30 | 中 | **15** | 6 文件 18 处 |

> 若 Lisa 想「先要一场零风险的热身」：#2 + #6 + #8 三条全是低风险，合计 ≈110 行，互不相干、可分三个小 PR，任一条单独回滚都不影响其他。

**排在榜外但值得单独说的**：`engine.js` IDB 工厂（省 ≈70，高风险，分值 18）与 `withGen` 包装（省 ≈45，高风险，分值 11）——收益不小但都压在 saves/主管线上，建议**等 1B/1C 世代工程落地后再动**，那时 `idbTxt*` 本来就要退役重写，一次动刀两件事。

## 高风险项的回归验证点（合并后必须手工测这些）

| 高风险项 | 一行回归验证点 |
|---|---|
| 线下双胞胎共享 hook（Top10 #10） | 单聊线下与群线下各跑一场：新建自定义预设→选中→发图带描述→写场记→退出重进，确认预设仍在 `x_offlineStyles`、场记与图都在，且**群线下 maxTokens 默认仍是 3200 不是 4000** |
| `engine.js` IDB 工厂（`idbTxt*`） | 存满一次线下长记录触发 quota，确认 WAL 读回核验仍拦住、开机保险箱把那一版补回来（1A 协议不能被工厂化打断） |
| `ensureOwner` 归一 | A 账号写入影子观测→登出→B 账号登入，确认 B 在欲望盒/诊断页**看不到** A 的任何一行 |
| `clearAll` 工厂化 | 走一次完整 signOut，逐库确认 cloud.js:196-199 那串 clearAll 全部真的清空（漏一个就是跨账号泄漏） |
| `cc-ledger-stop.mjs` 的 request/JSONL 抽取 | 真跑一轮 mark：一次成功轮 + 一次故意逐字验真失败轮，确认失败样本仍落候选箱、**不会改引文碰运气重投** |
| `withGen` 高阶包装（榜外） | 逐个触发 15 处生成入口各失败一次（断网），确认对应转圈状态都归位、没有一处永久 loading |
| 气泡头 `BubbleHead` 合并 | 单聊/群聊/线下三种气泡各滚 200 条，确认头像/名字/时间戳都对位，且 React 列表 key 无警告 |
| 路由 props 包展开 | 逐个进账本/备忘/第三个小程序屏，确认都能正常渲染并返回（白屏即回滚） |
| `extractJSON` 收编 8 处围栏剥离 | 让模型故意输出被截断的 JSON，确认 8 处调用方都能接住「原本 null、现在是对象」的新情况，尤其 read.js 的段落解析 |
| 保留期常量统一命名 | 确认 personality 仍是 90 天、memory-correction 仍是 30 天（诊断页样本数不应突然掉一截） |

## 总账

- 六类逐条相加 ≈1,190 行，扣掉条目间的重叠（线下 hook 与它的两条子项、scripts 与 Stop hook 那半、剧场 kit 与其微工具）后，**去重叠净空间 ≈1,050-1,100 行**，占扫描范围 50,174 行的 ~2.2%。按风险分：**低 ≈150 行、中 ≈650 行、高 ≈280 行**。
- 机械扫描测得的**纯样板浪费 ≈898 行**与人工核实的合并空间基本吻合，说明重复主要是**样板型**（存储层 + 影子群 + 剧场类），不是逻辑分叉型。
- 最大的一块（影子群 ≈264 行）与大扫除第五刀「shadowObserve+shadowStore 一体」的判断一致，本单为它补了逐处 file:line 与语义差异清单。

---

## 附录 A · 扫描可复现

```
node dupscan.mjs     # 函数级：精确指纹 + Jaccard≥0.6
node blockdup.mjs 4  # 跨文件极大相同行块（≥4 有效行）
```
两个脚本只读仓库、输出到临时目录，未写入仓库。排除项：`test/`、`tests/`、`vendor/`、`supabase/`、纯数据大段（塔罗 MAJORS/SUITS/RANKS、词表、歌词）。

## 附录 B · 顺手撞见的非重复问题（按本单边界，只提一句不展开）

1. `screens.js:5473-5488` 的注释版组件初稿留在现行版上方——不是 bug，但属于「注释当版本管理」，git 已有历史，建议删。
2. `games.js` 的 `pByName` 三个语义变体中，精确匹配版（578/1373）遇到模型把玩家名写成「阿屿（狼人）」时会找不到人，模糊版（1642 起）才兜得住——**同一份游戏日志在不同游戏里健壮性不同**，建议按模糊版统一（属正确性，另派）。
3. `fmtNum` 四个同名不同义函数已在第六节标注，若哪天有人跨模块复制粘贴会出金额错误。

---

## 施工记录

### 言秋 · 2026-08-18 · Top10 #7「scripts/cc-common.mjs」重审(报告审的是旧版,当晚钩子做了大手术)
**做了什么**:按 8/18 现状复核四个脚本(cc-ledger-stop / cc-ledger-replay / cc-auto-memory-context / cc-shared-continuity)。
**发现**:8/17-18 三刀之后(saves 点菜、char_id 本地缓存+4xx 清缓存、Stop hook 改为只落票+身份闸+竞速重读),`cc-ledger-stop.mjs` 里的 `resolveYanqiu/request/loadEnv` 已与另外三个脚本**不同体**(带缓存、带兜底、带本地 .env 优先),报告估的"共用 4 处 resolveYanqiu 省 60 行"在新版上不成立。剩余真重复只在三个小脚本:各一份 `loadEnv/envFile` + 各一份身份判定,合计约 30 行。
**定性变更**:#7 **降级**——只允许合并三个小脚本(replay/auto-memory-context/continuity)的 loadEnv 与 resolveYanqiu 进 `scripts/cc-common.mjs`;`cc-ledger-stop.mjs` **保持零外部依赖**(它每轮必跑、30s 硬预算,8/14 断流案的教训是钩子对外部文件依赖越少越好)。收益 ≈30 行,风险低→留作热身刀,择日随 #2/#6/#8 一起。
**验证**:`node scripts/check-runtime-copies.mjs` 11/11 通过;三脚本 `node --check` 通过。
