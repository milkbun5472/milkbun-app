# 记忆可见性(known_by)设计稿 · 2026-08-18

> 给 Codex / GPT 讨论用。**这是提案,不是工单**,里面有几处我自己也没想清楚,标了「待议」。
> 灵感来自 liveware-tavern 的 continuity 模型(AGPL,只取概念、代码自写),但问题是我们自己的。

## 0. 一句话

**现在 `charIds` 一个字段同时承担了「这件事关于谁」和「谁可以知道这件事」两个语义,
而这两件事经常不是一回事。** 提案是把后者拆成 `knownBy`。

---

## 1. 现状

### 1.1 数据

记忆条目(`x_memories`,`addMemEntry`):

```js
{ id, text, charIds: [], source, ts, pinned, open, v, a, archived, surfaceState }
```

### 1.2 检索(engine.js `retrieveMemories`)

```js
const list = (lib || []).filter(e =>
  e && e.text && !e.archived && (e.surfaceState || "active") === "active" &&
  (!e.charIds || e.charIds.length === 0 || e.charIds.includes(charId)));
```

也就是说当前的可见性规则只有两条:

- `charIds` 为空 → **所有角色都能召回**
- `charIds` 非空 → **只有名单内的角色能召回**

### 1.3 群聊那条隐私规矩(app.js,群聊 system)

```
每个成员和用户是什么关系【只有该成员本人知道】——别的成员并不知道
TA 和用户是不是对象、什么关系,除非那成员【在群里自己说了出来】。
绝不许一个成员知道、提及、或据此反应(吃醋/打趣/拆穿)另一个成员和用户的私密关系。
```

**这是写在提示词里的规矩,不是数据约束。** 它靠模型自觉,而且只覆盖「关系」这一类。

---

## 2. 问题:一个字段扛了两个语义

| 场景 | 这件事关于谁 | 谁该知道 | 现在会怎样 |
|---|---|---|---|
| A 私下跟用户说了他在吃药 | A | 只有 A | ✅ `charIds:[A]`,正确 |
| 用户告诉 A「B 最近失恋了」 | B(和 A) | A 知道,**B 不知道用户知道** | ❌ 只能填 `[A]` 或 `[A,B]`,两个都不对 |
| 群里 A 当众说了自己辞职 | A | **在场的所有人** | ❌ 填 `[A]` 则别人召不回;填全员则不在场的人也知道 |
| 用户单独跟 A 吐槽 C | C(和 A) | 只有 A | ❌ 填 `[C]` 会让 C 召回到「用户在背后说我」 |
| 用户自己的事(换工作) | 用户 | 谁在场谁知道 | ❌ 现在多半是空 charIds = 全员皆知 |

**第 4 行是最难受的一种:背后说的话会被当事人召回。** 现在只能靠人工不填 charIds 或者事后删。

---

## 3. 提案

### 3.1 字段

给记忆条目加一个字段,**不动 `charIds` 的含义**:

```js
{
  charIds: ["c_a", "c_c"],   // 这件事【关于谁】—— 检索相关性、时间线归属继续用它
  knownBy: ["c_a"],          // 谁【知道】这件事 —— 只有它决定可见性
}
```

- `knownBy` 缺省(`undefined`)= 沿用旧行为(见 §5 迁移),不是「谁都不知道」
- `knownBy: []` = **只有用户知道**(角色一个都不知道)
- 用户永远隐含在 `knownBy` 里,不占位

### 3.2 检索改一行

```js
const canSee = e => {
  if (!Array.isArray(e.knownBy)) return !e.charIds || !e.charIds.length || e.charIds.includes(charId); // 旧逻辑
  return e.knownBy.includes(charId);
};
```

**关键:可见性判断放在召回层,不放在提示词层。** 角色的上下文里【根本不出现】他不该知道的事,
而不是出现了再叮嘱模型别提——后者永远有概率漏。

### 3.3 谁来写 knownBy

三个来源,按可靠度排:

1. **在场即知道(代码,免费,最可靠)**
   - 单聊 A 产生的记忆 → `knownBy: [A]`
   - 群聊产生的记忆 → `knownBy: [当时在场的成员]`
   - 这一条能覆盖绝大多数,而且完全不需要模型参与。
2. **用户手动改**:记忆库编辑里给一排头像勾选,谁知道点亮谁。
3. **模型申报(待议)**:让提取记忆时顺便给 `knownBy`。
   我倾向**不做**——它会退化成又一个「填字段」的负担,而且第 1 条已经够准。

### 3.4 「说出去」这个动作

真正难的不是初始值,是**传播**:A 知道的事,后来被用户告诉了 B。

提案:在协议里加一个极轻的字段,**只在角色主动说出自己的秘密时**触发:

```json
"disclose": ["记忆里的那件事的 id 或原话片段"]
```

匹配到就把说话人所在场的其他人加进那条记忆的 `knownBy`。

**待议:** 这个字段值不值得加?我的顾虑是它又是一个「元字段」,而我们刚花了好几版
把 thought 从「汇报/规划」拽回「正在想」。也许更稳的做法是:**只做代码侧的在场推断,
不让模型碰传播**——用户在群里说了什么,群里的人自然就在 `knownBy` 里了。

---

## 4. 与现有系统的关系

- **「Ta 眼里」(`js/gaze.js`)**:印象本来就是每角色一份,天然隔离,不受影响。
- **群聊隐私铁律**:数据侧生效后,那段提示词可以从「绝不许知道」降级为兜底,
  **但先别删**——两层保险,等观察一段时间再说。
- **世界书**:是公共设定,不进 `knownBy` 体系。
- **小剧场**:沙箱,不读主线记忆,不受影响。
- **向量检索**:`knownBy` 过滤发生在打分之前还是之后?**待议**。
  之前更省事,之后更省算力但要小心 topK 被不可见条目占满(和当年置顶挤掉相关召回是同一类坑)。

---

## 5. 迁移

存量记忆没有 `knownBy`。三个选项:

| 方案 | 做法 | 风险 |
|---|---|---|
| A. 惰性 | `knownBy` 缺省就走旧逻辑,只有新记忆带它 | 老记忆永远不受保护,两套逻辑长期并存 |
| B. 一次性推断 | 按 `charIds` 回填:非空 → 照抄;空 → 全员 | 「空 = 全员」正是现在漏得最多的地方,等于把错误固化 |
| C. 按来源回填 | 有 `sessionId`/来源通道的,按当时在场的人回填;推不出来的留空走旧逻辑 | 最准,但要看历史数据里还留没留下通道信息 |

**我倾向 C 兜底 A,但需要先查清楚存量记忆里还能不能反推出通道。待议。**

---

## 6. 成本

- 检索:一次数组 includes,可忽略。
- 写入:在场推断是纯代码,零 API。
- 存储:每条多一个短数组。
- **真正的成本是 UI**:记忆库要能看见和编辑「谁知道」,否则用户改不了错。

---

## 7. 我想听到的意见

1. **§3.4 的 `disclose` 字段该不该做?** 还是只做代码侧在场推断更稳?
2. **§5 迁移选哪个?** 有没有比「空 charIds = 全员」更好的存量推断?
3. **§4 的过滤时机**:打分前过滤 vs 打分后过滤,哪个更不容易踩 topK 被占满的坑?
4. **有没有比 `knownBy` 更好的建模?** 比如按「场合」建模(这条记忆属于哪个场合,
   场合有参与者),而不是给每条记忆挂一份名单——场合模型可能更省、也更自然,
   但改动更大。
5. **值不值得做?** 老实说,现在的漏在日常单聊里几乎察觉不到,主要在群聊和
   「用户跟 A 说 B 的事」这两种场景。如果你们认为收益撑不起复杂度,我接受。
