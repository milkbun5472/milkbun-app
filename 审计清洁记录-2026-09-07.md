# 台账复核与本轮清洁

起点：`7ad7eb5d`，工作区干净。Lisa 本轮要求审计、清洁；按台账「一次一座山」，本轮选择云备份公共机制，并修复台账点名的跨午夜测试。没有修改用户存档。

## 逐项复核

| 台账项 | 当前证据与处理 |
|---|---|
| 自创顶栏 | 保留。涉及已调过的安全区，需一次一页、真机复核，本轮未改布局。 |
| PUSH_WHY 重复 | 具体原因已由 `pushWhy` 统一：自动备份 toast 调用它，设置和通讯录读 `pushState().why`。旧描述撤下；发现横幅标题不足一天也报一整天，作为具体剩余问题登记。 |
| 成功状态公共机制 | 手动 push 已用 `markSynced`，autoPush/autoPull 仍各自写成功戳和清警告。本轮将三条成功路径统一到 `markSynced`，保留原本成功/失败门槛。 |
| 超时计时器 | `Promise.race` 赢了也不会取消另一个计时器。补 `finally(clearTimeout)`；不改变 45 秒上限，不宣称取消底层网络请求。 |
| maxTokens | 不含 trpg，数字字面量低于 65535 为 19 文件 146 处。更新台账统计；本轮没有改模型调用。 |
| app.js 大文件 | 保留；本轮业务代码没有增加到 app.js。 |
| 跨午夜测试 | 固定测试时钟，增加午夜前后行为断言。产品日期算法保持原样。 |

## 旧审计报告的抽查

`AUDIT-REPORT-2026-09-04.md` 的结论不能直接当作现状：成长准则 `GROWTH_RULE` 已在 v63.63 删除，源码留有说明；`_normalTaskFull` 回滚串仍存在，留待独立审查发送链与引用后处理。

全量测试还列出 15 个零引用候选定义（包括旧手机详情组件、图标与辅助函数）。零引用扫描是线索，可能涉及动态访问和跨文件使用；本轮未据此盲删。

## 验证

- 改动前隔离副本：4490 项，4466 通过、24 失败。
- 改动后：4494 项，4470 通过、24 失败。新增四项行为测试全部通过；失败名称集合与改动前一致。
- 新测试覆盖：自动备份失败后重试、恢复 apply 失败/成功、thenable 成功/拒绝/挂起的计时器释放、跨午夜时间显示。
- 测试云客户端和存储均为内存替身，没有读写真实云存档。
- 全量测试未全绿；既有失败已入台账，不等同于都只是过时测试。
- 没有操作 Lisa 的真机；这里的验证是源码、完整脚本沙箱与自动测试，不冒称真机验收。

本轮收口范围是云备份成功状态与计时器、测试时钟及台账复核，不是全库重构完成。

## 第二轮：公共段后面又长出的三套背景

Lisa 继续点名「差不多的东西写了几版，修一处忘另一处」。本轮以 `35ccc37b` 为起点，集中收口三条群路，未改页面布局。

发现：`groupNowSegs` 原本收了心情、好感、年龄、行程等基础段，但后加的成长、情绪底色、睡眠、居住地、随身物、情侣档案又在群文字和群通话各写一遍；群线下另外读字段、拼文字。已有公共函数没有覆盖后加层，就是这次重复的落点。

改为两层：

- `groupBackgroundFor(c)` 统一六层读取，配角返回空对象。群线下六张成员表也从同一次读取产生。
- `groupBackgroundSegments(c, background, userName, opts)` 统一六段格式与档案隐私围栏。群文字与群通话经 `groupNowSegs` 使用；群线下按成员传入同一个格式函数。

保留差异：线下成长描述的叙事措辞由 `narrative: true` 指定；文字群额外的歌单和跨群连续性不搬进电话；即时穿着/动作仍受互通开关控制。单人线上/线下/单人通话继续由 `ctxFor`、`buildBundle` 提供背景，本轮没有把群用短段替换进单人提示词。

验证证据：用重构前保存的真实三条 `memberDesc` 和现在的实现分别运行同一份测试数据，两位主角色加一名配角，三份输出逐字相等。结果哈希固定进 `test/group-background-shared.test.js`。新增六项行为测试覆盖输出等价、私有档案归属、配角排除、空字段、六张线下成员表、闭群即时状态开关。

相关旧测试从「必须在两处写一段」改成检查公共读取/拼接接入，保留人设、背景、隐私约束；其中一条原已失败的测试是把拼接字段顺序钉死，改为检查各字段确实进入成员段。全量 4500 项，4477 通过、23 失败，失败集合为上轮基线的子集，无新增失败。语法和启动冒烟通过。没有调用真实模型或操作用户存档，也没有真机验收。

## 第三轮：日期公共方法与台账清洁

起点 `8c9acfd6`。日历、经期、行程的日期生成和解析合并到 `ScheduleClock`：新增 `formatDayParts`、`deviceDayKey`、`parseDayKey`，现有 `dayKey` 与 `shiftDayKey` 也共用格式方法。八个业务入口只转交参数，移除固定 24 小时位移和日期模块缺席时的另一套算法。实际脚本顺序已先加载 `schedule-clock.js`。

旧存档格式明确保留：`calKey`、`pDK` 将 `legacy` 参数传为 `true`，生成不补零键；`calPadKey`、行程日期用补零键。没有转换或重写已有存档。两种格式均可按设备当地日期解析；角色时区逻辑保持原有实现。

测试覆盖旧日历写入/读取、三地时区（温尼伯、上海、悉尼）、夏令时切换、闰年、跨年、真实装载顺序，以及日历已有分支渲染与坐标回归。定向 59 项全过；全量 4506 项、4483 通过、23 项既有失败，失败集合与第二轮一致。页面结构、安全区和主屏布局未改，没有真机验收。

台账撤下完成的日期条目，旧失败数直接改为当前数字，删除尾部已完成事项的补充段。施工规则直接登记现行公共入口；历史验证证据集中在本记录中。

## 第四轮：扩展重复扫描与备份显示收口

起点 `d9aa3c5e`。扫描范围为 `js/` 顶层的 114 个 JS 文件，共 104032 行；不是整个仓库全部语言的行数。算法去除空行、单行注释和首尾空白，再比较连续六行，至少 220 字符；发现 209 个重复窗口。窗口互相重叠，不能等同于 209 个独立缺陷，也不会发现所有改过变量名的近似重复。

复核结果：

| 位置 | 现状 |
|---|---|
| `OfflineMode` / `GroupOfflineMode` | 文风编辑、导入、往期记录、照片入口有长段复制，已入台账。 |
| `maybeAutoExtractGroup` / `maybeAutoExtractGroupOffline` | 归属过滤、证据字段与群来源存在实际分叉，已入台账；本轮未改记忆写入。 |
| `summarizeOffline` / `summarizeOfflineGroup` | 存在相同输出尾段，需按单人/群语境再核对，未认定可以整体替换。 |
| `theater.js` / `trpg.js` | 账本规则相似；遵守 trpg 专属维护边界，未修改。 |
| 备份状态三处显示 | 本轮已统一到 `Cloud.pushState()` 返回的显示字段。 |

备份显示：公共层生成 `needsAttention`、`headline`、`summary`、`detail`；设置首页、云同步详情和聊天横幅直接使用。删除页面里的重复判断、时间计算以及过期病史注释。近期备份成功后发生失败，标题为「这次备份未成功」；达到 24 小时才按天数提示。保持真实备份读写和既有告警阈值不变。

行为测试通过真实 Cloud 脚本和内存存储验证未备份、近期成功、近期失败、24 小时边界、恢复成功；直接运行横幅渲染段验证隐藏、标题、固定层级与点击入口。定向 26 项通过，全量 4508 项、4485 通过、23 项既有失败，失败集合与第三轮一致。页面尺寸和安全区未改；没有真机验收。

## 第五轮：群记忆候选落库收口

起点 `47c4becf`。群线上和群线下迁入 `saveExtractedGroupMemories`，删除两段候选写入实现；统一 NPC 归属过滤、knownBy、groupId、证据 ID、重复判定与开环资格。线上证据归一化补传真实消息窗口，已算出的证据 ID 现在随条目保存；线下检查实际使用的后台模型，而非主模型。

保留边界：两处的消息来源、抽取计数与标签不改；线下原有逐字证据关环流程仍在原位置，本轮不将它扩展到线上。不改已有记忆存档。公共写入入口再次检查互通开关。

新增行为测试直接执行 App 公共写入函数，使用实际证据归一化与开环资格模块；覆盖线上 mid、线下 id、NPC-only 排除、混合归属、用户事实回落、重复候选、群来源、场景标签和封闭群拒写。旧结构测试改查公共调用链，不再要求保留已删除的注释。未调用真实模型、未操作用户存档，未做真机验收。

全量 4512 项，4489 通过、23 项既有失败，失败集合与第四轮完全一致。语法检查、启动冒烟和差异空白检查通过。

## 第六轮：线下文风管理与设置区共用

起点 `5149761d`。单人和群聊线下迁入 `useOfflineCustomStyles` 与 `OfflineCustomStyleSection`，移除两处重复的文风草稿管理、增删改、文件导入和设置区实现。沿用原有存档字段及界面层级、尺寸；开局表单、照片和往期记录暂不合并。未改主屏、顶底栏或滚动容器。

修复两处编辑入口只填草稿却未打开编辑器的问题：开局打开原弹层，设置页打开内联编辑。保存或导入失败不再先更新选择、清空草稿或报告成功。用户现有文风与聊天记录不迁移。

新增七项行为测试覆盖新建、原 key 编辑、失败保留草稿、删除成败、导入成败、草稿隔离、设置组件层级及按钮回调。全量 4519 项，4496 通过、23 项既有失败，失败集合与第五轮一致。未调用真实模型、未操作用户存档，未做真机验收。本轮只更新家里，不部署公共版。

## 第七轮：尾账全绿

补漏工单的逐项成因、修复及验证见 `给Codex的补漏工单-2026-09-07晚.md`。以 `8caf2ad1` 交付后全量 4521 项通过，清理 16 个退役定义，玻璃材质接回公共配方，现用朋友圈接回相对时间函数。台账中的旧失败基线已撤下。

## 第八轮：线下真实照片发送链收口

起点 `8caf2ad1`。单人/群聊线下两份入库、相册登记、消息发送迁入 `useOfflinePhotoSend`；`offline` / `group-offline` 来源显式传入，各自会话回调和草稿仍由页面持有。照片选择、合照/自拍能力、消息形状、布局和安全区不变；不调整线上照片流程。

公共入口同步占锁，覆盖图片入库和发送回调整个异步过程，防止快速连点重复发送；抛错时通过 App 真实注册的 `window.__toast` 提示，保留草稿并释放锁。成功才清空并关闭。保留 `imgToVault` 原有返回与降级契约，不迁移或删除任何存档。

删除 screens.js 中注释藏着的旧版 MemoryRepairConflictSheet，仅保留有缺失记忆保护的现行实现；历史可从 Git 恢复，不把旧实现当审阅档案嵌在产品脚本里。

新增八项回归覆盖真实写入字段与来源、连点/重渲染、异步回调、失败重试、入口守卫、实例隔离、两路公共接线及退休草稿不回流。定向 42 项通过；最终全量 4529 项全部通过，语法、缓存指纹与 git diff --check 通过。独立临时浏览器配置下主屏启动无运行时异常，没有真机验收、没有调用真实模型或操作 Lisa 的存档。仅修改家里，不触发公共版部署。

## 第九轮：线下选图表单收口

起点 `23d5c522`。单人、群聊两份选图／预览／配字／发送 UI 迁入 `OfflinePhotoPicker`，旧内联实现全部移除。各页继续持有文件 ref、图片、说明、发送状态与回调；单人合照／自拍、群合影仍各自保留。未改顶底栏、安全区、滚动容器或存档，表单控件原有尺寸样式与缩图参数沿用。

新增三项测试检查两路接线、文件选择与缩放、输入清空、说明和发送回调、预览、禁用状态及实例隔离；旧测试由数两份按钮源码改为检查两处使用同一实现。定向 11 项和最终全量 4539 项通过，语法、指纹和 diff 检查通过。未调用真实模型，未操作用户数据；本轮未做真机视觉验收。仅推家里，公共版不部署。

## 第十轮：往期列表收口

起点 `5de6280d`。两处往期列表迁入 `OfflinePastSessions`，结束筛选只算一次，排序仅作用于新数组；日期、摘要截取、段数回退、空态和控件样式沿用。选择回调仍由各页关闭自己的列表并打开原记录。群聊详情的便签和成员状态入口未动，不迁移存档，不改顶底栏或滚动结构。

新增三项测试对应实际单人/群聊写入字段，覆盖结束筛选、排序不改原数据、原对象回传、摘要及空态回退、会话隔离和滚动样式。全量 4542 项通过；本轮未做真机视觉验收，未操作真实数据或调用模型。只更新家里。

## 第十一轮：往期详情收口

起点 `03393e00`。两处往期详情迁入 `OfflineSessionReader`，返回、删除、标题、总结、只读卡片统一；群聊通过 showNotes 与 onOpenState 显式保留便签与状态入口，单人传 char、群聊传 members。保留原 DOM 层级、尺寸、安全区底纹及滚动样式，不更改存档和删除持久化链。

新增三项行为回归覆盖身份传递、只读性、原 ID/索引删除、关闭顺序、便签差异和层级；旧断言由要求两份源码改为检查两处使用公共实现。全量 4545 项通过，语法、指纹、diff 检查通过；未做真机视觉验收，未触碰用户数据。仅更新家里。

## 第十二轮：开局文风表单收口

起点 `18c5cbb0`。开局预设选择/原文预览迁入 `OfflineSetupStyleSection`，编辑框迁入 `OfflineSetupStyleEditor`；两页旧内联实现移除，只传各自已有的 styleEditor。删除多余解构，编辑草稿、导入、保存仍走公共管理器。设置页紧凑编辑器保留不同尺寸；开场白、入场参数、单人房间提示不动。选择区用 Fragment 保留原 DOM 层级。

定向 13 项、全量 4546 项通过，覆盖真实保存字段、编辑不新增、失败保留、导入、取消和实例隔离；新增开局 UI 回调至管理器的编辑保存测试。语法、指纹和 diff 检查通过；未做真机视觉验收，未操作真实存档或调用模型。台账保留这次发现的开局页往期卡片双实现，不将其与已共用的聊天中往期选择列表混为一谈。只更新家里。

## 第十三轮：开局往期卡片收口

起点 `95b3ac77`。两处开局往期卡片迁入 `OfflineSetupHistory`，筛选、摘要回退、日期、点开和删除按钮只留一份；保留原顺序、样式和删除回调参数，原数组不修改。聊天中往期列表的倒序展示仍独立，不把两种已有行为抹平。

新增三项测试覆盖结束筛选、原对象/原索引、重复与缺失 ID、空态、权限隐藏和会话隔离。全量 4549 项通过，语法/指纹/diff 检查通过；未做真机视觉验收，未调用模型或操作真实记录。台账中此前列出的线下重复块已逐项收口，删除该行；不代表整库重复已扫完。只更新家里。

## 第十四轮：搜歌请求业务链收口

起点 `eab2d0c4`。跨文件查看模型配置、语音及音乐请求后，选中五处重复的网易云首条搜索链：聊天换歌、接受邀请、情侣唱片、生成歌单、配乐。统一复用已有 neteaseSearchOne；四处独立 fetch/JSON/首项提取移除。throwOnError 保留聊天/唱片的错误提示，默认失败返回 null 保留批量跳过，cacheBust:false 保留邀请原 URL。搜索词、歌曲对象、重试次数、存档 ID 与后续入库/播放策略不变；不改 trpg.js，不新增模型调用。

新增三项回归覆盖五路接线、编码、时间参数、原始字段透传、空结果、网络/解析异常及不自动重试。全量 4552 项通过，语法/指纹/diff 检查通过；使用虚构接口响应，不调用实际音乐服务或模型。这里只完成这块已确认的重复，不宣称完成全库审计。只更新家里。

## 第十五轮：歌曲资料映射收口

起点 `90e91a0d`；施工期间另一窗口提交 `5f1e3f64`（房间选择修改），同时带入本轮 app.js 调用点，本轮保留其改动并补齐依赖。聊天/邀请/唱片/歌单/配乐及一起听 toRes 共用 engine.js 的 neteaseTrackInfo。preferShort 显式保留页面 ar/al 与业务 artists/album 的优先级；空封面与歌手回退、各业务 ID 和备注不变。旧重复映射移除。

新增三项回归覆盖两种字段、同时存在的优先级、空字段、对象不变及调用接线，更新封面 UI 测试检查公共入口。包含另一窗口新测试的全量 4556 项通过；语法/指纹/diff 检查通过，未调用真实接口或模型，未改用户存档。只更新家里，公共不部署。

## 第十六轮：删除目标定位收口并修误删

起点 `f5f0726d`。单人/群聊的整场与消息删除共用 recordIndexForDelete。原四份先按 ID 找第一条，重复 ID 会忽略 UI 传回的原索引；现在索引与 ID 匹配时优先选中该项。失配则按 ID 重找，目标不存在不再盲删旧索引，缺 ID 的旧记录仍可索引定位。持久化、确认、影子观察和侧房隔离不变。

新增三项回归实际检查过滤后的剩余记录，覆盖重复 ID、缺 ID、过期索引、目标消失及四路接线。旧 OOC/持久化测试改为检查公共入口。全量 4559 项通过；语法、指纹、diff 检查通过。仅虚构数组测试，未删除任何真实记录。只更新家里。

## 第十七轮：公共交互弹层收口

起点 `4589787d`。按 Lisa 要求扫描同类层级问题：PromptDialog 仍是 App 内220层，ConfirmDialog 虽已 body portal 但300与图片预览撞层。二者遮罩/挂载/层级迁入 appDialogPortal，唯一 inline zIndex1200，高于扫描到的设置240、大图300、浮控940/999。地图内1200处于 isolation:isolate 的局部容器，未动。Toast 的设置遮挡风险另列台账待触发验证。

保留输入控件、确认/取消回调和点击内容阻止关闭。新增单行/多行输入的回调与共享层回归，旧测试不再用还残存的 Prompt 220 字符串冒充确认层验收。全量4561项通过；语法/指纹/diff检查通过。本轮未完成真机视觉验收，没有操作用户数据。仅更新家里。

## 第十八轮：反馈提示层收口

起点 `e1e83a0e`。Toast 从App内部绝对定位60层改为body portal；APP_OVERLAY_LAYERS统一dialog1200、feedback1210，反馈继续pointer-events:none，不改消息生命周期、内容和动画。公共确认层引用同一层级定义。

独立本机测试页使用完整Toast函数与DOM渲染适配器复现设置portal240，浏览器elementFromPoint检测修复前TOP:SETTINGS、修复后TOP:TOAST（检测临时打开命中，产品保持穿透）。初次夹具截断函数签名已纠正，不作为证据。非真机全App验收；未触碰真实数据。新增空态/挂载/穿透回归，全量4562通过，语法/指纹/diff检查通过。台账撤下该项，只更新家里。

## 第十九轮：同人文模型结果解析收口

起点 `4d9b3675`。作者生成、批量篇目、续写、书评、评论回复、互动叙事六处共用 engine 的 parseJSONLoose；撤下六份本地 extractJSON/repairJSON 链及过时病史注释。正文裸换行与双层字符串解析能力不再只有互动叙事能用。各入口的形状过滤、正文抢救、重试次数、提示词和存档映射不变；未扩大到其他业务解析器。

测试使用真实公共解析函数，覆盖书评换行/楼中楼、回复双包/截断/无效返回及六路接线；两份旧互动叙事测试撤下自造 JSON 桩。全量4565项通过，语法、指纹与diff检查通过。仅虚构响应，没有真实模型请求或用户数据操作；不涉及UI。只更新家里，公开版不动。

## 第二十轮：房间拉黑申请隔离（2026-09-08）

起点 `59617296`。拉黑、用户/角色解除申请、接受拒绝、延迟气泡与生成lane改用发起时chatKey；x_blocks沿用原键结构，主房仍为角色ID，侧房使用ChatRooms.chatKey。普通回复的拉黑结果、发送/拍一拍状态、设置与聊天入口同步接线，防止房间消息正确而状态仍串主房。申请上下文通过现有gateCtx/prompt，只取本房历史，绑定房间设定；已删除房间不退回主房上下文。两处调用预算依规则开满。

申请卡复用聊天40px头像与gap-2排列；用户头像遵循显示开关，卡宽可缩，不动顶底栏或滚动结构。新增虚构数据行为测试验证切房后延迟落点、双向申请结果、主房状态不受影响、旧主键兼容、上下文围栏和头像接线。全量4568通过，语法/diff/指纹检查通过；未真机视觉验收。既有误落主房的历史没有自动迁移或删除，避免误判来源。只更新家里，不动公开版。

## 第二十一轮：房间上下文与通话出口（2026-09-08）

起点 `c6b5a574`。普通房间回复、拉黑申请、单人语音/视频与线上OOC共用roomContextFor/roomPromptFor，按本房历史匹配世界书/召回查询，并统一时间开关表述；主房继续走原ctxFor。追加任务中的跨角色避重内容不再带进侧房，主线关系/心情/共同听歌按认知开关读，印象催更同时校验写回许可。房内穿着/动作/心情随既有x_roomStates独立保存，不因禁止主线写回而丢掉；OOC及其新增准则按chatKey持久化，OOC原文不当角色经历注入。

拨出、回拨、接听保存chatKey及房间快照，语音/视频走同一条通话链；回执和摘要回到原房，正式记忆按memoryCandidate、状态按sharedState/stateMood写回，心声仍仅存本房。sessionId拦住旧通话迟到消息和挂断信号，防止污染新通话。ChatRooms.allowsField作为仅有主线实现的能力名单，提示与执行共用；封住侧房悄悄话/刻唱片/代记/群投递等出口，既有学习游戏混搭与聊天本地能力保留。旧转账/情侣卡的手动回调也校验所在房间，防止开场带入卡再次执行主线动作。

新增六项虚构数据回归，实际执行语音和视频callSend/endCall、异步摘要、允许/禁止写回、迟到回包、OOC与准则读回、本房状态真实写入方；没有真实模型请求、通话或用户存档操作。全量4574项通过，语法/diff/版本检查通过。未改布局，未做真机通话验收；不能据此声称所有App跨页面业务都完成隔离审计。没有追溯删除/迁移旧记录，只更新家里，公开版不动。

## 第二十二轮：解除拉黑过程的角色气泡（2026-09-08）

起点 `9d73642d`。被拉黑后的三种反应、接受申请后的回复、拒绝申请后的回复统一经queueUnblockSpeech写普通assistant消息，撤下system卡片类型和拼接姓名/情绪前缀；申请本身及其处理状态保留原卡。沿用普通聊天头像与气泡，无布局修改；延迟消息继续绑定发起时chatKey。提示词、判定逻辑、调用次数及历史记录不改。

新增两项行为回归覆盖mutter/angry/appeal与accept/decline分支、普通消息字段和切房后的落点。定向18项、全量4576项通过；仅虚构模型响应，未操作真实存档，未真机视觉验收。只更新家里，公开版不动。

## 第二十三轮：主屏组件编辑页全屏定位（2026-09-08）

组件/装饰共用编辑页原为Home内absolute定位，跟随主屏偏移导致顶部露壁纸、底部按钮出屏。沿用天气详情body portal + fixed inset:0层240，保留现有Head、预览、唯一正文滚动与底栏；主屏根节点、100vh、安全区空带不改。更新旧测试中把absolute误认为全屏的断言，验证portal/body与固定视口。定向37、全量4576通过。

独立浏览器夹具从真实源码提取外壳表达式，在带54px偏移的模拟Home外挂载；390x844、320x568均top=0、bottom=视口高度，底部按钮分别830/554、滚动800可达。夹具正文为虚构长表单，非完整App或iPhone真机验收。只更新家里；整框颜色需求待继续。

## 第二十四轮：组件整框自定义底色（2026-09-08）

组件适配器接回公共外观编辑器ground，沿用x_homeWidgetLooks存档，默认清理判据补ground；公共底色行加入原生自选颜色，装饰同步受益。组件不开装饰照片上传链。homeInnerOf统一提供局部主题，GlassCard覆盖自身白底，深浅底翻转文字颜色；预览/桌面外框同时使用已有decorGroundStyle。原样不改主题，边框强调色独立，未动主屏位置、尺寸、安全区和编辑页全屏结构。

三项新增回归覆盖真实写入函数的保留/清理、真实GlassCard背景优先级、深浅字色与两处接线。全量4579项通过；未做完整App真机颜色验收，交Lisa验收。未操作真实存档或模型，公开版及Cloudflare未发布、未创建项目。

## 第二十五轮：照片装饰空槽遮住底图（2026-09-08）

照片框自定义底图已经画在底板，但无照片的占位格仍绘制深色实底，单张框占满整板时表现全黑。共用photo槽绘制在无src且有有效gnd时改透明；已有照片、无底图默认槽、相框边饰均保持。预览和主屏同用HomeDecorItem，无数据迁移和原图操作。

新增真实HomeDecorItem行为回归，以imageRefs与ground.imageRef实际字段验证底图、透明空槽、已有照片和原样。全量4580通过。独立浏览器使用相同真实函数、虚构SVG底图验证解码成功与空槽computed background透明；不是用户原图或iPhone真机验证。只发家里，Cloudflare与Netlify不动。

## 第二十六轮：组件图片底、贴合框与自定义敲击物（2026-09-08）

组件图片底复用装饰上传/图库与背景解析，ground.imageRef写入x_homeWidgetLooks；局部主题把图片传给真实GlassCard，预览与主屏共用。新增组件专用贴合框：零内边距、22px圆角裁切。木鱼tapImageRef独立持久化，可恢复默认；透明图片走resizeImageAlpha，原敲击、连击、五秒清零不变。未改主屏根布局、安全区或编辑页滚动结构。

真实函数回归覆盖图片底覆盖白底、缺图回退、贴合裁切、上传入库/失败保留、敲击图片保存/恢复及连续敲击计数、编辑态不计数。全量4583通过；未做iPhone真机图片与视觉验收，交Lisa试用。无真实用户数据或模型调用；只发家里，不创建Cloudflare项目、不部署公共版。

## 第二十七轮：保存栏纸底延续（2026-09-08）

组件/装饰共用保存栏由平色t.bg2改为公共pageSkin纸底，覆盖按钮周围与底部留白；保留按钮、padding与0.4安全区公式。全量4583通过。独立浏览器使用真实外壳/底栏样式表达式、虚构纸纹与长表单，390x844及320x568验证纸纹存在、按钮不出屏、正文滚动不变；非iPhone真机验收。Lisa本轮授权修好后首次发Cloudflare，另走公开出货线完整检查；Netlify不动。

## 第二十八轮：TA眼里复看回执（2026-09-08）

专门复看通过acceptReview先验十块正文/null，再原子进入既有落库；缺项/错误形状不再被当成“没变化”。review对明确null和相同原文记录各块checks，保留正文/原写入时间/已读状态；聊天applyParsed收到相同原文也记复看，不再计漏答。线上/线下/群聊已有applyParsed入口同获修复，沙盒写入权限不动；其他场景没有新增印象卡写路。

单聊任务收尾不再贬低状态记录，点名复看排在聊天收尾后；线下原本已尾置，未另加重复指令。四项新增真实函数回归覆盖没改时间、错误返回无写入、聊天相同原文、混合改写/未改；全量4587通过。不增加调用、不缩短自动复看冷却、不修改历史时间戳；未用真实角色或模型测试，不保证模型从此每轮遵循。只发家里，两个公共站保持65.66。

## 第二十九轮：TA眼里轻量更新（2026-09-08）

按Lisa新要求取代上一轮的强制点名策略：每轮可记录有依据的小认识、补充或修正，空卡和已有卡不再等轮数；没有新增可省略，不要求复看回执。单聊线上/线下经spec、群聊线上/线下经updateRule共用判据；房间、封闭群、NPC和数字生命原有落库权限保持。通话、穿书、匿名信箱、解梦馆没有新增写回，平行沙盒不扩权。

删除点名队列、漏答计数与自动付费复看入口，不在旧强制要求后补例外。旧存档不迁移、不删除；已有明确回执仍兼容，手动专门复看与原有首次建卡（30条门槛、既有失败上限/冷却）保留。省略不改正文、时间或历史，不伪装成看过。

旧强制队列测试替换为新契约；真实Gaze函数覆盖空/部分/满卡每轮提示、100轮省略零写入、旧漏答存档无催写、十块即时小补充及角色隔离、历史保留。语法、版本指纹、diff检查和全量4571项通过；未调用真实模型，不保证每轮一定产生更新。只发家里，Netlify与Cloudflare不部署。

## 第三十轮：秋秋悬浮球重开归位（2026-09-08）

AssistantDock不再读写旧x_assistDock点位，前台拖动只存组件状态。启动默认右下角；visibilitychange回到前台、BFCache pageshow恢复时取消拖动并收回小球，回默认位置。未发送输入、聊天、设置和旧存档不删除；普通页面内切换不触发visibility重置。根布局、安全区公式、点拖区分与现有拖动边界不动。

三项真实组件函数回归覆盖旧点位不读取、前台拖动/吞点击、重建归位、后台恢复保留输入、BFCache与监听清理；更新旧“跨启动记位置”测试。独立Chrome浏览器使用真实React和AssistantDock、虚构聊天依赖，在390x844与320x568拖到y=30后刷新及模拟回前台，均回默认右下且小窗能点开；非iPhone真机测试。全量4574、语法、指纹与diff检查通过。仅家里发布，公共链接未部署。

## 第三十一轮：单聊逐气泡后台通知试用（2026-09-08）

现有本地通知通道改为单聊文字、表情、语音逐条交付后通知，稳定标签区分房间、回合、气泡；前台和关闭权限时不通知。其他通知生产者保留，群聊、线下及照片/交易卡未扩展，不声称全场景覆盖。无额外服务器、模型调用或保活；系统冻结页面后不能保证收取回复和通知。

SW本地通知使用waitUntil，点击带房间标识；冷启动目标暂存内存等待角色加载，已删除侧房不落回主房。首次未受SW控制时使用注册通知回退，等待后再检查前后台。设置改为试用说明并增加测试按钮，不再保证后台必达。

五项真实脚本/函数测试覆盖开关权限、前台抑制、标签唯一性、异步回退失败、冷启动地址清理、SW生命周期及侧房跳转。独立Chrome真实React渲染SenseConfig，320/390宽度测试按钮不溢出；未触发真实手机通知。全量4579、语法、指纹和diff检查通过。只推家里，不部署两个公共站；手机切去其他App的实际送达交Lisa验收。

## 第三十二轮：通话启动缺失编号函数（2026-09-08）

startCall调用不存在的全局uid，语音与视频在setCall前抛错。两公共站线上bundle与65.66产物哈希一致，均存在此问题，家里亦然。改为启动处用内建日期/随机数生成通话会话编号，不新增外部依赖，原会话隔离判据与所有入口保持。

删除room-call-scope测试中人为补的uid桩，新增主房/侧房/群聊 × 语音/视频六种启动与连续编号不同回归，原迟到结果隔离测试保留。全量4580、语法、指纹与diff检查通过。无真实模型调用、无用户数据变更、无界面/提示词改动。仅家里发布，公共包等待明确deploy授权后按完整出货线同步。

## 第三十三轮：一起听悬浮条折叠（2026-09-08）

MiniPlayer新增收起键，折叠为52px小唱片，点击/键盘展开；展开条仍点正文回播放器，关闭键仍停播。折叠偏好存x_miniFolded，沿用x_miniPos拖动与45层级；展开/resize根据真实尺寸约束已有点位，防止右侧展开出屏。未操作音频、换歌、quote或模型调用。

新增真实组件函数测试验证折叠/展开存档与零音频回调，旧层级测试去掉固定截取长度。独立Chrome真实React在390/320宽度验证52px收起、边缘展开不越界及零播放/跳转回调，全量4581、语法、指纹和diff检查通过。仅家里发布，公共不部署。

## 第三十四轮：提示词B版撤去跨角色比较（2026-09-08）

Lisa授权仅家里：删除crossSameness/crossOffer气泡、心声、帮助表态扫描及两处任务拼接，不留空转函数。共享规则撤掉逐句独特性判据，依据当前人物、关系和处境；允许普通回应，保留反机械三拍与能力边界。共享消费者同步生效，未改人设、记忆、房间权限、能力催用、心声字段、拆泡长度或标点后处理。

旧算法测试退休，替换为无扫描/注入契约与真实V2任务串动态层回归；原心声、双语、收尾及各通道接线检查保留。全量4570、语法、指纹和diff检查通过；测试数量减少来自撤掉旧算法专属测试。无真实模型调用或用户数据操作，自然程度交Lisa实聊验收。公共两站不发布。

## 第三十五轮：后台模型默认跟随主模型（2026-09-08）

Lisa明确默认未选后台线路就跟随主模型，选择后才独立。bgActive统一实现此契约，行程、查手机及既有后台消费者共用；显式选择失效时保持不可用，不静默改走主池。callAI入口新增空配置错误，避免读取null.baseUrl。未修改提示词、世界书、用户数据、自动任务开关/冷却/重试次数，无真实模型调用。

更新旧cheap_required不跟随的测试契约，真实配置表达式覆盖默认跟随、切换主模型、独立选择、失效选择和无模型；真实callAI空配置测试确认网络前明确失败。行程、查手机、开机等32项定向通过，按版本脚本更新后全量4571、语法、指纹和diff检查通过。只推家里，公共两站不发布。

## 第三十六轮：GD 音乐源家里试用（2026-09-08）

Lisa授权仅家里接入。独立编写MusicSource适配，未复制yzjy实现：GD模式使用其网易云曲库搜索/320k播放/封面/歌词，保留网易云曲目ID与旧存档。设置中手动选择，默认仍原接口；Cookie不发送给GD，原接口配置不删除。来源署名与非商业学习限制展示于设置，GD不展示账号红心/云歌单按钮，不执行账号请求；不自动跨源换歌。搜索、角色切歌、邀请、唱片/角色歌单及跑团配乐共用入口，两处歌词共用适配。平行房间权限与模型提示内容不改。

GD请求合并/短缓存、本机五分钟45次保守上限（服务说明50次），刷新保留计数、15秒超时；限流失败中止批量搜索，避免误判搜不到而追加模型推荐。实际服务可用性、地区限制与歌曲授权不保证。旧ID只有编号时GD不补造歌名，搜索曲目保留gdPicId/gdLyricId供播放后补图。

独立Chrome本地整App实测《西湖》能播，音频时长253.99秒，零页面错误、无Cookie。虚构数据浏览器回归390/320宽度通过搜索、收藏、播放、歌词、元数据保存与重开；每个场景4次模拟音乐请求，无真实模型调用。新增6项适配回归及可重复脚本scripts/check-gd-music-ui.cjs；语法/diff/开机检查及主测试集4577项通过。另跑根目录node --test（补最后一项定向测试前）：4703项中4701通过，另2项tests/auto-refresh旧断言已在HEAD核实同样失败（缺impression及旧自动更新标题），不在本轮顺改。公共Cloudflare/Netlify未动。

## 第三十七轮：好感结算与关系起点（2026-09-08）

Lisa授权仅家里。bumpAff统一按模型幅度乘0.2，单次限±1；0/无效值不落盘，删除心情关键词、随机漂移与旧注释。单聊线上/线下、互通群线上/线下、礼物反馈仍走同一入口，既有侧房/封闭群/NPC边界不变；未新增其他入口结算。setAff支持函数式增量，批量结算与和好+1读取最新状态，避免闭包覆盖。

手动提高关系起点时补足新基线，已有高分保留；取消±12容差与整段平移。降级标签、相同基线重复保存不扣分/重置；情侣邀请接受仍只更新情侣状态，不直接补80。旧存档不批量迁移。分手扣5～10及七天/恢复一半的复合条件保留，新速度待实际体验后再调。提示词与模型调用不改。

旧平移测试替换为真实setAff/bumpAff/saveRel函数行为测试：零值/非法值、幅度与封顶、批量累加、旧存档、关系两方向、62/63边界、重复保存与角色间关系；旧和好/NPC接线断言同步函数式写法和有限数检查。主测试集4579项、语法与diff检查通过。无真实模型调用、无用户存档操作；仅家里发布，公共两站不动。

## 第三十八轮：主动私聊收件人归属与开场压力（2026-09-08）

Lisa反馈角色将室友生活细节对用户说成“你”。未取得当轮实际请求，不能断定素材来自哪条记忆。已确认主动开场遗留逐句独特性与强制换新事要求；删除该规则和过期注释，改为允许普通简短、避免固定流程。近六次开场仍保留，只提示不机械复述，不强制换素材。

proactiveHintAll公共拼接明确当前收件人姓名，第三人身份/物品/经历保持归属，未知不补造；旧版与V2主动私聊、约回等入口同步。普通被动消息不加这段；群聊与线下使用自己的场景与对象，本轮不改它们的提示或动念路由。无新增调用、无存档迁移、无真实模型测试；行为效果待Lisa实聊验收。仅家里，公共两站不部署。

## 第三十九轮：日程与天气退回生活背景（2026-09-08）

Lisa授权撤掉日程/天气催报备，不删数据。SCHEDULE_CONTEXT_RULE公共定义背景用途与话题相关性，buildBundle消费者、群线上/通话groupNowSegs、群线下行程段共用；既有未启用时间感知/工程师/空日程边界保留。天气缓存仍随日程附带，删除特殊天气必须在互动中带出的压力和内容示范。今日/此刻/下一项/未来三天数据、状态字段更新、重roll规则与API调用不变。

新增真实公共行程块执行测试；成员背景原哈希测试仅归一化本轮明确替换的日程规则，其他字段仍逐字验证。无真实模型调用或质量对照，不把单次重roll较好归因于去重。只家里，公共不部署。

## 第四十轮：撤除误触换装与聊天任务压力（2026-09-08）

Lisa授权保留历史与日程继续修。WearingRefresh删除用户动作关键词触发器及唯一调用点的文本输入：用户、第三人、引述、询问或指令都不直接认定为角色已行动。缺档、pending、日程切换检查保留；模型回复中角色实际决定的wearing协议与存储不变。

普通单聊共用稳定协议（主房/侧房）撤掉能力轮数KPI，保留日常使用许可、recall纠错与补发、完整字段字典。既有两小时时间间隔提示改为事实边界：不默认爽约，也不虚构共同活动已完成，不催行程报备，情绪仍依人设关系。此次是修这些入口特有旧规则，不向群聊、线下、通话和探针新增额外时间或穿着任务；日程公共背景规则保持全消费者共用。无新增调用、无用户数据变更。

新增真实门控执行、单聊接线、gapHint表达式执行回归；旧逐字提示词测试同步新契约。全量4586项通过（包含开机），语法与缓存指纹检查通过。没有真实模型质量测试，不能宣称已解决所有报备回复；仅家里交付，公共两站不部署。

## 第四十一轮：单聊最终请求手动导出（2026-09-08）

Lisa授权抓取实际请求。复用captureWirePayload和既有CtxDebug，不新建第二套抓取器；手动开关仍仅内存，默认关闭，保留最近八个发送尝试。原线下入口不变，新增tag=聊天的单聊入口（含重试），三种协议共用最终body捕获，不改提示词、预算或请求。角色设置中开放现有诊断区，明确记录跨角色单聊/线下，并按时间、通道和尝试类型识别；可停止、刷新、清空、展开和单条导出。saveTextFile复用原生壳/分享/下载通道。

不捕获headers，不保留endpoint，常见认证字段和各协议图片块省略。文本仍含聊天/人设/记忆，不是匿名包，页面明确提醒勿公开；用户自行写在普通文本里的敏感内容无法保证自动识别。没有持久化、云同步或额外模型调用。未改顶栏/底栏/根节点/滚动容器。

真实捕获函数测试覆盖关闭/范围/八条上限/停止/协议图片脱敏及原body不变；真实CtxDebug组件按钮行为覆盖开关、刷新、导出、清空，静态检查窄屏换行与原有布局约束。全量4591项、语法及diff检查通过。未做真手机分享验收、未获取Lisa实际请求；待她正常聊一轮导出诊断，仅家里发布。

## 第四十二轮：撤掉公共反模板规则的报备引导（2026-09-08）

依据Lisa手动导出的真实单聊请求：当前活动在今日/此刻/末尾穿着任务出现三次，世界书亦要求分享优先。本轮只修改公共STOCK_REPLY_BAN两行，撤掉“先从自己正在干什么起”与替代话题示范，不指定开场/回应顺序；既有buildBundle及群消费者共享，不改世界书、人设、历史、记忆、日程或生成参数。

状态只读核查：freshLiveStateValue穿着18小时、动作45分钟过期返回空；无字段时间戳也返回空。调用方将这些情况统一表述为未建档。请求距上次聊天约18小时，但没有原状态字段时间戳，不能据此宣称存档丢失或确定此例的过期原因。putLiveField相同值不更新时戳是既有防旧状态续命机制，本轮不改有效期/重建/写入；下一步如修应区分变更与确认，不能直接让旧值永不过期。

新增公共规则及真实状态读写函数执行测试，旧反模板措辞测试同步。只在家里交付，公共不动；诊断文件私密内容不复制进仓库测试。实际聊天效果待用户继续导出对照。
