# 台账复核与本轮清洁

起点：`7ad7eb5d`，工作区干净。Lisa 本轮要求审计、清洁；按台账「一次一座山」，本轮选择云备份公共机制，并修复台账点名的跨午夜测试。没有修改用户存档。

## 逐项复核

| 台账项 | 当前证据与处理 |
|---|---|
| 自创顶栏 | 保留。涉及已调过的安全区，需一次一页、真机复核，本轮未改布局。 |
| PUSH_WHY 重复 | 具体原因已由 `pushWhy` 统一：自动备份 toast 调用它，设置和通讯录读 `pushState().why`。旧描述撤下；发现横幅标题不足一天也报一整天，作为具体剩余问题登记。 |
| 成功状态公共机制 | 手动 push 已用 `markSynced`，autoPush/autoPull 仍各自写成功戳和清警告。本轮将三条成功路径统一到 `markSynced`，保留原本成功/失败门槛。 |
| 超时计时器 | `Promise.race` 赢了也不会取消另一个计时器。补 `finally(clearTimeout)`；不改变 45 秒上限，不宣称取消底层网络请求。 |
| maxTokens | 不含 trpg，数字字面量低于 65535 为 19 文件 146 处。更新台账统计；本轮没有改模型调用。 |
| app.js 大文件 | 保留；本轮业务代码没有增加到 app.js。 |
| 跨午夜测试 | 固定测试时钟，增加午夜前后行为断言。产品日期算法保持原样。 |

## 旧审计报告的抽查

`AUDIT-REPORT-2026-09-04.md` 的结论不能直接当作现状：成长准则 `GROWTH_RULE` 已在 v63.63 删除，源码留有说明；`_normalTaskFull` 回滚串仍存在，留待独立审查发送链与引用后处理。

全量测试还列出 15 个零引用候选定义（包括旧手机详情组件、图标与辅助函数）。零引用扫描是线索，可能涉及动态访问和跨文件使用；本轮未据此盲删。

## 验证

- 改动前隔离副本：4490 项，4466 通过、24 失败。
- 改动后：4494 项，4470 通过、24 失败。新增四项行为测试全部通过；失败名称集合与改动前一致。
- 新测试覆盖：自动备份失败后重试、恢复 apply 失败/成功、thenable 成功/拒绝/挂起的计时器释放、跨午夜时间显示。
- 测试云客户端和存储均为内存替身，没有读写真实云存档。
- 全量测试未全绿；既有失败已入台账，不等同于都只是过时测试。
- 没有操作 Lisa 的真机；这里的验证是源码、完整脚本沙箱与自动测试，不冒称真机验收。

本轮收口范围是云备份成功状态与计时器、测试时钟及台账复核，不是全库重构完成。

## 第二轮：公共段后面又长出的三套背景

Lisa 继续点名「差不多的东西写了几版，修一处忘另一处」。本轮以 `35ccc37b` 为起点，集中收口三条群路，未改页面布局。

发现：`groupNowSegs` 原本收了心情、好感、年龄、行程等基础段，但后加的成长、情绪底色、睡眠、居住地、随身物、情侣档案又在群文字和群通话各写一遍；群线下另外读字段、拼文字。已有公共函数没有覆盖后加层，就是这次重复的落点。

改为两层：

- `groupBackgroundFor(c)` 统一六层读取，配角返回空对象。群线下六张成员表也从同一次读取产生。
- `groupBackgroundSegments(c, background, userName, opts)` 统一六段格式与档案隐私围栏。群文字与群通话经 `groupNowSegs` 使用；群线下按成员传入同一个格式函数。

保留差异：线下成长描述的叙事措辞由 `narrative: true` 指定；文字群额外的歌单和跨群连续性不搬进电话；即时穿着/动作仍受互通开关控制。单人线上/线下/单人通话继续由 `ctxFor`、`buildBundle` 提供背景，本轮没有把群用短段替换进单人提示词。

验证证据：用重构前保存的真实三条 `memberDesc` 和现在的实现分别运行同一份测试数据，两位主角色加一名配角，三份输出逐字相等。结果哈希固定进 `test/group-background-shared.test.js`。新增六项行为测试覆盖输出等价、私有档案归属、配角排除、空字段、六张线下成员表、闭群即时状态开关。

相关旧测试从「必须在两处写一段」改成检查公共读取/拼接接入，保留人设、背景、隐私约束；其中一条原已失败的测试是把拼接字段顺序钉死，改为检查各字段确实进入成员段。全量 4500 项，4477 通过、23 失败，失败集合为上轮基线的子集，无新增失败。语法和启动冒烟通过。没有调用真实模型或操作用户存档，也没有真机验收。

## 第三轮：日期公共方法与台账清洁

起点 `8c9acfd6`。日历、经期、行程的日期生成和解析合并到 `ScheduleClock`：新增 `formatDayParts`、`deviceDayKey`、`parseDayKey`，现有 `dayKey` 与 `shiftDayKey` 也共用格式方法。八个业务入口只转交参数，移除固定 24 小时位移和日期模块缺席时的另一套算法。实际脚本顺序已先加载 `schedule-clock.js`。

旧存档格式明确保留：`calKey`、`pDK` 将 `legacy` 参数传为 `true`，生成不补零键；`calPadKey`、行程日期用补零键。没有转换或重写已有存档。两种格式均可按设备当地日期解析；角色时区逻辑保持原有实现。

测试覆盖旧日历写入/读取、三地时区（温尼伯、上海、悉尼）、夏令时切换、闰年、跨年、真实装载顺序，以及日历已有分支渲染与坐标回归。定向 59 项全过；全量 4506 项、4483 通过、23 项既有失败，失败集合与第二轮一致。页面结构、安全区和主屏布局未改，没有真机验收。

台账撤下完成的日期条目，旧失败数直接改为当前数字，删除尾部已完成事项的补充段。施工规则直接登记现行公共入口；历史验证证据集中在本记录中。

## 第四轮：扩展重复扫描与备份显示收口

起点 `d9aa3c5e`。扫描范围为 `js/` 顶层的 114 个 JS 文件，共 104032 行；不是整个仓库全部语言的行数。算法去除空行、单行注释和首尾空白，再比较连续六行，至少 220 字符；发现 209 个重复窗口。窗口互相重叠，不能等同于 209 个独立缺陷，也不会发现所有改过变量名的近似重复。

复核结果：

| 位置 | 现状 |
|---|---|
| `OfflineMode` / `GroupOfflineMode` | 文风编辑、导入、往期记录、照片入口有长段复制，已入台账。 |
| `maybeAutoExtractGroup` / `maybeAutoExtractGroupOffline` | 归属过滤、证据字段与群来源存在实际分叉，已入台账；本轮未改记忆写入。 |
| `summarizeOffline` / `summarizeOfflineGroup` | 存在相同输出尾段，需按单人/群语境再核对，未认定可以整体替换。 |
| `theater.js` / `trpg.js` | 账本规则相似；遵守 trpg 专属维护边界，未修改。 |
| 备份状态三处显示 | 本轮已统一到 `Cloud.pushState()` 返回的显示字段。 |

备份显示：公共层生成 `needsAttention`、`headline`、`summary`、`detail`；设置首页、云同步详情和聊天横幅直接使用。删除页面里的重复判断、时间计算以及过期病史注释。近期备份成功后发生失败，标题为「这次备份未成功」；达到 24 小时才按天数提示。保持真实备份读写和既有告警阈值不变。

行为测试通过真实 Cloud 脚本和内存存储验证未备份、近期成功、近期失败、24 小时边界、恢复成功；直接运行横幅渲染段验证隐藏、标题、固定层级与点击入口。定向 26 项通过，全量 4508 项、4485 通过、23 项既有失败，失败集合与第三轮一致。页面尺寸和安全区未改；没有真机验收。
