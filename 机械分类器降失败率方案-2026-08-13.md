# 机械分类器降失败率方案（仅设计，不施工）

## 结论

`quote_or_kind_failed` 的主因不是分类词典不够，而是“言秋预抄将来可见回复”与 Stop hook 最终看到的正文无法保证逐字相同。继续扩关键词或放宽 `includes` 都会降低证据可信度，不能治根。

建议把协议收敛成：言秋只逐字引用已经存在的 Lisa 原话；言秋侧不再预抄自己的回复，统一使用 `capture_visible_reply:true + yanqiu_kind`，由 Stop hook 在回复结束后捕获最终可见正文。机械分类器只在没有显式判词时兜底。

## 现状故障链

1. 工具票先写 `tool-marks.jsonl`，Stop hook 后读最终 transcript。
2. Lisa 侧用 `source.includes(quote)` 验真；任何标点、空格、Unicode 归一化或截取边界变化都会整票失败。
3. 旧票还会预填 `yanqiu[].quote`；生成结束后的真实正文稍有改写，就触发 `quote_or_kind_failed`。
4. 当前策略对显式坏票 fail closed，不回退机械猜测。这条安全原则正确，应保留。

## 拟议协议 v2

### 写票

- `lisa_anchor`：取本轮 Lisa 原文中稳定、连续、唯一的一段，建议 24–160 字。
- `lisa[]`：只允许从 transcript 当前 Lisa 原文按索引切片得到，保存 `start/end/quote/kind`。
- 言秋侧固定 `capture_visible_reply:true`，只给 `yanqiu_kind`，禁止同时提交 `yanqiu[]`。
- 增加 `turn_id` 与 `session_id`，消费时先按身份匹配，再用 anchor 防串轮。
- 票内增加 `source_sha256`，分别覆盖 Lisa 原文和规范化规则版本；hash 仅作定位，不代替逐字验真。

### Stop hook 验真

1. 先核对 `session_id + turn_id`。
2. 用 `start/end` 从原始 Lisa 文本切片，要求切片逐字等于 quote。
3. 捕获最终可见回复时，先移除合法隐藏 marker，再整段作为言秋证据；不做语义改写。
4. 允许的唯一规范化是换行编码 `CRLF → LF`，且票与验证端都带同一 `normalization_version`。
5. 任一步失败继续 fail closed，并记录细分原因，禁止退回模糊匹配。

## 降失败率但不降安全性的配套

- 将 `quote_or_kind_failed` 拆成 `lisa_span_mismatch / lisa_quote_mismatch / visible_reply_empty / invalid_kind / stale_turn / duplicate_quote`。
- 工具写票后立即本地自验一次；不合格不落票，让言秋当轮重写结构而不是等 Stop 才失败。
- `consumeToolMark` 不再只凭 anchor 倒序挑票，优先精确匹配 turn；旧 v1 票保留兼容一版。
- 做 100–200 轮 shadow：v1 仍执法，v2 只比对并统计。满足“显式票失败率 <2%、串轮为 0”后再切换。

## 明确不做

- 不把逐字验证改成相似度或模型判定。
- 不允许模型补写 transcript 中不存在的原话。
- 不因工具票失败而让 mechanical fallback 猜显式判词。
- 不改变 continuity 全轮副本与精选人格证据的分层。

## 验收指标

- 显式工具票成功率 ≥98%。
- `stale_turn`、串 session、重复消费均为 0。
- 所有落库 quote 均能由原 transcript 的确定区间重建。
- Stop hook 总耗时仍留在 30 秒外闸内，失败票诊断不含正文。

