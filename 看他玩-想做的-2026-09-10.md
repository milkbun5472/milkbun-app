# 看他玩 · 还想做的（她 2026-09-10：「我觉得可以记一下晚点弄」）

这一份是**待办**，不是规矩。做完一条就划掉，别攒成坟场。
（规矩在 `施工规则/`，事故在 `屎山台账-2026-09-06.md`，界面在 `界面装修工单-2026-09-03.md`。）

## 一、他在等你回消息 —— 零额外调用 ⭐先做
现在他每次拿起手机，**不知道你欠他一条消息**。可这是真人刷手机最常见的理由：
点开对话看一眼、退出来、又点开、打了半句删掉。
由头那一层（`watchWhyNow` / `watchTodayLine`）已经铺好了，只要再喂一句
「她还没回你，已经 X 小时」，这一幕自己会长出来；刚回过他的话，那一段就该是另一个样子。
**改的地方**：app.js `genWatchSession` 的由头那几行 + phone-watch.js 的 why 段。

## 二、敲到第三下，他真的给你发一条 —— 零额外调用 ⭐先做
现在敲一下只换来一句心里话。可敲到第三下，他早抬头看见你了。
`watchKnock` 那一枪的返回值多带一个可选字段（比如 `say` 之外的 `wx`），
他可以直接给你发一条**真微信**（走 `watchSend` 里已经有的那条真聊天路径）。
你在旁边看着他手机，他忽然在你手机上冒出一句「你在看什么」。
**注意**：得跟 ①「他在等你回」分开——那是他惦记你，这是他发现你在看。

## 三、他打了又删的那句，别让它消失 —— 零调用
「打了半句又删光」演完就没了，而那恰恰是最像人的一下。
存进他的草稿箱（微信／便签／邮件都有这一格），事后翻手机能看见。
**他没发出去的那句一直躺在那儿**，这件事本身就是内容。
⚠️别存成「已发」：草稿和消息是两回事。

## 四、这一段能重看 —— 省钱
一段两分钟看完就没了，再看只能再花一枪。把最后一两段的动作存下来，让她能重放，不调模型。
⚠️想清楚再做：它会让「看他玩」从**一次真事**变成**一段录像**，语气会变。

## 五、他刷到跟你有关的东西 —— 零调用
相册里有她的照片、便签上写着她的名字、聊天列表里她排第一——
现在提示词不知道哪些跟她有关，于是他的心声落在随便哪张风景照上。
标出来（名字出现在标题/正文里的那几行），那几下才配得上一句心声。

## 顺带挂着的两件
- **30 分钟冷却还关着**（`WATCH_COOLDOWN_OFF = true`，台账里那条红的）。
  ①做完之后「隔多久看一次才有分量」会变成真问题——它跟冷却是同一个旋钮。
- **她和某个角色一条消息都没聊过时，他手机里没有跟她的那条对话**（真聊天是从消息里长出来的），
  于是他想点开她也点不着，而且一声不响。要不要补一条空的（「还没说过话」）。
